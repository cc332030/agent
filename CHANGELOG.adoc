= 变更日志

本文件是本规范集合的**统一变更日志**（命名与位置见 `README.adoc`「目录结构」），按**时间倒序**记录、最新在最上方；条目格式为 `版本 | 日期 | 变更摘要`，单行、一条一个变更点、只写"改了什么、给引用方带来什么影响"。

条目撰写规则见 link:specs/general/changelog.adoc[]，版本与倒序机制见 link:specs/general/doc-design.adoc[]「版本与变更记录」；**本文件的变更条目不适用于 `specs/` 规范文件本身**——规范变更由其 git 历史承载（见同节）。

> 历史条目中的路径为**当时位置**（按规范不保留历史来源注记的口径，不在旧条目里改写路径）。当前分层以本文件 3.2 条与 `README.adoc` 目录结构为准。

- 3.7 | 2026-09-12 | **依据图书馆的分篇判据与『规范准入与自身取舍』主题**：此前"内在依据"一律"不另建主题文件"、就地散在各规范文件与提示词里，导致两类扫不到——①**准入判定与新条目审校**要走遍所有文件才能收齐依据（用户发起规范议题时最常撞上）；②本站**更严取舍与组织约定**（配置类不写逻辑严于框架常规用法、先例优先严于"JDK 优先"直觉、NPC 禁合并属组织约定）与**外部标准原文**同处一馆却未分清"对谁成立"，读者会把本项目口径读成"某标准规定的"。现入口 `library/README.adoc` 新增「**何时该给一类依据独立成篇（判据）**」（**与规则本体分离** 且 **按"依据对谁成立"归类**，命中其一即不该独立成篇，**不预建空壳**），并按第二条判据新增主题 `library/adoption.adoc`——准入判定的依据（ISO/IEC/IEEE 29148 的可验证/可追溯、ISO/IEC Directives Part 2 与 RFC 2119/8174 的用语强度、ISO 9241-110 的可控性"借用其原则"），并**逐条如实标注同义性差异**：标准只给方向与下限，本集合的更严取舍与组织约定的每条都写明"**未**规定此类要求"
- 3.7 | 2026-09-12 | 机械防线：`check_library_guard` 扩到新主题——`LIBRARY_TOPICS` 增 `adoption.adoc`，并新增该主题的**要点锚点**校验（"同义性差异与覆盖点"节名 + 三条取舍的判据句），防"取舍判据被删而依据仍凑得齐"→ 又退化成把本站口径当标准原文；配套用例 3 条（锚点缺失报 / 锚点齐备过 / 新主题未登记报），单测 154 → 157
- 3.7 | 2026-09-12 | `library/adoption.adoc` 新收「图书馆自身的落点裁定」一行（仓库根私有、不随分发）并指向既有论证、不复制结论；`AGENTS.adoc` 图书馆段补④独立成篇判据与防线口径（体积仍在 24000 B 上限内）
- 3.6 | 2026-09-12 | **公开面残留旧口径清理**：`README.adoc`「子 agent 复核必须自带硬超时、留证按三态台账」仍写"语义复核派给子 agent（**或外部复核者**）"——上一轮已把来源选择收紧为强制同 Agent、不得点名外部 Agent/NPC，公开面这一句却还把外部复核者列成正常选项（引用方按它读会继续派外部执行者）。现改为"派给子 agent（同 Agent 强制，不把外部复核者当选项）"，与 `specs/general/collab.adoc` 同口径
- 3.6 | 2026-09-12 | **机械防线补漏：`check_delegation_guard` 对"强制同 Agent"改为双向设防**——上一版只核"新口径的要素仍在"（同 Agent 判据 / 不得点名外部 NPC / 判定标准 / 降级路径 / 一次性调用），于是**另一种同样致命的破坏**静默通过：把**许可范围悄悄放宽**。实测两处漏检：①`check_priority_guard` 只读 `=== P6.` 条目正文，**清单概览行**（`* **协作执行者选择（P6…）**`）回到旧口径"优先同源、外部来源作备选"时全绿；②把放宽语句**另起一句**追加（"拿不到同 Agent 时也可以换外部 Agent 顶替"）时全绿——两种写法下"不得点名外部 Agent / 外部 NPC"等字样都还在，而按它读的人会把外部执行者重新当成合法路径。现按**放宽形态的正则**在五处落地文件（`collab`/`testing`/`self-check`/`priority`/`verify`）集中设防，**禁止式表述与反例引用不误报**（"不得用外部 Agent 顶替""不得改回『外部来源可以顶替』的宽松写法"），且否定词只认命中片段紧邻的那一小段——防"前半句禁止、后半句放宽"被误放行（本项目真实形态）；用例 295 → 299（新增"清单行回旧口径""另起一句放宽""前半句禁止后半句放宽""禁止式不误报"四条）
- 3.6 | 2026-09-12 | **最高关注项 P6 与公共侧口径对齐（同一轮收紧的漏改）**：`specs-project-maintainer/priority.adoc` 的 P6 仍停在旧口径（"优先用与执行者同源、外部来源作同源不可用后的**备选**"），而公共侧已收紧为"**强制同 Agent、不得点名外部 Agent/NPC、不可用时本人串行或如实标悬置**"——同一件事在清单里读到的与在规范里读到的相反，且清单更显眼（最高关注项先被读到），实际后果是继续把复核派给不可核对的外部执行者。现 P6 改为**独立的 `=== P6.` 条目**（含要求/依据/判定标准/重构防误改四处），口径与公共侧一致，并如实写明"换来源不再因'更独立'而合法"；未被防线的"漏改点"一并补上：`specs/general/testing.adoc`（三视角复核对执行者的表述）、`specs/general/self-check.adoc`（环境能力自评里的子 Agent 判定）由"优先同源"改为"同 Agent 强制/不得点名外部"
- 3.6 | 2026-09-12 | 机械防线：`check_priority_guard` 对 P6 由"只核小标题/级别/依据行"改为**同时核正文口径**（强制同 Agent 三处要点须在 + 旧口径字样不得复活），并把 P1-P5 清单扩为 P1-P6；用例覆盖"P6 被删/被换成旧口径/只剩关键词"三种反例。漏改根因记录在此：上一轮只改了公共侧 `specs/general/collab.adoc` 等文件，而最高关注项清单里那段**同义旧文本**无人核对——防线当时只核格式（`**要求（L1，最高`、`**依据**`），旧口径全绿
- 3.6 | 2026-09-12 | **配置类不写逻辑（L1，跨语言）**：承载配置的类（Spring `@ConfigurationProperties`、`@Configuration` 等，其他语言同类）**不得包含任何逻辑——任何情况都不允许、无例外**，只保持 POJO 的基本功能；取值兜底、条件判断、格式转换、派生值等一律下沉到 utils/service。落点：通用层 `specs/general/coding.adoc`「类设计」（条文 + 可逐条核对的判定标准 + 识别特征）、技术栈层 `specs/stack/java.adoc`（标注 `@ConfigurationProperties` 的类一律属配置类）与 `specs/stack/spring.adoc`「配置」（点名两类配置类），并被 `README.adoc` 目录说明同步
- 3.6 | 2026-09-12 | 机械防线：新增 `check_config_class_guard`——钉住通用层条文（含『任何情况都不允许』与『逻辑下沉到工具类或服务』）、判定标准（条件分支/循环为零、不做计算与对外访问、`@Bean` 装配形态）、Java/Spring 两侧识别特征、与框架既有做法的边界（该条严于 Spring 常规用法、存量随动迁移）与 `README.adoc` 同步；配套用例 12 条（单测 272 → 285、`check_effective` 48 → 49 条，抓手缺失 0），要点随 `AGENTS.adoc` 工具声明登记
- 3.3 | 2026-09-12 | **按 CI 内 agent 的踩坑报告补齐 CICD/CNB/执行规范**：CI 侧新增「校验链完整（定义未执行防线）」（流水线须跑全既定校验含配套测试、测试文件须能被框架自动发现、不在 CI 调用非确定性 AI、CI 只守护不代改）、「触发与作用范围」（触发路径须覆盖校验对象）、「依赖与外部资源可用性」（镜像/制品先确认实际可用）、「超时与资源」（每次执行须有可判定超时、防长期 pending）
- 3.3 | 2026-09-12 | CNB 平台规范新增「对象钉定与可追溯」（派发与复核须钉定 commit sha、压缩提交/强推会替换对象且旧 sha 结论须在新 sha 重核、CI 状态按 sn+sha 定位）与「协作与执行者可用性」（派发前确认执行者可用、派发须自带超时、流水线不无界挂起）
- 3.3 | 2026-09-12 | 执行规范同步：git 侧新增 rename 未被识别的两类**非操作类**根因（合并/基点错位、一拆二/内容再分配）与核对、提交历史须按目标分支基线组织；验证侧新增「验证须覆盖项目全部既定校验手段」（"存在测试"≠"测试被执行"）与「验证对象须钉定 commit sha」；协作侧新增派发对象钉定 sha 与派发前确认执行者可执行
- 3.3 | 2026-09-12 | 机械防线扩展：新增 **CI/CD 与平台协作防线**（钉住 CICD 校验链完整/触发范围/依赖可用/超时/非确定性 AI、CNB 钉 sha/强推对应关系/执行者可用性/不无界挂起，及 verify/collab 两处通用落点）（单测 195 → 202）
- 3.6 | 2026-09-12 | **与 #38（CI 内 agent 踩坑报告）的规范补齐合并合流**：CICD/CNB/验证/协作四块新增条目与主干随后的「NPC 禁合并」「环境标志与专用口径 `env-guard`」「子 Agent 强制同 Agent」并存——`specs/platform/cnb.adoc` 由四节构成（分支与合并请求统一 / 冲突处理 / **合并请求的合并主体（NPC 禁合并）** / **对象钉定与可追溯** / **协作与执行者可用性**），并显式写明两条禁令的边界（「冲突处理」的自动解决冲突与「对象钉定」里提到的合并，执行主体恒为人工）；机械校验口径同步（`check_specs.py` 的检查项清单描述合并为一处，`check_specs_test.py` 同时保留 `TestCheckEnvMarkerGuard`/`TestCheckNpcMergeGuard` 与 `TestCheckCiCdGuard`；单测 272 + 7 = 279）
- 3.6 | 2026-09-12 | **CI/CD 与平台协作防线并入检查链**：`check_ci_cd_guard` 与既有防线共存并纳入全量校验（引用存在/节名/调度器登记等四口径对 CICD 与 CNB 新增节同样生效）；「定义未执行」核验清单由 48 条增至 52 条（新增 CICD 校验链完整 / 触发与依赖可用与超时 / 平台钉定 sha 与执行者可用 / 验证覆盖全部校验手段四条，抓手缺失仍为 0）
- 3.6 | 2026-09-12 | **NPC 禁合并（L1，唯一一类"用户明确要求也不照做"的操作）**：CNB 平台上由 NPC/CI 环境唤起执行的 agent **严禁合并合并请求**——**即使发起人明确要求或给出直接授权也必须拒绝**（授权不免除，因为失效目标是不可逆且合规相关的合入动作）；**只交付到"创建/推送 PR 分支并汇报"为止，合入一律只由人工完成**。落点：`specs/platform/cnb.adoc`「合并请求的合并主体（NPC 禁合并）」（禁令本体、无豁免、可逐条核对的判定标准、与「冲突处理」不矛盾的边界），并被 `prompts/_common.txt` `delivery` 片段、`PROMPTS.adoc` 公共约定与 `README.adoc` 同步（提示词会被未知项目复制执行，只改规范等于没改）
- 3.6 | 2026-09-12 | 子 Agent 选择由"**优先**同源"收紧为"**强制同 Agent、不得点名外部 Agent/NPC**"（L1）：换来源曾是合法路径，而"同源不可用"无判据可判，于是"随手抓一个外部 Agent 复核"总能自圆其说——实证两次失败（派发即 error、1 h+ 未回传），复核对不上口径也等不到结论。现写明同 Agent 判据（同 Agent 身份 + 同入口/调度器）、**不得点名外部执行者**的三条判定标准（派发目标/交换面/回传面）、不可用时**只降级为执行者本人串行 + 如实标悬置**、不换任何外部来源；`specs/general/testing.adoc`、`self-check.adoc`、`specs-project-maintainer/verify.adoc` 同口径对齐
- 3.6 | 2026-09-12 | 依据入库：`library/sources.adoc` 新增「变更审批与'合并主体'」（DORA 交付性能研究、GitHub Docs 受保护分支与 required reviews）——**如实标注同义性差异**（标准**未**规定"某类执行者绝对不得合并"，本集合据此给出的是**本项目自身的组织约定**）与**本项目实证反例**（曾出现"被直接授权后实际执行了合并"）
- 3.6 | 2026-09-12 | **提示词新增环境标志与专用口径 `env-guard`**：把"只在某个执行环境成立的做法"（容器已复制仓库、默认分支不是合并目标、`pnpm`/`mvn` 可能要 `npx` 包裹、缺工具不扩大改动）从注释与记忆里收进规范，形式是**可实测的标志**（`printenv CNB_EVENT` 非空且触发内容含 `@<Agent名>`）+ **未命中时的中性口径**（按项目构建文件正常执行）——写进提示词就当通用要求会让别的环境照着做而失败；标志取值登记在维护方入口 `AGENTS.adoc`
- 3.6 | 2026-09-12 | 机械防线：新增 `check_env_marker_guard` 与 `check_npc_merge_guard`——前者钉住片段标志/触发条件/中性口径、各提示词仍 `include`、登记处写明标志；后者**钉住七处要点**（禁令本体 / 无豁免 / 可核对判定标准 / 与「冲突处理」的边界 / **同文件既有两节不得被新节顶掉**（本轮实测犯过：新增节点把「冲突处理」整段替换，规则凭空消失而校验全绿）/ 提示词片段同口径 / 公开提示词入口同步）；`check_delegation_guard` 由"钉旧字样"改为**多要素核对**（防关键词堆砌假绿），`check_public_content_coverage` 的"列进清单"改**逐格判定**（说明列里顺带提及不再算列进，实测漏报已修）；单测 252 → 272、`check_effective` 47 → 48 条（抓手缺失 0）

- 3.3 | 2026-09-12 | **新增依据图书馆 `specs/library/`（可逐字核对的依据落点）**：规范正文刻意只给"怎么走"（规则 + 判定标准 + 依据名），但依据只存在名称时，规则执行久了就退化成"只记得是这么做的"——无法判断它还成不成立（link:specs/general/verify.adoc[]「验证总纲」的"防慢慢脱离初衷"）、无法据以取舍与举一反三。现按主题存放依据：**外部标准的逐字原文摘录**（IEEE 1028、IEEE 1016、ISO/IEC/IEEE 29119-4/25010/29148、ISO/IEC Directives Part 2、ISO 10007、ISO 9001、ISO 9241-110、RFC 2119/8174/3629/3339/6749/9700/7617/8018、NIST SP 800-63B、OWASP/CWE、SemVer 2.0.0、PEP 8/20/257/508、POSIX、GNU Make、Maven Surefire/Failsafe、git、Microsoft Learn、The Twelve-Factor App、JaCoCo/PIT、Google 风格指南、ShellCheck、阿里巴巴 Java 开发手册），并逐条标注它支撑哪些规范条目、哪些是**节选**、哪些**未逐字取回/标"未确证"**、哪些属**借用原则而非标准原文**（同义性如实说明）；入口 `specs/library/README.adoc` 登记全部主题，**按需懒加载、不属必加载层**（常驻体积不变），适用面**不限于规范依据**（项目里任何需要找依据的东西都可入馆）
- 3.3 | 2026-09-12 | 依据图书馆**纳入公共内容**（随规范分发给引用方）：登记在 `AGENTS_COMMON.adoc` 加载调度器（触发特征=需要查依据/要决策或复盘/核对依据是否仍成立），README 目录结构与使用要点同步；图书馆**不复制规则**（规则本体在 `specs/` 各文件），只补依据
- 3.3 | 2026-09-12 | 机械防线扩展：新增**依据图书馆防线** `check_library_guard`——图书馆入口/主题文件须存在且被调度器登记、入口登记与实际主题**双向一致**（防"建了文件没登记"与"登记了空壳"）、外部标准**逐字引文锚点仍在**（防依据被压成名称）、馆内**不得出现私有落点或私有抓手名**（图书馆是公共内容，引用方读到即死链）（单测 197 → 205）
- 3.3 | 2026-09-12 | "定义未执行"核验清单补一条（依据不得只剩名称，有抓手）；`check_effective` 登记 45 条、有抓手 43 条
- 3.4 | 2026-09-12 | **图书馆搬出公共内容：`specs/library/` → 仓库根 `library/`（本仓库私有）**：`specs/**` 是随规范**整个分发**给任意项目的公共内容，公共内容里的一个字节都会被未知项目取到；而依据图书馆天然包含**只对本项目成立**的内容（当初的失效、取舍、实证、选型理由）——放进公共内容，要么被改写成"对谁都不犯错"的空泛表述（依据失去价值），要么把本站处置口径（可关闭、不新增要求、冲突以引用方自身规范为准）反复声明一遍。现改为：图书馆落在**仓库根**、属本仓库自己的内容（与 `prompts/`、`script/` 同性质），按需加载、不进任何常驻层；**引用方不再收到它**，`AGENTS_COMMON.adoc` 加载调度器登记与 `README.adoc`/`PROMPTS.adoc` 里的"分发给引用方/可关闭"口径同步撤销。**只有规范条目本体的"依据名"在公共内容里，依据全文回到本站**
- 3.4 | 2026-09-12 | **已分发的路径被撤回（兼容性事件，须让引用方知道）**：上一版把图书馆放在 `specs/library/` 并**已写进公共内容**（`AGENTS_COMMON.adoc` 调度器登记 + `README.adoc`/`PROMPTS.adoc` 的说明）——本次连同登记与说明一并撤回，改由本仓库自己的项目规范入口登记。对**首次接入的引用方**：公共内容净减少（少一条加载项、少一条路径），无新增约束。对**已按上一版读过规范的引用方**：其上下文里可能留有"依据图书馆在 `specs/library/`、可关闭、不新增要求"这段印象，而该路径此后不存在——属"已发出的公共说明被收回"，故在此显式记录：**引用方无需任何动作**（图书馆从来不是对引用方的要求，其规则本体始终在 `specs/`，冲突次序仍以引用方自身规范为准），但若其 AI 仍按旧路径去找依据，会读到 404——这是"旧路径失效"而非"规则失效"。**该结论有前提、不可当通用挡箭牌**：本次撤回的内容**全是说明性/指引性内容**（"要查依据时可按需读某路径"），公共内容里**没有一处**把 `specs/library/` 写成对引用方的要求或接入动作——已逐句核对上一版的三处措辞：`AGENTS_COMMON.adoc` 是"需要查依据时加载"的调度项、`PROMPTS.adoc` 是"涉及依据与取舍时按此核对"、`README.adoc` 是说明书性质的"该目录是什么"；故"引用方无需动作"成立。**若撤回的是要求性内容，结论不成立**（那属改变引用方行为，须显式授权 + 给"不采用时怎么办"）
- 3.4 | 2026-09-12 | `PUBLIC.adoc` 补两项可核对边界（本轮独立复核指出）：①"私有落点"的**四类枚举**（私有目录名/私有脚本名/私有文件/隐含仓库结构的相对路径假设），并写明机械检查只覆盖前三类、第四类交语义复核——**"单条 grep 无命中"只否定命中的那一类，不等于全类自足**；②自足受限集合是两类的**并集**（会被入口取到的 / 会被复制到未知项目执行的），分类只为解释、不进体积与整合口径（同一文件可同时命中两类，如 `INSTALL.adoc`）
- 3.4 | 2026-09-12 | 图书馆**适用面放开**：不再限定"只放规范的依据"——本项目里**任何需要找依据**的东西都可入馆（规范、提示词、脚本、CI 乃至日常决策的依据；技术选型、组织约定、设计取舍），入口 `library/README.adoc` 的主题登记表为唯一索引；仍**不预建空壳主题**（需要独立成篇时再按判据新增并登记），未独立成篇的内在依据仍在各规范文件与提示词正文里就地承载
- 3.4 | 2026-09-12 | 新增 **`PUBLIC.adoc`（公共内容入口索引，维护方内容）**：公共内容此前只有一句口头定义（"`AGENTS_COMMON.adoc` + `specs/`"），而 `INSTALL.adoc`（接入时读）、`prompts/_common.txt`（AI 以纯文本读取公共片段）、`script/clean_tmp.py`（随规范分发的通用工具）**同样会被引用方取到**——覆盖面无清单的后果是双向的：机械检查漏掉半个公共内容，而"自足"要求又被误加到只对维护方成立的文件上。该索引登记于 `AGENTS.adoc`，并列出**公共内容入口清单**与**不属公共内容的文件**（站内说明文档、维护方层、图书馆等）
- 3.4 | 2026-09-12 | 机械防线调整：**图书馆防线**重写——落点由 `specs/library/` 改为仓库根 `library/`，登记处由加载调度器改为**项目规范入口 `AGENTS.adoc`**；**删除"自足性/不得夹带私有落点"两条**（那是对公共内容的要求，私有内容本就可以引用自己仓库的任何落点），改为钉住**馆内引用可解析**（引用悬空即依据链断在这里）与**入口登记双向一致**。新增**公共内容覆盖面防线** `check_public_content_coverage`（入口清单存在且被登记、两个公开入口都在清单里、清单点名文件真实存在）；调度器登记完整性只核对公共内容（维护方私有落点不再被误报为"未登记"）；单测 206 → 212
- 3.4 | 2026-09-12 | **检查集合再补 `prompts/`（上一轮"收全"未竟）**：上一轮把检查集合从 `specs/` 扩到 `library/**` 与仓库根全部 .adoc，但 `prompts/**` 仍漏收——而 `prompts/review.adoc`、`prompts/refactor.adoc` 是**对外分发的产物**（`PUBLIC.adoc` 明写"机械检查按『公共内容』口径覆盖它们"），CI `paths` 亦含 `prompts/**`。漏收后果与此前同类：语法编译/引用存在性/节名引用/链接格式四口径对它们**整体失效**——实测往 `prompts/review.adoc` 注入死链 + 悬空节名，`check_specs.py` 仍 exit 0 静默通过。现把 `prompts/` 并入 `collect_adoc_files`（口径=仓库根全部 .adoc + `specs/`、`specs-project-maintainer/`、`library/`、`prompts/` 递归），使 `PUBLIC.adoc` 的覆盖承诺成立；同时把 `check_public_content_is_self_contained` 的覆盖对象扩到②类（`prompts/*.adoc` 同样不得引用 `specs-project-maintainer/` 等私有落点——此前 `PUBLIC.adoc` 称其受机械覆盖，实测不成立）；`prompts/_common.txt`（非 .adoc、不进集合）的自足性仍无抓手，属已知缺口。单测 237 → 247
- 3.4 | 2026-09-12 | **机械校验覆盖面补齐 + 三处真实漏洞修复**（独立复核指出）：① **检查集合收全**——`collect_adoc_files` 原来只收 `specs/`+`AGENTS_COMMON.adoc`+`AGENTS.adoc`+`INSTALL.adoc`，`library/**` 与根 `PUBLIC.adoc`/`README.adoc`/`PROMPTS.adoc` 全部漏收，语法编译/引用存在性/节名引用/链接格式四口径对它们**整体失效**（实测 `library/README.adoc` 有三处悬空节名无人拦）；现一次收全（仓库根全部 .adoc + `specs/`、`specs-project-maintainer/`、`library/` 递归），集合元素统一为**仓库根相对 POSIX 路径**（可判定的取值）；② **语法验证真正生效**——`check_asciidoctor_syntax` 加 `--failure-level=WARN`：asciidoctor 默认对 WARNING/ERROR 仍返回 0，`include::` 目标缺失、`image::` 找不到这类"只告警"的问题必然漏报；老版本不支持该开关时**显式降级告警**而非静默通过；③ **目录型引用不再整体跳过**——新增 `_is_dir_ref`，`specs/`、`script/` 这类目录引用按 `os.path.isdir` 核对存在性（原实现与占位符混为一类跳过，等于必然漏报）；④ **`LIBRARY_QUOTE_ANCHORS` 补三类取样来源标记常量**（`官方文本已取回`/`官方网页已取回`/`未逐字取回`），防线覆盖到"引文被压成名称"；⑤ **三处悬空节名修正**（`library/README.adoc` 的 `doc.adoc「文档不得注水」`→`「注释与文档」`、`collab.adoc「失联感知与硬超时」`→`「资源限制与感知」`、`encoding.adoc「Windows 批处理必须 CRLF」`→`「换行符（行尾）」`；`PUBLIC.adoc` 的 `AGENTS.adoc「工具声明」`→`「Agent 项目自身工具声明」`）；⑥ **`check_public_content_coverage` 改按节切表格区**——不再"全文 `re.findall` + 5 个 `.adoc` 硬编码豁免"，改为按 `== 公共内容入口清单` 节取表格区、"不属公共内容"段取反向举例，两处点名文件都须真实存在（新增任何合法举例自动纳入，不必回头改白名单；清单节名被改写即报错，不静默不查）；⑦ `AGENTS.adoc`「依据图书馆」段压缩（23822 → 23533 B，落点表述与 `PROMPTS.adoc`、`library/README.adoc` 保持同口径）；⑧ `CHANGELOG.adoc` 作为**只追加的历史记录**豁免引用存在性/节名/链接格式/历史来源四类检查（旧路径"查不到"是记录本身），但**仍纳入语法编译**；CI `paths` 补 `CHANGELOG.adoc`。单测 213 → 237
- 3.4 | 2026-09-12 | `check_effective` 登记 46 条（有抓手 44）：补"公共内容覆盖面须有清单"一条，图书馆那条的来源与抓手口径同步
- 3.4 | 2026-09-12 | 自身侧 `git mv` 自查改用 **`-M10%`**（低于 git 默认 50% 阈值）：本次"图书馆搬家"的入口文件被整篇重写、**相似度实测 11%**（`git diff --cached -M10% --summary` 输出 `rename ... (11%)`），默认阈值下索引里只剩"删除 + 新增"，合规的 `git mv` + 改写被误判成 delete+create（最高关注项 P1 的自身侧抓手会误报）。**阈值理由**：`-M` 用近似索引（默认按 blob 大小、对候选做逐块指纹比对，短文可返回 0%），叠加"移动后又被大幅改写"是常态，故阈值只需落在真移动的下界之下——真 delete+create 是两文件内容无关（相似度≈0），而移动+改写实测仍有 11%；取 10% 属保守取值，且本检查只对"暂存区同时有删除与新增"这一形态**提示人工复核**、不是判违规（单测 212 → 213）
- 3.4 | 2026-09-12 | 机械防线实现层修正（本轮独立复核指出）：`check_dispatcher_registry` 的维护方私有落点判定由 `startswith` 前缀匹配改为"目录前缀 + 精确文件名"（前缀匹配会把未来出现的 `PUBLIC2.adoc` 之类误放行）；`check_library_guard` 的"登记双向一致"明确**以入口登记表解析集合为准**、`LIBRARY_TOPICS` 只是下限补丁（不是第二真源）；`-M10%` 的实测数字由误记的 16% 统一为实测 11%
- 3.4 | 2026-09-12 | 复查又补两处**真实漏报**（防线自称的能力与实现不符，均实跑复现）：①`check_public_content_coverage` 声称"公开入口都在清单里"，实现却是 `rel in 全文`——**正文里提一句也算命中**，把 `INSTALL.adoc` 那行从「入口清单」表里删掉、只在正文保留一句，检查仍全绿；现改为按**表格区**判定（`| `rel` |` 这一列的写法），与"按节切区"的其余判据口径一致。②`check_library_guard` 的馆内引用正则只认**带目录前缀**的写法（`specs/...`、`script/...`）——馆内很自然写"登记与维护说明见 `AGENTS.adoc`"，这类**根级文件名**引用被整体跳过，改成不存在的名字也不会被发现；现补根级文件名白名单（`_LIB_ROOT_FILES`）纳入存在性核对，白名单外的根级名仍按普通词处理（不误报）
- 3.4 | 2026-09-12 | 上述两处各补正反例回归用例（把入口移出表格只留正文 → 报；馆内根级文件名引用命中/悬空 → 不报/报），单测 237 → 240
- 3.4 | 2026-09-12 | 复查第三轮：**三处"判据把两件事混成一件"的误报/漏报**（独立复核第 7 条清单对表通过后新压出，均实跑复现）：① **`_names_in_zone` 误报**——判据绑死"反引号 + 文件名字面"（`^\|[^\n]*`rel``），把「入口清单」表里那一格改成 AsciiDoc 惯用的 `link:` **文本**写法（表格结构、所在节、覆盖面均未变，纯排版）后，**明明列在表里却报"未列进表"**；现放宽为"表格行内任一点名形式"（反引号 / `link:` 文本 / 裸文件名，`^|` 行锚保留），"只在正文提及"仍被排除、原反例仍报。② **馆内根级文件名走 `link:` 形态漏报 + 双重基准**——`_LIB_ROOT_FILES` 只接上了**反引号**形态，馆内更自然的 `link:AGENTS_COMMON.adoc[]` 形态没接上：文件缺失时不报（漏报）；且 `check_library_guard` 内两段对 `link:` 的基准不同（反引号按**仓库根**、`link:` 按**本文件目录**），同一句引用 `CHANGELOG.adoc` 两种写法给出**相反结论**（文件在时 `link:` 形态误报解析成 `library/CHANGELOG.adoc`）。现抽出 `_lib_resolve` + `_check_library_target`：**全仓库只有一个基准**（仓库根优先、未命中再本文件目录），反引号/`link:`/`../` 三种写法共用；根绝对路径显式报错、`../` 越界仍报。③ **基准口径记录性风险（`_ref_base` 注释 vs 站点实际渲染语义）**——注释里"根文件按仓库根解析"与"其余文件按本文件目录解析"两套说法并存，而站点实际渲染者是 `index.html` 用 **Asciidoctor.js 浏览器端现渲染**、其 `link:` 按**当前文档目录**解析，而站点渲染的文档就在仓库根 → 现状"恰好成立"但成立理由与注释互相矛盾；将来谁按注释去"修正"基准，会把 `README.adoc` 里根级 `link:` 改成 `../` 形态、反而把站点链接弄死。现把 `_ref_base` 注释与 `AGENTS.adoc` 写明**与渲染者一致的单一说明**（文档渲染在仓库根 → 根级相对即仓库根相对），不留两套说法（单测 240 → 252）

- 3.2 | 2026-09-12 | **安装入口兼容 `AGENTS.md`**：入口文件名规则从「只认 `AGENTS.adoc`」改为「默认 `AGENTS.adoc`；根目录**已存在 `AGENTS.md`** 时就地融合到 `AGENTS.md`、**不重命名/不删除/不另建**」——各平台对 agent 规范文档的固有默认命名是 `AGENTS.md`，只认单一扩展名会让安装规则在已有 `AGENTS.md` 的项目上落不了地（重命名会打断平台识别、删旧建新会丢内容、双入口会分叉）；并明确入口一律按固定格式书写（不因扩展名不是 `.adoc` 就降级成一句"读某地址"）。INSTALL/README 同步，`check_specs.py` 增钉该命名规则四项判据（单测 195 → 197）
- 3.2 | 2026-09-12 | **子 Agent 排查口径优化：优先用与执行者同源的 Agent**：新增"需要子 Agent 时优先用与执行者同类（同入口、同调度器、加载同一份 `AGENTS_COMMON.adoc`）的 Agent；换成外部来源（外部 NPC/另一个产品）承担同一职责只在**同源不可用后**才成立"；并补"**同源也不可用时的降级路径**"（由主 agent 串行承担同一职责 + 如实标注独立性边界，或如实标悬置，不跳过、不用不可核对的外部执行者顶替）与"**优先一次性调用**"（一次性、边界明确、可超时；不交有自主探查权的执行者，其耗时与边界不可预估）。维护方落点同步进 `specs-project-maintainer/verify.adoc`（外部复核者不是第一手段、派发前先核实 slug/镜像可用），并由 `check_delegation_guard` 增钉该要点（单测 191 → 195）
- 3.2 | 2026-09-12 | **公共内容自足 + 维护方层更名**：`specs-project/` → `specs-project-maintainer/`（维护方自查层），并**从公共内容中移除全部指向该层的引用**——此前 `AGENTS_COMMON.adoc` 与多个 `specs/` 文件把"最高关注项、定级口径、验证三视角、运行契约、读取范围、执行前自检、来源真实性"等**同时在公共内容里写着、又只存在于私有层**，引用方按链接读不到该文件（死链），读到的规则"只成立一半"；现改为**公共内容自足**：同一文件里既有公共规则又有项目自身内容时，一律把公共部分留在公共侧自足表达、把项目自身部分收回维护方层
- 3.2 | 2026-09-12 | **按"对谁成立"重划归属（不只是换目录）**：分级定义（L1/L2/L3）、最高关注项的完整判据与"强调的正确做法"、判准维度（严格执行/尽力而为）、验证三视角/效力等级/适用边界/总纲/运行契约、读取范围与长会话治理、执行前自检清单与环境能力自评、来源真实性 → 全部归位公共内容（对任意项目成立，且引用方确实需要读到）；套用公共规则（谁必须做到、判准怎么取值、改完自己的规范怎么验），示例改为中性表述（不再点名本仓库私有落点）
- 3.2 | 2026-09-12 | 维护方层（`specs-project-maintainer/`）只保留**只对维护方成立**的：最高关注项不可降级清单（P1-P5 级别与依据行）、规范集合自身的维护（归属判定/准入九问/定级四问与判定表/自身重构/拆分判据/提案校验）、维护方的验证义务与落点、公共内容被加载时的可控性清单。该层**不由公共入口登记、不随公共内容分发**，由维护方自己的项目规范入口 `AGENTS.adoc` 登记
- 3.2 | 2026-09-12 | 自检与来源真实性随公共内容回归必加载链：`specs/general/self-check.adoc`、`specs/general/source.adoc` 在 `AGENTS_COMMON.adoc` 调度器登记（触发条件=执行任何非平凡任务 / 产出涉及引用与事实的内容），任何项目都能按入口读到
- 3.2 | 2026-09-12 | 机械防线扩展：新增**公共内容自足性防线**（公共内容出现指向 `specs-project-maintainer/` 的引用即拦下——引用方读到的是死链）（单测 183 → 187）
- 3.2 | 2026-09-12 | 校验脚本同步新分层：常量、`collect_adoc_files` 范围、各防线的钉点与单测路径全部更新；`check_verify_guard` / `check_adoption_guard` 的公共侧改钉 `specs/general/testing.adoc`「验证与运行契约」，维护方侧钉 `specs-project-maintainer/verify.adoc`、`context.adoc`
- 3.1 | 2026-09-12 | **`specs-project/` 落地补齐**：把"会被维护方之外的人看到"的措辞统一中性化——项目自身维护层不再自称"公共内容/通用层"，而是"由项目自身规范登记与维护的维护方层"；`check_specs.py` 的两条"公共内容"检查（私有引用、不得声明机械防线）明确只覆盖真正的公共内容（`AGENTS_COMMON.adoc` + `specs/`），维护方可用自家防线名
- 3.1 | 2026-09-12 | 常驻体积：必加载层体积上限的计量对象修正为**真正每次无条件加载的两个文件**（`AGENTS_COMMON.adoc` + `specs/core/execution.adoc`）；`priority.adoc` 归维护方层后不再计入
- 3.1 | 2026-09-12 | 项目入口 `AGENTS.adoc` 瘦身：把"维护方的验证义务"整节迁入 `specs-project/verify.adoc`（新增同名节），入口只留一行落点，回到体积上限内
- 3.0 | 2026-09-12 | **新增 `specs-project/` 项目自身维护层**：把只对"维护规范集合/共享资产集合这类项目"成立的规范（规范优先级与最高关注项、分类准入与定级口径、规范集合自身重构、读取范围与长会话治理、执行前自检、编译/运行验证、依据与来源真实性）从公共内容 `specs/` 迁出；由规范集合入口登记、同类维护项目加载，**普通引用方项目不加载**（原内容仍随入口一并分发，条目未删除，引用方或同类项目的自检仍以原口径执行）
- 2.1 | 2026-09-12 | 明确同一条规则的**两种读法**：执行场景只给"怎么走"（规则 + 判定标准 + 依据名），原因与取舍只在"要思考、要决策"（新增/调整/定级/重构、有取舍或争议、复盘）时给；两形态同源、不复制两份，**依据只压缩为标准名/编号、不得整段删除**
- 2.1 | 2026-09-12 | 规范自身重构的验收在"内容不丢"之外增加**有效性核对**：两形态分离（读者一致性）、可执行性不降级（不得把"必须 + 判定标准"压成"要注意"）、可见性不丢（最高关注项引用不得因去重删除、移层后仍可加载）
- 2.1 | 2026-09-12 | 常驻层只给"怎么走"：最高关注项的原因收敛为一行**依据**（标准名/编号），展开的失效原理与量化抓手归其专项规范（如上下文规范）；防常驻层再度膨胀，由 `check_priority_guard` 钉住形态声明与依据行
- 2.2 | 2026-09-12 | 公共内容不再声明机械防线的存在：防线属维护方、随规范分发即宣称与实际不符
- 2.2 | 2026-09-12 | 机械防线扩展：新增"公共内容不得声明机械防线"检查，拦住声明句与裸防线名（单测 179 → 185）
- 2.2 | 2026-09-12 | 统一「两问一查」旧称：与三视角内容不符，改称"三视角各一问"
- 2.2 | 2026-09-12 | 子 agent 复核须自带硬超时：到点视为失联，放弃并如实标悬置，不得无限等待（外部评审卡死 1h+ 的实证）
- 2.2 | 2026-09-12 | 复核职责不绑定特定执行者：外部复核不可用时由发起方组织等价复核并标注独立性边界
- 2.2 | 2026-09-12 | 留证改为可机械核对的三态台账：通过／未发现问题／悬置各占一栏，不得合并
- 2.2 | 2026-09-12 | 修口径冲突：P2 与执行原则的验证义务不再外溢到代码类改动（按改动性质取值、取更严一侧）
- 2.2 | 2026-09-12 | 修写法冲突：七节点表的复盘行改为"仅公共内容改动"，效力标法写进表头
- 2.2 | 2026-09-12 | 准入判定项数统一为九问（此前自称八问、实列九条）
- 2.2 | 2026-09-12 | 判准维度（严格执行/尽力而为）由必加载层归位通用层，必加载层只留一句话与落点
- 2.2 | 2026-09-12 | 机械防线扩展：新增**清单逐项与文档-脚本一致性防线**（准入项数、七节点、`check_*` 名字、硬超时与三态留证）（单测 170 → 177）
- 2.2 | 2026-09-12 | 新增「验证的效力等级」：确定项可判通过/不通过，概念项只到"未发现问题"、须标悬置，不得当作阻断交付的条件
- 2.2 | 2026-09-12 | 明确复核结论只作发现手段、不作判定依据：机械校验按判据本体验证，不按引用形式近似
- 2.2 | 2026-09-12 | 机械防线扩展：规范验证防线补效力等级与"不得阻断交付"（单测 168 → 170）
- 2.2 | 2026-09-12 | 新增「任务生命周期与节点自查」：任务从提出到收尾各节点各查什么，并写明哪些节点不设
- 2.2 | 2026-09-12 | 新增「验证的适用边界」：代码类改动按机械判据判过不过，只有公共内容改动才做三视角与全局核对
- 2.2 | 2026-09-12 | 明确验证须**每次换一个干净上下文**：复用发起改动或上一轮验证的上下文等于自己复核自己
- 2.2 | 2026-09-12 | 新增「一条规范何时该拆分」与「拆分后的自洽核对」：三条硬条件、默认不拆、拆后逐项核对
- 2.2 | 2026-09-12 | 准入判定补第九问「非拆分而膨胀」：拆出新的文件/节只为更整齐者不收
- 2.2 | 2026-09-12 | 复盘补「任务节点与拆分判据也须复盘」：索引指向的落点失效即等于没有这条要求
- 2.2 | 2026-09-12 | 机械防线扩展：新增**任务生命周期防线**（单测 159 → 168）
- 2.2 | 2026-09-12 | 修 `verify.adoc` 一处"同文件"悬空节名指向；节名引用检查补"同文件"式自然语言指向的机械抓手
- 2.2 | 2026-09-12 | 常驻体积上限补计项目入口 `AGENTS.adoc`（同为每次会话无条件加载的常驻物）
- 2.2 | 2026-09-12 | 新增「验证总纲」：验收从"跑一遍检查"改为**三视角**，并写明判定标准与标准出处
- 2.2 | 2026-09-12 | 新增「运行契约」：明确公共内容**会被未知项目加载**，新增须逐维核对影响面/成本/可控性并留证
- 2.2 | 2026-09-12 | 「规范验证」由两问扩为**两问一查**：新增③接纳面查与①②共同构成三视角，仍由同一个干净子 agent 一次读取、各留证
- 2.2 | 2026-09-12 | 验证对象扩到**会被外部执行的产物**：`prompts/` 与随规范分发的工具同样按三视角核对，提示词补"逐项给命令与统计结果"
- 2.2 | 2026-09-12 | 机械防线扩展：新增**接纳面防线**与公共内容私有引用检查，规范验证防线扩为三视角（单测 142 → 159）
- 2.2 | 2026-09-12 | 新增「规范验证」定式：补①完整性（改动前后等价）与②有效性与认知质量两问，合用一个干净子 agent、都过并留证才算完成
- 2.2 | 2026-09-12 | 明确**同一对象只起一个子 agent**：同一份 diff 的核对合并为一次派发，不重复起子 agent
- 2.2 | 2026-09-12 | 机械防线扩展：新增**规范验证防线**，并核对 P2 与 `AGENTS.adoc` 三处口径一致（单测 134 → 141）
- 2.2 | 2026-09-12 | 新增「从属者加载由已加载入口驱动」：子 agent 不得以"没被派发"为由跳过加载，"上级已读过"不免责
- 2.2 | 2026-09-12 | 新增「执行环境能力自评」：环境能力须先判定（不确定按不可用），缺失走降级路径并如实记录，不空自评
- 2.2 | 2026-09-12 | 新增判准维度「严格执行 / 尽力而为」：答"执行到什么程度算做到"，未执行不得记作通过
- 2.2 | 2026-09-12 | 补齐引用路径第三类基准与覆盖面判据：反引号式 `specs/...` 按加载入口解析、两态同源
- 2.2 | 2026-09-12 | 机械防线扩展：新增常驻层体积上限、从属者与能力自评防线、本仓库自身侧 git mv 自查（单测 120 → 132）
- 2.1 | 2026-09-12 | 明确同一条规则的两种读法：执行侧只给"怎么走"，原因与取舍归决策侧；依据只可压成标准名/编号，不得整段删除
- 2.1 | 2026-09-12 | 规范自身重构的验收在"内容不丢"之外增加**有效性核对**：两形态分离、可执行性不降级、可见性不丢（最高关注项引用不得因去重删除、移层后仍可加载）
- 2.1 | 2026-09-12 | 常驻层只给"怎么走"：最高关注项的原因收敛为一行依据，展开论证归其专项规范；形态与依据行由机械防线钉住
- 2.0 | 2026-09-12 | **规范集合重构瘦身**：常驻层（`AGENTS_COMMON.adoc` + `specs/core/`）只保留 L1 底线、最高关注项与其落点，常驻体积约由 56 KB 降至 45 KB；"怎么写规范"的元规范（分类准入、定级口径、规范自身重构）统一归位到通用层 `specs/general/spec-lifecycle.adoc`，不再每次会话加载
- 2.0 | 2026-09-12 | 新增「如何给一条规范定级」与「规范集合的自身重构」：定级四问、条款类型判定表、归属对象、归类举证、级别变更与复盘，以及"先判归属 → 再判层级 → 再判重复 → 最后压缩表述"的重构顺序与"删/移/留"速查（原散落在 `specs/core/priority.adoc`，现归位通用层）
- 2.0 | 2026-09-12 | 修正自相矛盾：`specs/core/` 不再出现与"底线"无关的 L2/L3 方法论条目；`priority.adoc` 内"必加载层出现 L2/L3 即级别混乱"与"P4 条款为 L2 且在必加载层"的冲突按"最高关注项条款本身可非 L1"的口径消除
- 2.0 | 2026-09-12 | 消除重复与失效引用：`git mv` 权威定义收敛到 git 规范、其余位置改为一句话引用；执行原则中重复的 P1 落点小节与自检小节合并；清理公共规范里指向本仓库脚本的引用（引用方看不到，改用引用方自查命令或通用表述）
- 1.4 | 2026-09-12 | 换行符（行尾）改为按解释器分流：仓库内容以 LF 为基准，Windows 批处理 `.bat`/`.cmd` 必须 CRLF（LF-only 在 Windows 上直接执行失败）、PowerShell `.ps1` 用 CRLF；行尾由仓库 `.gitattributes` 固定、检出时自动转换，不再靠人工逐个文件手动调整
- 1.4 | 2026-09-12 | `.bat` 启动脚本的编码要求明确为 ANSI/GBK（`cmd.exe` 按系统 ANSI 代码页解析，存为 UTF-8 会解析失败），`.ps1` 仍为 UTF-8（必要时加 BOM）
- 1.4 | 2026-09-12 | 新增换行符防线：编码规范的 LF 基准、`.bat`/`.cmd` 必须 CRLF、`.gitattributes` 检出归一等判据，以及 bash/python/powershell 栈文件的行尾要求不得被删或弱化
- 1.3 | 2026-09-12 | 明确 Java 测试类命名契约为四类后缀：`Tests` 常规、`BootTests` 启动型、`PerfTests` 性能、`IT` 端到端（与 Maven Failsafe 默认 includes 对齐），并规定类别与执行阶段的绑定（`PerfTests`/`IT` 不随常规测试执行、须独立执行）
- 1.3 | 2026-09-12 | 新增 Java 测试类命名防线：规范与调度器两侧须一致写明四类后缀判据，避免引用方照任一处学习漏掉某类测试
- 1.3 | 2026-09-12 | 新增执行前自检与知识边界规范 `specs/general/self-check.adoc`：动手前逐项自检（指令、规范是否已加载、规划是否落盘、读取是否最小必要、证据是否已核实、如何收尾），过程与完成前自检，禁止顺口编造
- 1.3 | 2026-09-12 | 新增依据与来源真实性规范 `specs/general/source.adoc`：内部引用须指向真实存在的当前文件与节名、外部标准只写名称编号且不得编造、数据须有来源、无法确证时宁可不引
- 1.3 | 2026-09-12 | 新增编码与语言无关规范 `specs/general/encoding.adoc`：全产物 UTF-8、仓库 LF、注释与文档只描述使用方须知的约束不绑定实现语言
- 1.3 | 2026-09-12 | 新增代码安全规范 `specs/general/security.adoc`（跨语言）：输入校验与输出编码、鉴权与会话、密钥与敏感信息、失败关闭与安全默认值
- 1.3 | 2026-09-12 | 新增依赖管理规范 `specs/general/dependency.adoc`（跨语言）：来源可信与版本固定、版本冲突统一、锁文件与可重现、升级须看变更说明并验证
- 1.3 | 2026-09-12 | 最高优先级铁律与执行原则补齐两条 L1：不可逆破坏性操作先确认（删除/清空/强推）、不得编造事实与来源；并新增 `specs/core/execution.adoc`「破坏性操作」「规范执行与自检」
- 1.3 | 2026-09-12 | 最高关注项由 P1-P4 增至 P1-P5（新增 P5 不可逆操作先确认、不得编造事实与来源），不可降级保护与 `check_priority_guard` 防线同步扩展
- 1.3 | 2026-09-12 | 机械防线扩展：新增自检防线与来源防线（`check_self_check_guard`、`check_source_guard`）及配套测试（单测 99 个）
- 1.3 | 2026-09-12 | 补充「如何给一条规范定级」：新增定级四问（违反后果、豁免性、可验证性、代价与误伤）、条款类型与 L1/L2/L3 的判定表、级别与强制对象的正交判定、定级结论的归类举证、级别变更与定期复盘；`check_priority_guard` 钉住定级口径五节与 P1-P5 各自级别
- 1.2 | 2026-09-11 | 新增上下文与读取范围规范 `specs/general/context.adoc`：读取按最小必要信息集（定位优先、渐进披露、重型读取委派子 agent）与长会话治理（任务切换清洁上下文、简单任务优先另起会话），并列为最高关注项 P4
- 1.2 | 2026-09-11 | 最高关注项由 P1-P3 增至 P1-P4（新增 P4 读取范围与长会话上下文），不可降级保护与机械防线同步扩展
- 1.1 | 2026-09-12 | 新增规范分类与准入规范（`specs/general/spec-lifecycle.adoc`）：明确一条规则属公共规范还是项目规范（对谁成立）、属哪一层（层级/语言栈/项目类型三方正交），给出七问准入判定与"新增规范提案校验"（合理性、是否已有标准、是否已有条目、升级与举一反三），要求可机械校验的条目同步补抓手
- 1.1 | 2026-09-11 | 新增变更日志编写规范：明确条目"记影响、不记过程"，只记对外行为/契约、破坏性变更、缺陷修复、安全与数据、性能、依赖兼容、运维部署，不记内部实现、过程与工作量
- 1.1 | 2026-09-11 | 新增规范优先级与最高关注项：条目分 L1 强制 / L2 建议 / L3 允许，`git mv`、完整性校验、内容不减少为不可降级项
- 1.1 | 2026-09-11 | 规范新增/调整后存量文件随项目开发逐一迁移，不再一次性变更所有存量；主动声明一次性收敛时除外
- 1.1 | 2026-09-11 | 规范条目依据优先引用业界标准（ISO/IEC/IEEE、RFC 等），出处只写标准名/编号、不附链接
- 1.1 | 2026-09-11 | 明确 Java 测试类命名定式，并规定规范变更后存量文件的处理方式
- 1.1 | 2026-09-11 | 公共任务提示词显式标注主侧重与 L1/L2/L3 优先级，并要求 review 与 refactor 两个方向相互隔离、不得混用
- 1.1 | 2026-09-11 | 建立统一变更日志（本文件）：全项目变更集中一处记录、不再分散在各文档（本次初始化）
- 1.0 | 2026-08-23 | 规范集合首次对外发布：提供通用规范入口 `AGENTS_COMMON.adoc` 与 `specs/`，供任意语言、任意项目引用
