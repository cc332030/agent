
# 『引用坐标』判据本体（`specs/general/source.adoc`「内部引用」的「引用坐标不得靠位置」一条）。
# 判据见 `specs-project-maintainer/priority.adoc`「机械防线的核对对象是判据本体，不是轴名」：
# 只核"这一节还在不在"属防线空转——把位置式禁令抽掉、把"不得只写文件名"抹掉后仍会全绿。
[[guards.check_reference_coord_guard]]
file = "specs/general/source.adoc"
groups = [
    [
        "坐标二形态与位置式禁令",
        [
            "引用坐标不得靠位置（L1",
            "引用式坐标",
            "位置式坐标一律不得使用",
        ],
        "缺则「引用坐标」这一条没有可核对的本体：位置式（条号/序号）无人拦、引用式坐标的目标写法也无落点",
    ],
    [
        "引用式须写目标全名",
        [
            "不得只写文件名",
            "写全名才能定位到那一份",
        ],
        "缺则引用式坐标可以只写文件名——同级/跨层解析落到哪一份不进判据，正是 Issue #208 实测的悬空形态",
    ],
    [
        "位置式不得靠定期核对补救",
        [
            "随增删必然漂移",
            "不得靠",
        ],
        "缺则把「按位置引用」当成可维护形态（用定期核对补救），执行者会继续写条号/序号",
    ],
    [
        "判定标准（可逐条核对）",
        [
            "判定标准（任一命中即违规）",
            "顺序量",
        ],
        "缺判定标准则本条自身不可判定（「什么算位置式坐标」交回执行者凭感觉）",
    ],
    [
        "边界（编号即内容本身的载体不属本条）",
        [
            "编号即内容本身的载体",
            "不属本条",
        ],
        "缺边界则清单表行号、隐患编号一类「编号就是内容」的写法被误判成违规（本轮实测的假红形态）",
    ],
]
kind = "file_groups"

# 『skill 引用坐标』判据本体——落点是**实测依据**（安装侧真正落到落点的只有 SKILL.md），
# 故本组核的是"这句话还在不在"：它被抽掉时，执行者会按"引用方能看到 references/ 目录"
# 写引用（与安装侧实测相反），而那道防线的形态判据仍在、继续报绿。
[[guards.check_skill_ref_coord_guard]]
file = "script/specs-rules/_tokens.toml"
groups = [
    [
        "兄弟文件引用前缀清单",
        [
            "SKILL_SIBLING_REF_PREFIXES",
            # 锚点须落在**清单本体**上：只核 `SKILL_SIBLING_REF_PREFIXES = [` 时，
            # 清空成 `= []` 仍含该子串、照样过（本轮实测的空转形态）。
            '''"references/",
    "scripts/",''',
        ],
        "缺则这道防线没有可核的引用面（哪些前缀算「指向兄弟文件」退回执行者凭感觉）",
    ],
]
kind = "file_groups"

# 『引用坐标』的**判据图形**（位置式表述的正则）也在规则数据里：图形写在防线函数体里时，
# 把"条号"那一段整段删掉仍会全绿（本轮实测的空转形态），故一并核它在不在。
[[guards.check_reference_coord_guard]]
file = "script/specs-rules/_tokens.toml"
groups = [
    [
        "位置式坐标的判据图形",
        [
            "REF_COORD_POSITIONAL_RE",
            "REF_COORD_POSITIONAL_OK_RE",
            "RELATIVE_POSITION_OK_RE",
            # 图形本体里**只出现一次**的片段：只核"变量名还在"时，把正则改成空串
            # （`REF_COORD_POSITIONAL_RE = `）仍能过——那正是"判据图形被抽走"的形态。
            "(?:最后|末尾|倒数第",
            "序号(?:为|是)?",
        ],
        "缺则这道防线没有可核的图形：位置式（条号/序号）与「第 N 步」的区分退回函数体硬编码，改判据就要改脚本",
    ],
]
kind = "file_groups"
