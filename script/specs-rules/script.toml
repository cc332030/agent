[[guards.check_cross_platform_script_guard]]
file = "specs/general/script.adoc"
groups = [
    [
        "入口层不得承载逻辑",
        [
            "入口层不得承载逻辑",
            "判定标准",
        ],
        "须写明**入口层不得承载逻辑**并给可判定判据（整份入口可逐行解释为'定位逻辑代码 → 转交全部参数 → 返回退出码'三件事）——缺这句，入口又会变成'顺便处理一下参数'，两平台各写一遍是本条要防的核心失效",
    ],
    [
        "入口层不为逻辑层添第二套选项语义",
        [
            "入口层不为逻辑层添第二套选项语义",
            "例外",
            "转义",
        ],
        "须写明入口**不为逻辑层添第二套选项语义**（只转交、不解释参数、不设默认值、不吞参数），并写明 `cmd.exe`/PowerShell 需转义时的例外——缺则'不得自行解析参数'会被读成'连必要的转义都禁止'（那是做不到的：`cmd.exe` 承载不了某些字符），或反过来留下'入口可以自己定默认值'的口子",
    ],
    [
        "入口层须让退出码可判定",
        [
            "入口层须让退出码可判定",
            "exit /b",
            "LASTEXITCODE",
        ],
        "须写明入口**让退出码可判定**并给三平台的可核对写法——`cmd.exe` 不写 `exit /b %errorlevel%` 时退出码是最后一条命令的（不等于逻辑代码的），这是'看起来在转交、实际丢失成败'的最常见形态",
    ],
    [
        "不得**在 `.sh` 里再写一遍",
        [
            "不得**在 `.sh` 里再写一遍",
            "只允许**一份薄壳",
            "该平台版本",
        ],
        "须写明**不得在另一平台的入口里重写逻辑**——'Windows 的我可以不写、顺手写个 sh 版本更快'正是本条的失效形态",
    ],
    [
        "跨平台逻辑层",
        [
            "跨平台逻辑层",
            "一份",
        ],
        "须写明逻辑**只写一份**放在跨平台逻辑层（一份跨平台逻辑代码）里——缺则'分两层'退化成'两个入口各写一份'。**此处刻意不用「跨平台逻辑脚本」这个说法**：它把「脚本」一词同时给了入口与逻辑两层，读者会据此把跨平台的那一份也当成「脚本」、进而按脚本的可执行要求去要求它（那正是让用户敲 `python3 foo.py` 的由来）——故两层分别称「逻辑代码」与「入口脚本」，名字本身即区分二者",
    ],
    [
        "CRLF",
        [
            "CRLF",
            "LF",
            "纯 ASCII",
        ],
        "入口文件须**按各自平台规范落盘**（`.bat`/`.cmd` 与 `.ps1` 用 CRLF、`.sh` 用 LF、`.bat` 纯 ASCII）——入口是平台专属文件，行尾/编码错则入口本身就跑不起来",
    ],
    [
        "实现语言取舍",
        [
            "实现语言取舍",
            "Python",
            "Node.js",
            "Go",
            "Rust",
        ],
        "须给出**实现语言取舍**（环境好搭建/不易出错/好维护/兼容性/效率的默认取向：Python 3 与 Node.js 优先、单文件分发用 Go/Rust）——缺则执行者只能凭'哪个顺手'选语言",
    ],
    [
        "同处一目录",
        [
            "同处一目录",
            "同名",
            "主名",
        ],
        "须写明入口与逻辑代码**同处一目录、主名相同**（逻辑 `foo.py` → 入口 `foo.sh`/`foo.bat`，不另建 `bin/`/`script/`/`windows/`）——缺则入口一分散，改逻辑时连带改入口、又改出第二份；也是用户明确要求的落点约定",
    ],
    [
        "不加前后命令",
        [
            "不加前后命令",
            "必要参数",
            "直接执行",
        ],
        "须写明入口**须由解释器原生可执行、调用方不加前后命令**（只给脚本名即可跑；`bash foo.sh` / `python3 foo.py` / `powershell -File foo.ps1` 属违规），并写明'脚本自身功能参数属必要参数'的例外——缺则用户'不希望脚本执行前后加命令和参数'的要求无法判定（要么入口跑不起来、要么被读成连功能参数也不许加）",
    ],
    [
        "入口语言取舍",
        [
            "入口语言取舍",
            ".bat`/`.cmd`",
            "`.sh`",
        ],
        "须写明**入口语言取舍**（入口语言按平台默认具备者选：Windows 取 `.bat`/`.cmd`、Linux/macOS 取 `.sh`；不得为一侧入口引入对方平台要另装的运行时；编译型单文件分发时同一二进制兼作两平台入口）——缺则执行者会按'逻辑层用什么语言'给入口选语言（逻辑用 Python 就写 `.py` 当入口），把安装步骤推给调用方",
    ],
    [
        "不得要求调用方先做前置动作",
        [
            "不得要求调用方先做前置动作",
            "请先",
        ],
        "须写明**入口不设前置步骤**（不要求调用方先 `cd`/设环境变量/`chmod +x`/装依赖）——缺则入口把配置责任推给用户，'拿来就能跑'落空",
    ],
    [
        "不得为“跑逻辑”自加命令",
        [
            "不得为“跑逻辑”自加命令",
            "一一对应",
        ],
        "须写明入口**不得为跑逻辑自加命令、也不得给逻辑代码塞参数**（调用前的 `cd`/`mkdir`/安装/下载/校验，或转交时多加了固定参数）——缺则'只转交'被绕过，入口又开始做别的事",
    ],
    [
        "薄壳之外不得多做",
        [
            "薄壳之外不得多做",
            "与逻辑层调用等价",
            "不等价",
        ],
        "须写明**薄壳之外不得多做、但可以做「把逻辑层当命令直接跑」的薄壳**（判定标准：入口里出现与「把调用方给的参数交给逻辑代码」**不等价**的动作即违规；「先探测解释器、一个都没有就报错退出非 0」仍属薄壳）——缺则「入口不承载逻辑」被读成「入口只许有一条形态」，把「另留一个只跑逻辑的入口脚本」这种正当形态也一起禁掉",
    ],
    [
        "安装/下载/解压任何运行时",
        [
            "安装/下载/解压任何运行时",
            "不属",
            "代为获取运行时",
        ],
        "须写明**入口不得为「让逻辑跑起来」而安装/下载/解压运行时、包或依赖**（`apt`/`apk`/`yum`/`pip`/`winget`、`curl | sh` 一类都不属薄壳），且**代为获取运行时不是入口的职责**——要么由人按本平台既有软件分发方式完成、要么在**人已交互登录、能看见命令与报错的正常会话**里由 agent 完成，不得在入口或非交互执行里静默发生——缺则「让逻辑跑起来」被读成「入口自己想办法搞一个运行时」（把环境差异与不可控的系统改动带进入口）",
    ],
    [
        "不拿裸 shell 当逻辑层",
        [
            "不拿裸 shell 当逻辑层",
        ],
        "须写明**不拿裸 shell 当逻辑层**（bash 在 Windows 上不可得或来自 Git Bash/WSL、cmd 在 Linux 上不可用）——缺则逻辑又被写进 shell，平台差异回到逻辑层",
    ],
    [
        "不得假设自身所处目录",
        [
            "不得假设自身所处目录",
            "cd",
        ],
        "须写明逻辑代码**不得假设自身所处目录**（入口以绝对路径调用、不 `cd`）——缺则入口一 `cd`，逻辑代码里的相对路径在本地与 CI 下行为不同",
    ],
    [
        "（L1）",
        [
            "（L1）",
        ],
        "条文须标注级别——本条的'薄壳/不写两遍'是不可豁免的底线，标成建议即等于没有",
    ],
]
kind = "regex_section"
label = "跨环境脚本（入口 + 跨平台逻辑 + 实现语言取舍）"
message = "跨环境脚本防线被破坏：{file}「{CROSS_PLATFORM_SCRIPT_SECTION}」缺失要点 {missing}——{desc}；本条是用户明确要求的跨平台约定，不得删除、不得降级为建议"
pattern = "^== 跨环境脚本（入口 \\+ 跨平台逻辑 \\+ 实现语言取舍）(?:\\s|$).*?(?=^== |\\Z)"

[[guards.check_cross_platform_script_guard]]
file = "specs/stack/batch.adoc"
groups = [
    [
        "行尾",
        [
            "行尾",
            "CRLF",
            "纯 ASCII",
            "BOM",
        ],
        "批处理栈须写明编码与行尾（`cmd.exe` 按字节读行尾、按系统 ANSI 代码页解析，故 CRLF + 纯 ASCII + 不写 BOM）——缺则批处理按通用 LF/UTF-8 落盘、在 Windows 上直接执行失败",
    ],
    [
        "薄壳",
        [
            "薄壳",
            "exit /b",
            "errorlevel",
        ],
        "批处理栈须写明作为跨环境入口时的薄壳三件事与 `exit /b %errorlevel%`——缺则批处理入口又承载参数解析/路径拼装，或退出码恒为最后一条命令的（调用方/CI 看不到真实成败）",
    ],
    [
        "延迟展开",
        [
            "延迟展开",
            "enabledelayedexpansion",
        ],
        "批处理栈须写明块语句里 `%var%` 在**解析期**展开、须用延迟展开（`!var!`）——缺则循环里累加/拼接取不到值，属批处理最常见的静默失效",
    ],
    [
        "引号",
        [
            "引号",
            "%~1",
        ],
        "批处理栈须写明路径须加引号（Windows 路径常含空格）、可选参数用 `%~1` 去引号后判空——缺则含空格的路径直接报错、参数判空判错",
    ],
    [
        "@echo off",
        [
            "@echo off",
        ],
        "批处理栈须写明首行 `@echo off`——缺则每条命令都被回显、调用方与 CI 无法从输出判断实际执行了什么",
    ],
    [
        "未定义",
        [
            "未定义",
        ],
        "批处理栈须写明未定义变量不得静默容忍（`%var%` 未定义时被替换成空串、命令变残句）——缺则错误被推到更难定位的地方",
    ],
    [
        "$",
        [
            "$",
            "cmdlet",
            "-eq",
        ],
        "批处理栈须写明不得把 PowerShell 语法写进 `.bat`（`$LASTEXITCODE`/cmdlet/`-eq` 在 `cmd.exe` 里不存在，混写出来一条都跑不通）——缺则批处理里继续混写 PowerShell 语法",
    ],
]
kind = "file_groups"
message = "批处理栈缺失要点 {missing}——{desc}"

[[guards.check_cross_platform_script_guard]]
file = "AGENTS_COMMON.adoc"
groups = [
    [
        "跨环境脚本",
        [
            "跨环境脚本",
        ],
        "加载调度器的脚本加载项须有本条的识别特征（要把逻辑在两个平台各写一份时即命中）——缺则该条永远不会被触发加载，规则实际失效。**不核**『`.bat`/`.cmd` 与 `.sh` 成对』——那是 `script.adoc` 的条目本体取值，抄进调度器即第二真源",
    ],
    [
        "specs/stack/batch.adoc",
        [
            "specs/stack/batch.adoc",
        ],
        "加载调度器的技术栈层须登记批处理栈——缺则'写一个批处理脚本'没有触发特征、`.bat` 的专属规则实际失效",
    ],
]
kind = "file_groups"
message = "跨环境脚本防线被破坏：AGENTS_COMMON.adoc 缺失 {missing}——{desc}"

[[guards.check_rule_script_separation_guard]]
file = "specs/general/script.adoc"
groups = [
    [
        "规则数据外置、脚本不承载措辞（L1）",
        [
            "规则数据外置、脚本不承载措辞（L1）",
            "独立配置文件",
            "新增/调整一条规则时需要改",
            "同一段",
            "字面量出现在脚本源码里",
        ],
        "条文：规则数据须放独立配置文件、脚本只留引擎与接线；并给可核对判定标准（改规则要动 .py / 同一段实现被复制多份 / 关键措辞以字面量出现在脚本源码里）",
    ],
    [
        "规则文件是纯数据（L1）",
        [
            "规则文件是纯数据（L1）",
            "纯数据格式",
            "不含可执行代码",
            "不得",
        ],
        "边界：配置文件须是纯数据（不含可执行代码、不 import），并写明**不得**写成同模块的 `.py` 常量——缺则『隔离』只剩形式（数据与代码仍在同一模块）",
    ],
    [
        "格式取 TOML（L2，本集合的选用）",
        [
            "格式取 TOML（L2，本集合的选用）",
            ".toml",
            "重复表头由语法直接拒绝",
            "解析器随标准库",
            "不得",
        ],
        "本集合的格式选用：规则文件取 `.toml`——重复表头由语法直接拒绝、长锚点串可读性更好、解析器随标准库（`tomllib`，不为读配置装第三方依赖）；并写明不得为同一批规则同时维护两种格式的真源（改一处漏一处）",
    ],
    [
        "配置缺失即报错、不得静默空转（L1）",
        [
            "配置缺失即报错、不得静默空转（L1）",
            "报错退出非 0",
            "不得退回硬编码",
        ],
        "兜底：配置缺失/非法/引用未知规则项时须报错非 0、不得退回硬编码或跳过——缺则『核不到』与『核过且通过』分不清",
    ],
    [
        "报错文案随规则数据走（L2）",
        [
            "报错文案随规则数据走（L2）",
            "逐字保留",
        ],
        "说明随数据：缺失说明属规则数据的一部分、须外置并逐字保留——缺则防线虽在、读者不知道自己破坏了什么",
    ],
    [
        "生成的接线可核对（L2）",
        [
            "生成的接线可核对（L2）",
            "一一对应",
        ],
        "接线可核对：防线名 ↔ 配置键须一一对应、对不上须报错——缺则『规则写了却没人执行』",
    ],
    [
        "同一道防线只执行一遍（L1）",
        [
            "同一道防线只执行一遍（L1）",
            "只跑一遍",
            "重复报出",
        ],
        "同一道防线的规则在多处落点被共用时，须在同一次校验里只跑一遍——缺则同一处缺失被重复报出 N 遍（实测 12 条报错里只有 4 条是不同问题），真正的问题被埋进噪音里",
    ],
]
kind = "section_groups"
message = "规则与脚本的隔离防线被破坏：{file}「规则与脚本的隔离（规则数据外置）」缺失要点 {missing}——{desc}；该条对应用户明确要求（避免因规则变更导致不停修改脚本），不得删除、不得降级为建议"
section = "规则与脚本的隔离（规则数据外置）"

[[guards.check_rule_script_separation_guard]]
file = "specs/general/script.adoc"
groups = [
    [
        "一类规则一个文件（L1）",
        [
            "一类规则一个文件（L1）",
            "按被测落点切分",
            "不得把全部文件的规则堆进同一个文件",
            "冲突面同源",
        ],
        "规则数据须按被测落点切分（一个文件一类）；堆在一起时定位变慢、多个改动方冲突面同源（兄弟规则极易互相覆盖、合并时错并）",
    ],
    [
        "按落点自动扫描加载（L1）",
        [
            "按落点自动扫描加载（L1）",
            "登记**规则目录**",
            "逐个列出规则文件名",
            "加一个规则文件又要动脚本",
        ],
        "加载侧须登记规则目录、按目录自动扫描；逐个列出文件名时『加一个规则文件又要动脚本』——那正是隔离要治的形态",
    ],
    [
        "扫描与合并须确定、冲突须显式（L1）",
        [
            "扫描与合并须确定、冲突须显式（L1）",
            "按**文件名排序**",
            "同名键",
            "一律报错",
        ],
        "目录须按文件名排序加载（次序稳定），多文件合并时同名键一律报错、不得静默覆盖（分不清在跑的是哪一份就等于规则失效）",
    ],
]
kind = "section_groups"
message = "规则与脚本的隔离防线被破坏：{file}「规则与脚本的隔离（规则数据外置）」缺失要点 {missing}——{desc}；本条对应用户明确要求（一类规则一个文件、自动扫描加载、不把所有文件的规则堆在一起），不得删除、不得降级为建议"
section = "规则与脚本的隔离（规则数据外置）"

[[guards.check_script_header_guard]]
file = "specs/general/script.adoc"
groups = [
    [
        "文档头先行",
        [
            "文档头先行",
            "头部注释必须先行",
            "待办占位",
        ],
        "须写明**头部注释先行**（动手第一步就写、不是收尾补；不得只留 `TODO`/待补占位；改动后同提交同步）——缺这句，本条退回成'要写文档'，交付时文档头与实现早已不同步",
    ],
    [
        "脚本必须写文档头",
        [
            "脚本必须写文档头",
        ],
        "须写明**脚本必须写文档头**（承担实际功能的脚本都要有条目化文档头；只有一次性几行的临时命令可省并仍须一行用途）——缺则'没有文档头的脚本'仍是默许形态",
    ],
    [
        "**关键约定与设计决策**（**不得省略**）",
        [
            "**关键约定与设计决策**（**不得省略**）",
            "放弃了哪些备选",
        ],
        "条目清单须含**关键约定与设计决策**（约定与出处、为什么这么做与放弃了哪些备选做法、兜底与已知限制）并写明**不得省略**——只要求'定位、用法、环境变量、副作用'时，本条要防的失效（为什么这么取边界、放弃了什么，事后无从查）原样存在",
    ],
    [
        "设计决策写成可核对的记录",
        [
            "设计决策写成可核对的记录",
            "决策 + 理由 + 边界",
        ],
        "须写明设计决策写成**决策 + 理由 + 边界**、并给判定标准（只见'怎么做'没有'为什么'、记了备选方案却不说不选的理由、只写决定不写依据）——缺则'记录设计思路'会被写成一段叙述，读者仍无法据此判断'这条约定今天还成不成立'",
    ],
    [
        "不写硬编码数值",
        [
            "不写硬编码数值",
            "抽象描述或常量名",
            "两处真源",
        ],
        "须写明**取值写抽象描述或常量名、不写硬编码数值**并给判定标准（注释出现与常量重复的字面数值／抄死的目录名与默认值／同一取值两处各写一遍）——缺则'改代码不改注释'的文档说谎重新成为默许形态（用户报告：脚本很多细节随维护丢失）",
    ],
    [
        "不写进方法体",
        [
            "不写进方法体",
            "文件级文档注释",
        ],
        "须写明**决策留头部/文件级文档注释、不落进方法体**（方法体只记内部局部决策与边界）——缺则方案取舍被抄进每个方法，改一处必漏另一处",
    ],
    [
        "内容移交独立文档（L1）",
        [
            "内容移交独立文档（L1）",
            "该移交而未移交",
        ],
        "须写明**超出头部块注释容量的内容移交独立文档**并一行链接回指、给判定标准——缺则要么文档头堆成长文（正文规范又变成没人读的墙），要么内容被'写短点'删掉",
    ],
    [
        "行数不设限",
        [
            "行数不设限",
        ],
        "须写明头部注释**篇幅不设上限**（内容不因'写短点'删减、也不因超长判不合格）——缺则'简洁'会被读成篇幅配额，把内容要求折成字数",
    ],
    [
        "入口的注释边界",
        [
            "入口的注释边界",
            "不得复述",
        ],
        "须写明**入口注释只写入口自己、不复述逻辑层契约**（参数语义、默认值、行为与副作用）——缺则同一份契约写两处（改逻辑不改入口注释，按入口注释用会得到错误结论）",
    ],
]
kind = "regex_section"
label = "脚本头部注释（文档头）"
message = "脚本头部注释防线被破坏：{file}「{SCRIPT_HEADER_SECTION}」缺失要点 {missing}——{desc}；本条是用户明确要求的'文档先行也适用于脚本，脚本很多细节可能随着维护丢失'，不得删除、不得降级为建议"
pattern = "^== 脚本头部注释（文档头）(?:\\s|$).*?(?=^== |\\Z)"

[[guards.check_script_selfdoc_guard]]
file = "specs/general/script.adoc"
groups = [
    [
        "单打独斗",
        [
            "单打独斗",
            "相互独立",
        ],
        "须写明脚本**默认单打独斗**、拆成多个脚本时**各脚本相互独立**（不是同一模块）——缺则多脚本会被当成'一个多模块项目'、按模块给每个脚本各建一篇文档",
    ],
    [
        "多行文档注释",
        [
            "多行文档注释",
            "多行普通/块注释",
            "普通注释",
        ],
        "须写明承载方式的**三级优先级**（多行文档注释 → 多行普通/块注释 → 普通注释）——缺则'写在脚本里'没有可判定的形态，文档头会被降格成零散单行注释",
    ],
    [
        "不另建独立文档",
        [
            "不另建独立文档",
            "一律用**文档注释**承载",
        ],
        "须写明文档**默认写在脚本里、不另建独立文档**，并写明有文档注释机制的语言一律用文档注释承载——缺则'为脚本另建 .adoc'重新成为默许形态",
    ],
    [
        "超出头部块注释的容量",
        [
            "超出头部块注释的容量",
            "命中即违规",
        ],
        "须写明**唯一例外**是内容确已超出头部块注释容量时的移交，并给判定标准——缺则'能拆成两处写'会被当成另建文档的理由（判据是内容量、不是能不能拆）",
    ],
    [
        "大规模团队式协作",
        [
            "大规模团队式协作",
            "例外，不是默认",
        ],
        "须写明**大规模团队式协作是例外、不是默认**——缺则该例外会外推成所有脚本的默认文档组织",
    ],
]
kind = "regex_section"
label = "脚本文档的承载位置与协作粒度（默认写进脚本自身）"
message = "脚本自述文档防线被破坏：{file}「{SCRIPT_SELFDOC_SECTION}」缺失要点 {missing}——{desc}；本条依据用户口径（脚本单打独斗、文档直接写在脚本里、多行文档注释/多行普通注释/普通注释三级优先级），不得删除或降级"
pattern = "^== 脚本文档的承载位置与协作粒度（默认写进脚本自身）(?:\\s|$).*?(?=^== |\\Z)"
