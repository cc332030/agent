[[guards.check_scope_boundary_guard]]
file = "specs/general/scope.adoc"
groups = [
    [
        "== 工作空间边界（不依赖任何平台）",
        [
            "== 工作空间边界（不依赖任何平台）",
            "只允许修改本次任务的工作空间",
            "入口工作空间",
        ],
        "工作空间边界节：须有该节，且禁令按『只允许修改本次任务的工作空间（入口工作空间）』表达——离开平台（本地/服务器/容器）时『文件都在』，这条才是拦得住的那一条",
    ],
    [
        "== 平台上的仓库边界（代码托管平台）",
        [
            "== 平台上的仓库边界（代码托管平台）",
            "入口项目",
            "只改当前项目",
        ],
        "平台上的仓库边界节：须有该节，且写明平台把可写范围放大成『有权访问的全部仓库』、禁令按当前项目（入口项目）表达——否则平台场景下无人拦",
    ],
    [
        "未声明即拒绝",
        [
            "未声明即拒绝",
            "引用 ≠ 授权",
            "不构成改动",
        ],
        "未声明即拒绝 + 引用不等于授权：用户引用了工作空间以外的文件/其他项目不构成授权，未声明一律拒绝（这是本条的价值所在，写成『尽量』或省掉即失效）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "自我豁免",
            "顶替",
        ],
        "判定标准：须给出可逐条核对的越界形态（改动对象不在入口工作空间内 / 以『引用过它』为由自我豁免 / 转交他人顶替），否则只剩一句口号、无法判定",
    ],
    [
        "已知例外",
        [
            "已知例外",
            "用户显式声明",
            "只读",
        ],
        "已知例外：唯一的解禁情形是用户**显式声明**把该处纳入本次范围，且用户声明『只读/禁止改动』优先——不写例外则要么自相矛盾（连用户明说能改的地方也不敢改）、要么给『看着办』留口子",
    ],
    [
        "拒绝的形态",
        [
            "拒绝的形态",
            "拒绝不等于任务失败",
        ],
        "拒绝的形态：须如实说明依据与需补的声明、其余可做的部分照常完成，不得默默照做、也不得只回一句『不能做』就停摆",
    ],
    [
        "只读与对照照常做",
        [
            "只读与对照照常做",
            "不在禁止之列",
        ],
        "防『拒绝即停摆』的另一面：读取/对照被引用的外部文件不属禁止之列（禁的是改动），否则规则会被执行成『引用的东西都不能用』",
    ],
]
kind = "file_groups"
message = "改动范围边界防线被破坏：{file} 缺失要点 {missing}——{desc}；本条对应用户明确要求收窄的执行边界（『引用被误当成授权』），不得删除、不得降级为建议"

[[guards.check_scope_boundary_guard]]
file = "specs/platform/cnb.adoc"
groups = [
    [
        "变更范围只限当前项目",
        [
            "变更范围只限当前项目",
            "只允许修改当前项目",
            "入口项目",
        ],
        "主体禁令：只允许修改当前项目（本次任务的入口项目），越出该项目的改动不得执行（须有节名与禁令本身）",
    ],
    [
        "未声明即拒绝",
        [
            "未声明即拒绝",
            "引用 ≠ 授权",
            "不构成授权",
        ],
        "未声明即拒绝 + 引用不等于授权（平台侧同口径）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "自我豁免",
            "顶替",
        ],
        "判定标准（平台侧同口径，须可逐条核对）",
    ],
    [
        "例外",
        [
            "例外",
            "用户显式声明",
            "只读",
        ],
        "已知例外（平台侧同口径）",
    ],
    [
        "../general/scope.adoc",
        [
            "../general/scope.adoc",
        ],
        "须指向通用层规则本体（只写在平台层的话，非平台场景下读不到这条边界）",
    ],
]
kind = "file_groups"
message = "改动范围边界防线被破坏：{file} 缺失要点 {missing}——{desc}；不得删除、不得降级为建议"

[[guards.check_scope_boundary_guard]]
end_by = "end::scope-boundary[]"
file = "prompts/_common.txt"
groups = [
    [
        "只允许修改本次任务的工作空间",
        [
            "只允许修改本次任务的工作空间",
            "入口工作空间",
        ],
        "主体禁令：只允许修改本次任务的工作空间（入口工作空间）",
    ],
    [
        "只允许修改当前项目",
        [
            "只允许修改当前项目",
            "入口项目",
        ],
        "平台侧口径：在代码托管平台上按『只允许修改当前项目（入口项目）』表达（提示词会被复制到任意项目执行，两处都要有）",
    ],
    [
        "引用 ≠ 授权",
        [
            "引用 ≠ 授权",
            "不构成改动它的授权",
        ],
        "引用不等于授权：引用了工作空间以外的文件/其他项目只构成读取许可、不构成改动授权",
    ],
    [
        "必须拒绝",
        [
            "必须拒绝",
            "依据",
        ],
        "拒绝的形态：发现越界须拒绝并说明依据，不得默默照做",
    ],
    [
        "显式声明",
        [
            "显式声明",
            "只读",
        ],
        "已知例外：用户显式声明纳入范围；用户声明『只读/禁止改动』优先",
    ],
]
kind = "split_block"
message = "改动范围边界防线被破坏：{file} 的 `scope-boundary` 片段缺失要点 {missing}——{desc}"
split_by = "tag::scope-boundary[]"
