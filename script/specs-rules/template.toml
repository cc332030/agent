# 模板类内容的单独归类（用户提出）——规则数据（纯数据 TOML），引擎在 script/rules_engine.py。
#
# 一类规则一个文件（本防线按"被测落点"切分，不与其他防线堆在一起）；加载侧按目录自动扫描。
# 判据本体在维护方自查层（`specs-project-maintainer/spec-lifecycle.adoc`）——它描述的是
# "规范集合自己怎么组织"，对引用方项目不成立，故属维护方层；`AGENTS.adoc` 是它的登记处、
# `specs/general/context.adoc` 是公共侧的一跳引用（公共侧只给方向、不给判据本体）。

[[guards.check_template_separation_guard]]
file = "specs-project-maintainer/spec-lifecycle.adoc"
section = "要么不读、要么读全部的规范"
groups = [
    [
        "归类判据（什么算模板类内容）",
        [
            "归类判据（L1，先判再动手）",
            "要么不读、要么读全部",
            "代码模板",
        ],
        "须写明**归类判据**并点名代码模板——缺则这条会被读成「只针对代码模板」或「什么都能算」；用户口径里的典型正是代码模板（可整段复制的骨架与片段）",
    ],
    [
        "判定标准三条（复制 / 整份取用 / 独立于上下文）",
        [
            "判定标准（任一命中即属模板类内容）",
            "取用形态是",
            "要么不读、要么读全部",
            "独立于上下文",
        ],
        "须给可逐条核对的判定标准（取用形态是复制 / 判据要么不读要么读全部 / 取值独立于上下文），并写明「三条都不命中即普通规范条目」——只写「有些内容要复制」等于没有判据，执行者会把任意内容按自己方便归类",
    ],
    [
        "不得混放（规则 + 双向损害）",
        [
            "代码模板不与其他规则混放（L1）",
            "不得",
            "同一",
            "常驻层",
        ],
        "须写入**不得与其他规则混放**（同落点、常驻层两处都不行）——这是用户口径的正题（「不要和其他内容放一起，放一起浪费上下文」）；缺则模板会继续被塞进主题文件，读规则时白付模板的上下文",
    ],
    [
        "不得混放的判定标准（同节/常驻层/加载触发）",
        [
            "判定标准（任一命中即违规）",
            "同一节或同一文件",
            "常驻层",
            "加载触发方式",
        ],
        "须给**不得混放**的逐条判定标准（与规则同节或同文件 / 落在常驻层 / 加载触发与规则共用）——缺则「混没混放」回到评判者手里，抄规则时被夹带进来的模板无从指认",
    ],
    [
        "各自独立与其例外（L2）",
        [
            "模板最好各自独立（L2）",
            "例外",
            "有关联性",
            "内容不多",
        ],
        "须写明**各自独立**并保留**例外**（模板之间有关联性、或内容不多拆开反而更麻烦时可以不拆）——用户原话即带这个例外（「除非有关联性或者内容不多拆开反而麻烦」）；缺例外会把「独立」读成硬性要求，逼出为达标而拆的空壳（与「一条规范何时该拆分」的「默认不拆」取向相反）",
    ],
    [
        "依据行",
        [
            "依据（标准名/编号）",
            "Agent Skills",
            "ISO/IEC Directives Part 2",
        ],
        "须有依据行（渐进披露与「文件须便于按现行版本取用」两条外部依据）——缺则本条变成无出处的个人偏好，读者无从判断它是业界共识还是随手约定",
    ],
]
kind = "section_groups"
message = "模板类内容单独归类防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_template_separation_guard]]
file = "AGENTS.adoc"
groups = [
    [
        "维护方入口登记（模板类内容的归类与单独落点）",
        [
            "模板类内容的归类与单独落点",
            "specs-project-maintainer/spec-lifecycle.adoc",
        ],
        "维护方入口须登记该落点——`AGENTS.adoc` 是「维护这类规范集合的项目」的加载点，缺则执行者读不到这套判据（判据齐备但没有入口，等于没写）",
    ],
]
kind = "file_groups"
message = "模板类内容单独归类防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_template_separation_guard]]
file = "specs/general/context.adoc"
section = "生成效率"
groups = [
    [
        "公共侧一跳引用（模板类内容另按「要么不读、要么读全部」单独归类）",
        [
            "模板类内容",
            "要么不读、要么读全部",
            "单独归类",
        ],
        "公共侧须留**一跳引用**（「模板类内容另按『要么不读、要么读全部』单独归类」）——引用方项目也会按需加载与组织内容，不给出这一跳则它只看到「按文件切分」，无从知道模板另有归类；公共侧**不写判据本体**（那是维护方的组织口径，写进来即两处真源）",
    ],
]
kind = "section_groups"
message = "模板类内容单独归类防线被破坏：{file} 缺失要点 {missing}——{desc}"
