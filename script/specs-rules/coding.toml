[[guards.check_abstraction_adoption_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "唯一装配点",
        [
            "唯一装配点",
            "不得要求多个使用点各自提供同一实现或同一配置",
        ],
        "L1（a）唯一装配点：可替换点只有一处声明/装配落点，不得要求多个使用点各自提供同一实现或同一配置",
    ],
    [
        "可替换点须有可用默认",
        [
            "可替换点须有可用默认",
            "禁止既无默认又不声明",
        ],
        "L1（b）可用默认：每个可替换点须有默认实现/默认值，给不出默认时须显式声明必填失败，禁止既无默认又不声明",
    ],
    [
        "次数不随使用点数量增加",
        [
            "次数不随使用点数量增加",
            "出现次数 > 1",
        ],
        "判定标准可执行：须给出可逐条核对的判定（同一实现/配置在接入方的出现次数不随使用点数量增加），否则只剩口号、无法判定",
    ],
    [
        "二者必居其一",
        [
            "二者必居其一",
        ],
        "判定标准可执行（b）：须写明'有默认'与'显式必填声明'二者必居其一，否则'给不出默认'会被当成默认的豁免",
    ],
    [
        "L1",
        [
            "L1",
            "L2",
        ],
        "级别标注：须标出 L1/L2（否则条文会被当成建议，或全部按 L1 反过来挤掉合理裁量）",
    ],
    [
        "自动装配",
        [
            "自动装配",
            "ServiceLoader",
        ],
        "要点：能自动装配/约定生效的不得要求接入方逐点手写（L2）",
    ],
    [
        "限定符",
        [
            "限定符",
            "参数穿透",
            "作用域",
        ],
        "要点：多实现用限定符声明式区分、禁逐层传参；跨层级对象用作用域/上下文承载（L2）",
    ],
    [
        "存量边界",
        [
            "存量边界",
            "随动迁移",
            "不发动全库改造",
        ],
        "存量边界：该条严于「每个使用点各传一遍实现」的常见既成做法，须写明适用于新写的对外能力与改到的既有抽象（随动迁移、不发动全库改造），否则会被读成「必须立即全量重构」",
    ],
    [
        "ISO/IEC 25010",
        [
            "ISO/IEC 25010",
            "ISO/IEC/IEEE 29148",
            "The Twelve-Factor App",
        ],
        "依据行：须标注标准名/编号（易用性与可维护性属质量特性、需求须单一无歧义可验证、配置外置与依赖显式声明），否则后人无从判断它还成不成立",
    ],
]
kind = "regex_section"
label = "抽象与接入成本"
message = "抽象与接入成本防线被破坏：{file}「{ABSTRACTION_ADOPTION_SECTION}」缺失要点 {missing}——{desc}；该条来自用户的真实设计失效报告，不得删除、不得降级为建议"
pattern = "^== 抽象与接入成本.*?(?=^== |\\Z)"

[[guards.check_api_contract_reuse_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "跨服务/对外调用的请求响应类优先移动复用",
        [
            "跨服务/对外调用的请求响应类优先移动复用",
            "L1",
        ],
        "条文：须有该条并标 L1（防被降级成建议、退回「另建一套更省事」）",
    ],
    [
        "数据库实体类",
        [
            "数据库实体类",
            "不移动",
        ],
        "例外一：须写明数据库实体类除用户声明外不移动（它跟随表结构归属）",
    ],
    [
        "第三方类型",
        [
            "第三方类型",
            "例外",
        ],
        "例外二：须写明「类里引用了第三方类型」才可另建，否则例外面无从判定",
    ],
    [
        "本项目自身的依赖不算三方依赖",
        [
            "本项目自身的依赖不算三方依赖",
        ],
        "边界：须写明本项目自身的依赖不算三方依赖——该字句被抽掉即等于给出一个随时可套用的豁免口（「它依赖本项目别的模块」就能另建一套）",
    ],
    [
        "移动而非复制",
        [
            "移动而非复制",
            "同步更新全部引用",
        ],
        "动作：须写明是移动（并同步更新原引用）而非复制——只复制会立刻产生两个可独立演化的定义",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "同名或仅差包名",
        ],
        "判定标准：须含可逐条核对的反例（新建同构类 / 同名或仅差包名 / 复制不改原引用 / 以「依赖本项目其他模块」为由拒绝移动 / 移动数据库实体类而无声明）",
    ],
    [
        "随动迁移",
        [
            "随动迁移",
            "全库",
        ],
        "存量口径：须写存量随动迁移（不发动全库改造），否则会被读成「必须立即全库搬类」",
    ],
    [
        "依据",
        [
            "依据",
        ],
        "依据：须保留依据名（该条属本集合更严取舍，依据只写名称/编号、不写全文）",
    ],
]
kind = "file_groups"
message = "请求/响应类复用防线被破坏：{file} 缺失要点 {missing}——{desc}；该条对应用户提出的真实失效（同一数据契约出现两个定义、字段与校验各自漂移），不得删除、不得降级为建议、不得放开例外与边界"

[[guards.check_api_contract_reuse_guard]]
file = "specs/stack/spring.adoc"
groups = [
    [
        "HTTP 接口路径优先用中划线（kebab-case）",
        [
            "HTTP 接口路径优先用中划线（kebab-case）",
            "L1",
        ],
        "条文：须有该条并标 L1（防被降级成建议或删除）",
    ],
    [
        "下划线",
        [
            "下划线",
            "驼峰",
        ],
        "判据须可判定：须点名禁止下划线与驼峰（只写「优先中划线」无法判定「哪些写法算违规」）",
    ],
    [
        "服务路由",
        [
            "服务路由",
            "已发布",
        ],
        "两处照旧：服务路由/网关前缀与已发布且外部依赖的对外路径须明文照旧，否则会被读成「所有路径都要改名」（对外路径改名即破坏既有调用方）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "混用",
        ],
        "判定标准：须含可逐条核对的反例（出现 `_` / 路径片段用驼峰或大写 / 同一接口内混用）",
    ],
    [
        "存量处理",
        [
            "存量处理",
        ],
        "存量口径：须写存量随动迁移，不发动全库改名",
    ],
    [
        "命名",
        [
            "命名",
            "java.adoc",
        ],
        "分工：须写明本条只管路径字符串，类名与标识符命名另按命名规则（防两条规则混用）",
    ],
]
kind = "file_groups"
message = "接口路径防线被破坏：{file} 缺失要点 {missing}——{desc}"

# 调度器只核**触发特征**（何时命中），不核条目本体的取值句——`请求响应类优先移动复用`、
# `本项目自身的依赖不算三方依赖`、`接口路径优先用中划线` 都是判据正文，抄进调度器即
# 第二真源（`terminology.adoc`「数据字典」的反膨胀）。
[[guards.check_api_contract_reuse_guard]]
file = "AGENTS_COMMON.adoc"
groups = [
    [
        "请求响应类（新增内部调用接口的请求/响应类时即命中）",
        [
            "请求响应类",
        ],
        "调度器通用层条目须带触发特征（新增内部调用接口的请求/响应类时即命中），否则该条永远不会被触发加载",
    ],
    [
        "接口路径（写/改路由路径时即命中）",
        [
            "路由路径",
        ],
        "调度器 Spring 条目须带接口路径的触发特征（写/改路由路径即命中），否则 Spring 执行者读不到",
    ],
    [
        "对外接口的请求/响应参数与返回值（数据契约载体）",
        [
            "对外接口",
            "返回值",
        ],
        "调度器通用层条目须带『对外接口的请求/响应参数与返回值用什么类承载』这一触发特征（缺则写对外接口时不会触发加载『数据契约的载体』条）",
    ],
]
kind = "file_groups"
message = "接口路径/契约复用防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_api_naming_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "Feign 接口命名带所属域/项目前缀",
        [
            "Feign 接口命名带所属域/项目前缀",
            "L1",
        ],
        "条文：须有该条并标 L1（防被降级成建议、退回随手取名）",
    ],
    [
        "只此一类",
        [
            "只此一类",
            "其他对外接口",
        ],
        "范围：须写明本条**只约束 Feign 接口**、其他对外接口不在内——用户明确「目前只需要考虑 feign，不需要考虑其他对外接口」，范围标注被删等于判定面可被自行放大",
    ],
    [
        "已存在的固定前缀",
        [
            "已存在的固定前缀",
            "先例优先",
        ],
        "判据：须写明「优先使用已有的固定前缀」（先例优先），否则等于要求现场发明一套",
    ],
    [
        "词首",
        [
            "词首",
            "UcUserApi",
            "OucUserApi",
        ],
        "判据须可判定：须给出「无先例时按项目名取词首组合」与 `UcUserApi`/`OucUserApi` 实例，否则读者无法判断自己是否命中",
    ],
    [
        "Feign",
        [
            "Feign",
            "Api",
        ],
        "适用面：须点名 Feign 声明式 HTTP 客户端接口与固定的接口后缀，否则「给接口加前缀」会被读成「所有接口」或「只加后缀」",
    ],
    [
        "判定",
        [
            "判定",
            "同一",
        ],
        "判定标准：须含可逐条核对的反例（有固定前缀却另取一套 / 同类接口有的带有的不带）",
    ],
    [
        "全名",
        [
            "全名",
            "拼全名",
        ],
        "边界：须写明前缀只承载归属、不拼服务全名（防把业务信息堆进类名）",
    ],
]
kind = "file_groups"
message = "接口命名防线被破坏：{file} 缺失要点 {missing}——{desc}；该条对应用户提出的真实失效（同域同类 Feign 接口碰名、按名看不出归属），不得删除、不得降级为建议、不得放大到其他对外接口"

[[guards.check_config_class_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "配置类不写逻辑",
        [
            "配置类不写逻辑",
            "任何情况都不允许",
            "ConfigurationProperties",
        ],
        "条文：须有『配置类不写逻辑』条、写明『任何情况都不允许』（无例外），并点名 `@ConfigurationProperties` 一类承载配置的类",
    ],
    [
        "POJO",
        [
            "POJO",
            "工具类或服务",
        ],
        "边界与去向：须写明配置类只保持 POJO 的基本功能、逻辑下沉到工具类或服务（不写去向则逻辑无处安放，执行者只能塞回去）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "条件分支",
            "对外部对象",
        ],
        "判定标准：须给出可逐条核对的形态（条件分支/循环、计算与对外访问、`@Bean` 装配形态），否则只剩一句口号、无法判定",
    ],
    [
        "存量处理",
        [
            "存量处理",
            "随动迁移",
        ],
        "存量边界：该条严于框架常规用法（Spring 官方允许配置类里放派生 getter），须指向「规范变更的存量处理」（随动迁移、不发动全库改造），否则等于静默推翻引用方既有做法",
    ],
    [
        "识别特征",
        [
            "识别特征",
            "Config",
            "Properties",
            "Options",
            "Settings",
        ],
        "识别特征：须按 Config/Properties/Options/Settings 等命名识别，否则命名不规范的配置类会被漏过",
    ],
]
kind = "file_groups"
message = "配置类不写逻辑防线被破坏：{file} 缺失要点 {missing}——{desc}；该条是用户明确要求的硬性约定（无例外），不得删除、不得降级为建议"

[[guards.check_conversion_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "对象转换（多层嵌套对象的转换）",
        [
            "对象转换（多层嵌套对象的转换）",
        ],
        "条文：须有「对象转换（多层嵌套对象的转换）」节，作为该条的抽象形态落点",
    ],
    [
        "首选声明式映射",
        [
            "首选声明式映射",
            "优先",
        ],
        "首选路径：须写明**优先**用声明式映射完成转换（去掉优先口径即退回「怎么顺手怎么写」）",
    ],
    [
        "达标判据",
        [
            "达标判据",
            "一处来源",
            "静默",
        ],
        "判据：须写明判据是**目标式**的——同一份转换只有一处来源、结构变化时不静默漏字段（缺则判据退回「用没用某个库」）",
    ],
    [
        "主动声明",
        [
            "主动声明",
            "备注原因",
        ],
        "备注原因：须写明除主动声明外优先声明式映射、确需手写时备注原因（缺则「手写须写理由」这一动作丢失）",
    ],
    [
        "等价路径同样合规",
        [
            "等价路径同样合规",
        ],
        "多条路径：须写明深拷贝/结构复制工具、序列化中转、手工构建器等等价路径同样合规（缺则该条易被读成「必须用某个库」、与用户「不做强制性限制」的口径相抵）",
    ],
    [
        "无嵌套（单层）的转换不在本条范围内",
        [
            "无嵌套（单层）的转换不在本条范围内",
        ],
        "范围：须写明**无嵌套（单层）的转换不在本条范围内**（不是例外、是本条不管；口径按嵌套判、不得按字段数判）",
    ],
    [
        "纯数据结构类",
        [
            "纯数据结构类",
        ],
        "落点：须写明映射声明不塞进纯数据结构类（与「类设计」的纯数据结构类不写逻辑一致）",
    ],
    [
        "例外与边界（L2",
        [
            "例外与边界（L2",
            "表达不了",
        ],
        "例外与边界：须写明映射声明表达不了的语义不适用本条——例外被写宽即等于给出一个随时可套用的豁免口",
    ],
    [
        "存量边界",
        [
            "存量边界",
            "随动迁移",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移、不发动全库改造），否则等于静默推翻引用方既有的手写转换",
    ],
    [
        "依据",
        [
            "依据",
            "ISO/IEC 25010",
        ],
        "依据行：须标标准名/编号（防依据被整段删除后无从追溯）",
    ],
]
kind = "file_groups"
message = "对象转换防线被破坏：{file} 缺失要点 {missing}——{desc}；该条对应用户提出的规范建议（多层嵌套对象转换优先用 MapStruct、建议不手写转换代码），不得删除、不得回退成强制面"

[[guards.check_conversion_guard]]
file = "specs/stack/java.adoc"
groups = [
    [
        "对象转换（MapStruct）",
        [
            "对象转换（MapStruct）",
            "coding.adoc",
        ],
        "Java 栈须有该节并指向通用层规则本体（只让通用层有、栈文件没有，Java 执行者按栈文件学仍会手写转换）",
    ],
    [
        "优先用 MapStruct",
        [
            "优先用 MapStruct",
        ],
        "条文：须点名**优先**用 MapStruct（用户口径「使用 mapstruct 的优先性」）",
    ],
    [
        "建议不手写转换代码",
        [
            "建议不手写转换代码",
        ],
        "条文：须写明**建议**不手写转换代码（用户原文口径，非强制）",
    ],
    [
        "不做强制性限制",
        [
            "不做强制性限制",
            "等价路径同样合规",
        ],
        "非强制：须写明不做强制性限制、等价路径（深拷贝/序列化中转等）同样合规——写成强制面即与用户「不是强制，但是建议」相抵",
    ],
    [
        "@Mapper",
        [
            "@Mapper",
        ],
        "写法落点：须给出 `@Mapper` 映射接口这一声明形态，否则读者不知道「声明在哪写」",
    ],
    [
        "映射方法自动调用",
        [
            "映射方法自动调用",
        ],
        "嵌套与集合：须写明对象含对象、集合含对象由**映射方法自动调用/集合映射方法**表达（多层嵌套这一靶心的推荐写法落点）；并须写明手写循环不构成违规",
    ],
    [
        "不并存两套写法",
        [
            "不并存两套写法",
            "先例优先",
        ],
        "先例优先：须写明项目已有转换工具/既有 Converter 先例时跟随先例、不新增第二套写法",
    ],
    [
        "无嵌套（单层）的转换不在本条范围内",
        [
            "无嵌套（单层）的转换不在本条范围内",
        ],
        "范围：须同步写明单层转换不在本条范围内（与通用层同口径）",
    ],
    [
        "备注",
        [
            "备注",
            "原因",
        ],
        "备注原因：须写明确需手写时在代码注释写明原因（与通用层同口径）",
    ],
    [
        "例外与边界（L2",
        [
            "例外与边界（L2",
        ],
        "例外须标级并写明边界（无例外条文则把既有做法一刀切）",
    ],
    [
        "存量",
        [
            "存量",
            "execution.adoc",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移）",
    ],
]
kind = "file_groups"
message = "对象转换防线被破坏：{file} 缺失要点 {missing}——{desc}"

# 调度器只核**触发特征**（写多层嵌套对象之间的搬运代码时即命中）：`MapStruct` 是
# `java.adoc` 的条目本体专名，通用层与 Java 栈条目都不得把它当识别特征抄进来
# （通用层尤其：它跨语言、不得点名具体库）。
[[guards.check_conversion_guard]]
file = "AGENTS_COMMON.adoc"
groups = [
    [
        "多层嵌套对象之间的搬运",
        [
            "多层嵌套对象",
        ],
        "通用层『编写代码』条目须带该条的抽象识别特征（写多层嵌套对象之间的搬运代码时即命中），否则读到它的场景（写对象转换代码）不会触发加载",
    ],
    [
        "对象转换（Java 栈）",
        [
            "对象转换",
        ],
        "Java 技术栈登记须带该条识别特征（写多层嵌套对象之间的搬运代码时即命中），否则 Java 项目按栈登记加载时看不到这条",
    ],
]
kind = "file_groups"
message = "对象转换防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_http_semantics_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "== HTTP 接口语义",
        [
            "== HTTP 接口语义",
        ],
        "HTTP 接口语义须有独立承载节（缺则方法与状态码语义无处可查，只剩路径命名风格）",
    ],
    [
        "适用范围",
        [
            "适用范围",
            "对外提供或调用 HTTP 接口",
        ],
        "须写明**适用范围**（对外提供或调用 HTTP 接口的项目才适用）——无 HTTP 接口的项目不应被这条约束（准入面：不引入高频误伤）",
    ],
    [
        "安全方法不得产生状态变更（L1）",
        [
            "安全方法不得产生状态变更（L1）",
            "GET",
            "HEAD",
            "OPTIONS",
            "TRACE",
        ],
        "安全方法不得产生状态变更须是 **L1** 且点明四个安全方法——这是唯一一条不可豁免且可判定的底线，被降级即等于允许用 GET 做写操作",
    ],
    [
        "不得",
        [
            "不得",
            "承载写",
        ],
        "须有正向禁令：不得用安全方法承载写/删/状态流转等有副作用的动作",
    ],
    [
        "幂等",
        [
            "幂等",
            "PUT",
            "DELETE",
            "自动重试",
        ],
        "幂等语义与重试须对齐（`PUT`/`DELETE` 幂等、非幂等请求不得配自动重试）",
    ],
    [
        "状态码按语义使用（L2）",
        [
            "状态码按语义使用（L2）",
            "4xx",
            "5xx",
            "success:false",
        ],
        "状态码按语义使用须是 L2 且含**可判定的反例**（把错误包成 `200` + 响应体 `success:false`）——没有反例的「按语义使用」无法判定",
    ],
    [
        "对外错误响应统一结构（L3",
        [
            "对外错误响应统一结构（L3",
            "application/problem+json",
        ],
        "Problem Details 须**如实定为 L3（可选）**并点明媒体类型——外部材料只定义了一种格式、未要求必须采用，不得升成 L1/L2",
    ],
    [
        "RFC 9110",
        [
            "RFC 9110",
            "RFC 9457",
        ],
        "依据行须写标准名/编号（RFC 9110 / RFC 9457，只写名称、不附链接）",
    ],
]
kind = "file_groups"
message = "HTTP 接口语义防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_http_semantics_guard]]
file = "library/sources.adoc"
groups = [
    [
        "HTTP 接口语义与错误响应（RFC 9110 / RFC 9457）",
        [
            "HTTP 接口语义与错误响应（RFC 9110 / RFC 9457）",
        ],
        "图书馆须有该主题节（缺则条文无逐字依据可核对）",
    ],
    [
        "essentially read-only",
        [
            "essentially read-only",
            "GET, HEAD, OPTIONS, and TRACE methods are defined to be safe",
        ],
        "须有 RFC 9110 安全方法的**逐字**引文锚点（依据不得被压成一句名称）",
    ],
    [
        "idempotent",
        [
            "idempotent",
            "automatically retry",
        ],
        "须有 RFC 9110 幂等与重试的**逐字**引文锚点",
    ],
    [
        "three-digit integer code",
        [
            "three-digit integer code",
        ],
        "须有 RFC 9110 状态码定义的**逐字**引文锚点",
    ],
    [
        "machine-readable details of errors",
        [
            "machine-readable details of errors",
            "application/problem+json",
        ],
        "须有 RFC 9457 的**逐字**引文锚点与媒体类型",
    ],
    [
        "同义性",
        [
            "同义性",
            "判据化取值",
        ],
        "须**如实标注同义性差异**：标准给的是协议语义，不得用 200 包错误、非幂等请求不得自动重试是本集合推出的**判据化取值**，不得读成标准原文",
    ],
    [
        "可选项",
        [
            "可选项",
        ],
        "须标明 Problem Details 是标准定义的**格式**、非强制要求（本集合据此定 L3）",
    ],
]
kind = "file_groups"
message = "HTTP 接口语义依据被破坏：library/sources.adoc 缺失要点 {missing}——{desc}"

[[guards.check_persistence_access_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "持久化访问（数据库/缓存等）",
        [
            "持久化访问（数据库/缓存等）",
        ],
        "条文：须有「持久化访问（数据库/缓存等）」节，作为该条的抽象形态落点",
    ],
    [
        "统一入口（L1）",
        [
            "统一入口（L1）",
        ],
        "统一入口：须写明持久化操作走该技术给定的统一入口（**不点名具体框架**），并标 L1（防被降级成建议）",
    ],
    [
        "优先用类型安全/声明式查询构造 API（L1）",
        [
            "优先用类型安全/声明式查询构造 API（L1）",
            "方法引用",
            "findByXxx",
        ],
        "替代形态优先：须写明优先且一律使用类型安全/声明式查询构造 API（以方法引用/属性名引用表达列名、声明式方法名如 `findByXxx`、类型化 Criteria），标 L1",
    ],
    [
        "替代优先（L1）",
        [
            "替代优先（L1）",
            "表达不了",
        ],
        "替代优先：须写明技术自带的替代写法（类型安全/声明式形态）强制优先、仅在表达不了时才退回通用构造器且须写明理由（无此条则'无法替代'的边界无处可判）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "字符串",
            "绕过",
        ],
        "判定标准：须给出可逐条核对的**抽象**形态（构造器 `new`、字符串写列名、绕过统一入口），否则只剩一句口号",
    ],
    [
        "例外与边界",
        [
            "例外与边界",
            "跨语言执行脚本的落点",
        ],
        "例外与边界：须写明本条不禁止跨语言语句的承载（映射文件）与类型安全 API 表达不了的语义（退回时调用入口不变），否则会与「跨语言执行脚本的落点」互相打架",
    ],
    [
        "存量边界",
        [
            "存量边界",
            "随动迁移",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移、不发动全库改造），否则等于静默推翻引用方既有的 `new` 用法",
    ],
    [
        "依据",
        [
            "依据",
            "ISO/IEC 25010",
        ],
        "依据行：须标标准名/编号（防依据被整段删除后无从追溯）",
    ],
]
kind = "file_groups"
message = "持久化访问防线被破坏：{file} 缺失要点 {missing}——{desc}；该条对应用户明确提出的硬性要求（强制走统一入口、禁止 `new` 查询构造器），不得删除、不得降级为建议"

[[guards.check_persistence_access_guard]]
file = "specs/stack/java.adoc"
groups = [
    [
        "持久化访问（MyBatis-Plus / JPA 等）",
        [
            "持久化访问（MyBatis-Plus / JPA 等）",
            "coding.adoc",
        ],
        "Java 栈须有该节并指向通用层规则本体（只让通用层有、栈文件没有，Java 执行者按栈文件学仍会随手 new）",
    ],
    [
        "lambdaQuery",
        [
            "lambdaQuery",
            "lambdaUpdate",
            "ktQuery",
            "ktUpdate",
        ],
        "四个成员方法名须齐全（`lambdaQuery()`/`lambdaUpdate()`/`ktQuery()`/`ktUpdate()`）——漏掉 `kt*` 会让 Kotlin 项目学不全、漏掉 `lambdaUpdate` 则更新侧无落点",
    ],
    [
        "new QueryWrapper",
        [
            "new QueryWrapper",
            "子类",
            "new LambdaQueryWrapper",
        ],
        "禁止面须点名 `new QueryWrapper` 的**子类**（含 `new LambdaQueryWrapper`）——用户要求'禁止 new QueryWrapper 及其子类'，只写非 Lambda 形态即被放宽",
    ],
    [
        "Wrappers",
        [
            "Wrappers",
            "静态构造",
            "不得",
            "Mapper 接口",
        ],
        "`Wrappers` 一族静态构造方法须明确**同在禁止面**（`Wrappers` + “静态构造” + “不得”同现），且须给出 `IService` 之外的唯一落点（`Mapper 接口`上的注解/映射文件或默认方法）——只写“改用 `Wrappers`”等于把 static 形态重新放行、只禁 static 而不给落点则执行者无处可去",
    ],
    [
        "例外（L2",
        [
            "例外（L2",
            "理由",
        ],
        "例外须标级并写明'须写清理由'（无例外条文则等于把既有写法一刀切，无例外且不写理由则等于留后门）",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "字符串",
            "方法引用",
        ],
        "判定标准：须给出可逐条核对的形态（构造器 new / 字符串列名 / 绕过 IService），含'可用方法引用表达却写字符串'这一条",
    ],
    [
        "随动迁移",
        [
            "随动迁移",
            "execution.adoc",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移）",
    ],
]
kind = "file_groups"
message = "持久化访问防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_pagination_guard]]
file = "specs/stack/java.adoc"
section = "持久化访问（MyBatis-Plus / JPA 等）"
kind = "section_groups"
groups = [
    [
        "分页查询的返回类型与转换方式（L1，用户点名）",
        [
            "分页查询的返回类型与转换方式（L1，用户点名）",
        ],
        "条文：须有该条并标 L1（防被降级成建议——『分页怎么写都行』读起来无害，于是自建 page 重新成立）",
    ],
    [
        "普通接口写 Result<IPage<Rsp>>",
        [
            "Result<IPage<Rsp>>",
            "普通接口",
            "Rsp",
        ],
        "普通接口一侧：须点名**普通接口**的返回类型写 `Result<IPage<Rsp>>`（框架分页容器 + 该接口自己的响应类）——缺则普通接口仍会自建分页类或返回裸 `IPage`，两侧契约失去区分",
    ],
    [
        "Feign 接口写 Result<自定义Page<Rsp>>",
        [
            "Feign",
            "Result<自定义Page<Rsp>>",
        ],
        "Feign 一侧：须点名**声明式 HTTP 客户端接口**用 `Result<自定义Page<Rsp>>` 并跟随先例——缺则该档要么无处可归、要么被读成『两侧都要 ipage』",
    ],
    [
        "page.convert 而非新建 page",
        [
            "page.convert",
            "不得新建一个 page",
            "逐个搬运字段",
        ],
        "靶心（用户原话）：字段转换须走 `page.convert`、**不得新建 page 再逐个搬运**——只写『用 convert』而不写禁止面时，『新建一个 page 更直观』会重新成立",
    ],
    [
        "理由（分页元数据原样保留、漏搬不报错）",
        [
            "当前页",
            "每页条数",
            "总记录数",
            "总页数",
            "原样保留",
        ],
        "理由：须写明 `convert` 在容器内部替换元素、分页元数据**原样保留**（当前页/每页条数/总记录数/总页数），新建 page 时逐项搬运、漏一项既不报错也不提示——缺理由则本条的级别与处置会被降级",
    ],
    [
        "判定标准（逐条可核对）",
        [
            "自建分页类",
            "不得互改",
            "自我豁免",
        ],
        "判定标准：须给出可逐条核对的四条（普通接口侧不合格 / Feign 侧出现 `new` page 再逐字段 set / 两侧先例互改 / 自我豁免）——缺则本条自身不可判定，只剩一句口号",
    ],
    [
        "与相邻条目的关系（防误读）",
        [
            "与相邻条目的关系（防误读）",
            "数据契约的载体",
            "对象转换（MapStruct）",
        ],
        "分工：须写明本条只管分页包装体的形状与元素转换的走法，元素不得是实体类（`specs/general/coding.adoc`「数据契约的载体」）、字段转换优先声明式映射（本文件「对象转换」）——缺则三条互相拆台（读者会以为本条允许实体类进分页、或不必用 MapStruct）",
    ],
    [
        "存量边界",
        [
            "随动迁移",
            "execution.adoc",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移、不发动全库改造），否则等于要求立刻批量重写既有分页接口",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO/IEC 25010",
            "本集合自己的判据化取舍",
        ],
        "依据行：须标标准名/编号并如实写明『两侧各用哪种包装体 + 一律走 `convert`』是本集合的取舍（框架未规定）——缺则读者会把本集合的取舍当成框架要求",
    ],
]
message = "分页查询返回类型与转换防线被破坏：{file}「持久化访问（MyBatis-Plus / JPA 等）」缺失要点 {missing}——{desc}；该条对应用户点名的要求（普通接口 `Result<IPage<Rsp>>`、Feign 接口 `Result<自定义Page<Rsp>>`、转换走 `page.convert` 而不是新建 page），不得删除、不得降级"

[[guards.check_reuse_precedent_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "既有实现与先例优先",
        [
            "既有实现与先例优先",
            "L1",
        ],
        "条文：须有「既有实现与先例优先」条并标 L1（防被降级成建议）",
    ],
    [
        "不得以语言内置写法绕过既有能力",
        [
            "不得以语言内置写法绕过既有能力",
        ],
        "条文：须有「不得以语言内置写法绕过既有能力」条，否则『有先例也照写原生』的失效会重新出现",
    ],
    [
        "IdUtil",
        [
            "IdUtil",
            "CIdUtils",
            "UUID",
        ],
        "反例：须点出 UUID 手写这类典型反例，判据才可判定",
    ],
    [
        "CollUtil.isNotEmpty",
        [
            "CollUtil.isNotEmpty",
            "isEmpty",
        ],
        "反例：须点出集合判空手写这类典型反例，判据才可判定",
    ],
    [
        "先例",
        [
            "先例",
            "依赖",
        ],
        "判定动作：须写明先查项目自有能力/先例、再查已引入依赖，否则『先查』无落点",
    ],
]
kind = "file_groups"
message = "既有实现与先例优先防线被破坏：{file} 缺失要点 {missing}——{desc}；该条对应用户报告的真实失效，不得删除、不得降级为建议"

[[guards.check_deprecated_api_guard]]
file = "specs/general/coding.adoc"
section = "警告与弃用"
kind = "section_groups"
groups = [
    [
        "不得使用被弃用的类/API，改用替代者（L1）",
        [
            "不得使用被弃用的类/API，改用替代者（L1）",
            "不得在新代码里使用",
            "替代者",
        ],
        "条文：须有该条并标 L1，且明确**新代码不得使用被弃用者、须改用替代者**——只写「注意弃用」不构成禁止，降级成建议则用户点名的失效照旧发生",
    ],
    [
        "替代者优先取升级后的新类/同名新类",
        [
            "优先取升级后的新类、或同名新类",
        ],
        "取值：须写明替代者**优先取升级后的新类、或同名新类**（其余替代形态次之）——用户点名「升级类/同名新类最好」，缺则该取向丢失、执行者会随手挑一个能跑的替代",
    ],
    [
        "弃用标记形态",
        [
            "`@Deprecated`",
            "deprecated",
            "文档标注废弃",
        ],
        "识别特征：须给出弃用的识别形态（注解/文档标注等），否则「哪些算被弃用」回到临场裁量",
    ],
    [
        "判定标准（任一命中即违规）",
        [
            "判定标准（任一命中即违规）",
            "直接使用",
            "换了个弃用者不算替代",
        ],
        "判定标准：须可逐条核对——含「直接使用弃用类型/成员」与「替代者本身仍是弃用者（换了个弃用者不算替代）」两条，缺则判据退化成口号",
    ],
    [
        "例外（L2",
        [
            "例外（L2",
            "不存在",
            "保持原状",
        ],
        "例外：须写明替代者在依赖面内不存在时可保持原状（须写理由）——无例外则会把「框架只提供弃用入口」判成违规，例外写宽则给出随时可套用的豁免口",
    ],
    [
        "默认不为此调整依赖、也不动依赖版本（L1）",
        [
            "默认不为此调整依赖、也不动依赖版本（L1）",
            "不得**顺带改依赖清单或依赖版本",
        ],
        "边界（用户点名）：须写明**默认不调整依赖、也不动依赖版本**——缺则「换掉弃用类」会被读成许可顺手升级依赖，与用户口径相抵",
    ],
    [
        "存量不构成豁免",
        [
            "本条**只管既有调用点",
            "不构成**对新代码的豁免",
        ],
        "防误读：须写明「内部弃用不迁移」只管既有调用点、**不构成对新代码的豁免**——缺则该存量条会把新代码的禁止面整段抵消（两条同处一节、极易被读成后条覆盖前条）",
    ],
    [
        "依据",
        [
            "依据（标准名/编号）",
        ],
        "依据行：须标标准名/编号（防依据被整段删除后无从追溯）",
    ],
]
message = "弃用类/API 防线被破坏：{file}缺失要点 {missing}——{desc}；该条对应用户提出的规范调整（不用被弃用的类、改用替代者，默认不动依赖与版本），不得删除、不得降级为建议"

[[guards.check_deprecated_api_guard]]
file = "specs/general/dependency.adoc"
section = "升级与废弃"
kind = "section_groups"
groups = [
    [
        "弃用类/API 的迁移默认不动依赖面（L1）",
        [
            "弃用类/API 的迁移默认不动依赖面（L1）",
            "默认不调整依赖、也不动依赖版本",
            "coding.adoc",
        ],
        "依赖侧落点：须有该条并回指 `coding.adoc`「警告与弃用」（不写则依赖侧看似许可顺手升级）、且明确**默认不调整依赖、也不动依赖版本**——依赖条与代码条同向，缺一处即被读成「弃用迁移可以改版本」",
    ],
    [
        "判定标准",
        [
            "判定标准",
            "依赖清单或版本变化",
            "未走本节升级流程",
        ],
        "判定标准：须写明「弃用迁移的改动里出现依赖清单/版本变化、却未走升级流程」即违规，否则该条无法核对",
    ],
]
message = "弃用类/API 防线被破坏：{file}缺失要点 {missing}——{desc}；该条对应用户口径（弃用迁移默认不动依赖与版本），不得删除"

[[guards.check_logical_delete_naming_guard]]
file = "specs/general/coding.adoc"
section = "持久化访问（数据库/缓存等）"
kind = "subsection_groups"
subsection = "方法名与逻辑删除的对应（查询方法名即删隐面）"
groups = [
    [
        "条文：方法名即删隐面、标 L1",
        [
            "方法名即删隐面（L1",
        ],
        "条文：须有该条并标 L1（防被降级成建议——『名字怎么起都行』读起来无害，于是同一实体上两种口径各自漂移）",
    ],
    [
        "默认面取该技术是否自动附加删除标志条件",
        [
            "**默认面取该技术是否自动附加删除标志条件**",
            "默认＝不过滤删除标志",
            "默认＝只取未删",
        ],
        "靶心（用户原话）：须写明**默认面随该技术是否自动附加删除标志条件而变**——只写『非默认面要带特征』而不写默认面怎么定时，默认面会退回『约定俗成的那一面』，同一条契约在两种技术下配两套含义",
    ],
    [
        "非默认面须带特征词",
        [
            "**方法名须带该面的特征词**",
        ],
        "用户原话：查**非默认面**的数据须在方法名上带**删隐面特征词**（含已删/只取未删/只取已删）——缺则『查已删也顺手叫 list()』重新成立",
    ],
    [
        "判定标准（逐条可核对）",
        [
            "**判定标准（任一命中即违规）**：① 不带删隐面特征词的方法名查询了",
            "同一实体上同一删隐面并存两个名字",
        ],
        "判定标准：须给出可逐条核对的四条（不带特征词却查非默认面 / 特征词与查询面不一致 / 同一删隐面并存两个名字 / 自我豁免）——缺则本条自身不可判定，只剩一句口号",
    ],
    [
        "理由（删隐面只能从技术配置反推）",
        [
            "无从预期",
        ],
        "理由：须写明同一名字在两种技术下一次是含已删、一次是只取未删，**删隐面只能从技术配置反推、读代码的人与评审者都无从预期**——缺理由则本条的级别与处置会被降级",
    ],
    [
        "默认面的唯一例外（项目自身规范或先例）",
        [
            "项目自身规范",
            "先例",
        ],
        "防读成『框架文档即项目默认』：须写明项目自身规范或既有先例可定义该项目的默认口径、且**同一项目内只保留一种**——缺则同一项目里按各实体配置各算一套",
    ],
    [
        "边界（防扩大）",
        [
            "**只**约束**按实体/表做查询的方法名**",
            "includeDeleted",
        ],
        "边界：须写明只管按实体/表做查询的方法名（非查询方法、局部变量与参数名、非实体数据结构名不适用），且显式参数/调用形态与特征词等同——缺则本条会被读成『所有方法名都要加后缀』",
    ],
    [
        "存量边界",
        [
            "随动迁移",
            "execution.adoc",
            "不得据本条做全库改名",
        ],
        "存量边界：须指向「规范变更的存量处理」（随动迁移、不告警、**不得做全库改名**），否则等于要求立刻批量重写既有方法名（改名会同时破坏调用方与下游契约）",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO/IEC 25010",
            "本集合的判据化取舍",
        ],
        "依据行：须标标准名/编号并如实写明『默认面随技术是否自动附加而变 + 非默认面须带特征词』是本集合的取舍（外部材料只给『命名须表意、避免歧义』这一下限）——缺则读者会把本集合的取舍当成标准要求",
    ],
]
message = "方法名与逻辑删除的对应防线被破坏：{file}「持久化访问（数据库/缓存等）」的「方法名与逻辑删除的对应」缺失要点 {missing}——{desc}；该条对应用户点名的要求（未启用逻辑删除时默认含已删、启用时默认只取未删，查非默认面须带特征词；不限 MyBatis-Plus、自己实现的逻辑删除与其他语言同样适用），不得删除、不得降级"

[[guards.check_logical_delete_naming_guard]]
file = "specs/stack/java.adoc"
section = "持久化访问（MyBatis-Plus / JPA 等）"
kind = "section_groups"
groups = [
    [
        "Java 落点条文",
        [
            "方法名与逻辑删除的对应",
        ],
        "Java 落点：须有该条并把判据指回通用层（防框架专名散落到通用层）",
    ],
    [
        "框架专名（@TableLogic 与自动附加）",
        [
            "TableLogic",
            "自动为查询附加删除标志条件",
        ],
        "框架专名唯一落点：须点名 `@TableLogic` 与『框架自动为查询附加删除标志条件』这一机制——缺则 MyBatis-Plus 侧的默认面无从判定",
    ],
    [
        "JPA 对照（@SQLDelete 不自动附加）",
        [
            "SQLDelete",
        ],
        "对照档：须写明 `@SQLDelete` 只改删除动作、不自动附加查询条件（『自动附加』以声明实际生效为准）——缺则该侧会被按『启用了逻辑删除』误判",
    ],
    [
        "存量边界指回通用层",
        [
            "不改名",
        ],
        "存量边界：须写明既有方法不改名、不告警（口径在通用层同条）——缺则 Java 侧会被读成立刻批量改名",
    ],
]
message = "方法名与逻辑删除的对应（Java 落点）防线被破坏：{file}「持久化访问（MyBatis-Plus / JPA 等）」缺失要点 {missing}——{desc}"

[[guards.check_logical_delete_naming_guard]]
file = "library/adoption.adoc"
kind = "file_groups"
groups = [
    [
        "图书馆侧登记（本集合取舍 + 未规定）",
        [
            "方法名与逻辑删除的对应",
            "本集合自己的判据化取舍",
            "没有任何材料规定",
        ],
        "图书馆：本集合更严取舍须登记并如实标注外部材料**未**规定该层（防读者把它读成标准规定）",
    ],
]
message = "方法名与逻辑删除的对应防线被破坏：{file} 缺失要点 {missing}——{desc}"

# 判据本体侧**按条目自己的正文核**（`bullet_tokens`，不是整节）：「命名与代码质量」一节里
# 相邻条目与本条共用大量字样（「接口/类/注解成员排序」条含成员次序的全文、「实体类字段按
# 数据库顺序」条含表列序、「静态字段排在类最前」条含存量边界句），整节核时这些相邻条目会
# 把从本条抽走的字样**兜住**。本轮实测：按整节核时把本条整条移出该节后，报出的缺项里
# `不发动全库重排`/`ISO/IEC 25010`/`ISO/IEC/IEEE 29148`/`**例外（L2）**`/`**存量边界**：按`/
# `**判定标准（任一命中即违规）**`/`不改变求值语义`/`赋值环节` 八项**一条都不报**——防线的
# 三个判定点（依据行、判定标准、例外、存量边界）实际是空的。
[[guards.check_chain_assignment_order_guard]]
bullet = "链式赋值顺序随字段顺序"
file = "specs/general/coding.adoc"
kind = "bullet_tokens"
message = "链式赋值顺序防线被破坏：{file} 的「链式赋值顺序随字段顺序」条**本条自己的正文**缺失 {missing}——该条对应用户提出的要求（链式调用里的赋值顺序与字段、数据库顺序保持一致），不得删除、不得降级、不得自成第二个次序来源。每条锚点须在**本条 bullet 内唯一**：本条正文里同一个词只出现一次，故锚点直接取该词即可被抽走即报；写成「更重要的在下」那类**同节他条也含**的短语时，核对对象换成本条也拦不住（锚点本身没命中过本条、无从判断是被抽走还是本来就没有）"
# 锚点一律取**本条正文里只出现一次**的词：本步的报错只在 token 不出现在本 bullet 时触发，
# 故 token 若在本条内部**另有出处**（依据行、同段复述），抽掉目标那句话后 token 仍在、
# 只是从别处命中，报不出来。本轮实测：`不发动全库重排` 三处出现、`ISO/IEC 25010` 两处、
# `不改变求值语义` 两处（边界句 + 「不改变」的另一种说法），逐词删净后仍全绿。
section = "命名与代码质量"
tokens = [
    # 条文：赋值顺序随字段顺序、标 L1（防被降级成建议——『顺序怎么写都行』读起来无害）
    "链式赋值顺序随字段顺序（L1，任何语言）",
    "**书写顺序须与字段顺序一致**",
    # 不另定次序、回指既有两条（防第二真源）
    "字段顺序**取该条已定义者",
    "实体类**按「实体类字段按数据库顺序」",
    "其他类**按「接口/类/注解成员排序」取该类的成员次序",
    # 理由（同一份字段清单只有一个次序来源）
    "同一份字段清单的第二个来源",
    "只是次序无声漂移",
    # 判定标准（逐条可核对）
    "**判定标准（任一命中即违规）**",
    "含按字母序、按使用频率、按改动追加的次序",
    "以「这样读起来更顺」",
    "把「字段顺序」读成「本条的专用顺序」",
    # 例外只在语义要求时开口（L2）
    "**例外（L2）**",
    "**执行次序决定语义**",
    "按所选字段在字段顺序里的相对先后",
    # 边界（防反用：只规定书写次序、不多写）
    "**只规定赋值环节的书写次序",
    "不要求为满足本条的次序而多写一次赋值",
    # 存量边界
    "改到哪处才顺带调整该处的次序",
    # 依据行（标准名/编号 + 本集合取舍声明）
    "**「赋值顺序随字段顺序」是本集合的判据化取值**",
    "通行风格指南未规定赋值次序须跟随字段次序",
]
# Java 落点侧同样**按条目自己的正文核**：「编码」一节里相邻条目含同样的框架专名
# （「成员与注解排序」条含实体类字段与表列序、「数据对象模板」条含 `@Accessors(chain = true)`
# 与 `@SuperBuilder`），整节核时把本条的载体名抽走也报不出来。本轮实测：把本条 Java 落点条文
# 整条改成「赋值环节按**书写者顺手的次序**排列，**不要求**按字段声明次序」后 **0 条报红**
# （锚点词全在相邻条目里命中过）。
[[guards.check_chain_assignment_order_guard]]
bullet = "链式赋值顺序随字段顺序（L1）"
file = "specs/stack/java.adoc"
kind = "bullet_tokens"
message = "链式赋值顺序（Java 落点）防线被破坏：{file} 的「链式赋值顺序随字段顺序（L1）」条**本条自己的正文**缺失 {missing}——Java 执行者按栈文件学时仍会随手排；载体是链式 setter 与 builder 链，顺序取字段声明次序"
section = "编码"
tokens = [
    "`@Accessors(chain = true)` 的链式 setter",
    "`@SuperBuilder` 的 builder 链",
    "**赋值环节的书写顺序按字段声明次序**",
    "判据本体、判定标准、例外与存量口径见",
    "此处**不复述**",
    "本条**不另定一套顺序**",
]
[[guards.check_chain_assignment_order_guard]]
file = "AGENTS_COMMON.adoc"
kind = "line_tokens"
anchor = ["任何代码活动"]
tokens = ["链式调用**（含链式赋值、builder 链的赋值环节）"]
message = "链式赋值顺序防线被破坏：{file} 的「任何代码活动」条目缺失 {missing}——缺该识别特征时，写链式赋值不会触发加载该条"

[[guards.check_chain_assignment_order_guard]]
file = "specs/stack/java-object.adoc"
kind = "file_groups"
groups = [
    [
        "模板侧照抄约定",
        [
            "赋值环节按字段声明次序",
            "链式赋值顺序随字段顺序",
        ],
        "模板：整段照抄的清单里须给出赋值顺序约定并指回判据本体——模板是新建对象时真正被读的那一份，缺则该约定在照抄时丢掉",
    ],
]
message = "链式赋值顺序（模板）防线被破坏：{file} 缺失要点 {missing}——{desc}"

# 图书馆侧**按那一行核**（`line_tokens`，不是整份文件）：这三条锚点在 `library/adoption.adoc`
# 里各自另有出处（同文件相邻条目含「本集合自己的判据化取舍」「没有任何材料规定」，标题句含
# 「链式赋值顺序随字段顺序」），按整份文件核时把本条整条掏空成一句空话也报不出来。本轮实测：
# 把该条替换成「链式赋值顺序随字段顺序，本集合自己的判据化取舍，没有任何材料规定。」后
# **0 条报红**——防线唯一的机械抓手在图书馆侧是空的。
[[guards.check_chain_assignment_order_guard]]
anchor = ["链式赋值顺序随字段顺序", "本集合自己的判据化取舍"]
file = "library/adoption.adoc"
kind = "line_tokens"
# 锚点同样取**该行内只出现一次**的词：`没有任何材料规定`/`Google Java Style Guide 3.4.2`/
# `用户原文口径` 在 `library/adoption.adoc` 的**别条目**里反复出现（分别 12 处 / 2 处 / 9 处），
# 抽掉本行那句后 token 仍在、只是从别的条目命中——本轮实测这三项逐词删净后**一条都不报**。
tokens = [
    "没有任何材料规定**“赋值环节的书写次序须跟随字段次序”",
    "也**没有**规定“以哪一处为准”",
    "**字段顺序不另定**",
    "**赋值顺序不一致即违规**",
    "**例外只在语义要求时开口**",
    "为什么取“跟随字段顺序”而不是“随书写者方便”",
    "只改了字段、忘了赋值处时不报错",
    "**本项目实证**：用户原文口径为“链式调用时，比如赋值，顺序 和字段、数据库顺序保持一致”",
]
message = "链式赋值顺序防线被破坏：{file} 的该条登记行缺失 {missing}——本集合的取值须如实标注外部材料**未**规定该层（防读者把它读成标准规定），并须给出理由与实证"

# 判据本体侧**按条目自己的正文核**（`bullet_tokens`，不是整节）：「命名与代码质量」一节里
# 相邻条目与本条共用大量字样（「链式赋值顺序随字段顺序」含"字段顺序"与存量边界句、「实体类
# 字段按数据库顺序」含"表列序"、「静态字段排在类最前」含例外句），整节核时这些相邻条目会把
# 从本条抽走的字样**兜住**。锚点一律取**本条 bullet 内只出现一次**的词。
[[guards.check_method_placement_guard]]
bullet = "成员与方法次序"
file = "specs/general/coding.adoc"
kind = "bullet_tokens"
message = "成员与方法次序防线被破坏：{file} 的「成员与方法次序」条**本条自己的正文**缺失 {missing}——该条对应用户提出的要求（新增成员按业务流程先后落点、允许加在已有成员的前面或中间、重载紧邻、先初始化的在前、被依赖者在前、数据转换方法写在查询接口之后、只对新增成员生效），不得删除、不得降级、不得被读成「一律追加到末尾」。每条锚点须在**本条 bullet 内唯一**"
section = "命名与代码质量"
tokens = [
    # 条文：新增成员按流程次序落点、标 L1（防被降级成建议）
    "**成员与方法次序（L1，任何语言）**",
    "**新增**成员/方法/函数时",
    "其**位置**按「**流程次序**」定",
    # 允许插前面/中间（用户点名的原话，也是与 Google「不得追加到末尾」的接口）
    "允许把新增的方法加在**已有成员的前面（含最前面）或中间**",
    "**不得**为「不动已有顺序」而一律追加到类/文件末尾",
    # 重载相邻
    "**重载成员**（同名不同参数）**一律紧邻已有同名成员**",
    "**重载之间的次序同样按流程次序**",
    # 例外：转换跟随查询
    "**例外（L2）**",
    "**数据转换方法**",
    "**默认写在查询它的那个接口/方法之后**",
    "先把查询写出来、再写转换",
    # 理由（位置即次序信息、追加序不报错）
    "位置本身表达「谁先谁后」",
    "多套互相无关的次序",
    "只是次序无声漂移",
    # 判定标准（逐条可核对）
    "**判定标准（任一命中即违规）**",
    "而按其流程次序本可在其他成员**之前或之间**",
    "把 `RestTemplate` 写在其依赖的 `ObjectMapper` 之前",
    "与已有同名成员**不相邻**",
    "数据转换方法排在其**查询接口之前**",
    # 边界（防反用：只管新增落点、不重排既有）
    "本条**只管新增成员的落点位置**",
    "**不要求**为满足本条而**重排**既有成员",
    # 存量边界：只对新增生效、review 不提出问题
    "本条**只对新增成员生效**",
    "**review 时把「既有成员的位置」当已知现状、不提出问题**",
    # 依据行（标准名/编号 + 本集合取舍声明）
    "**「按流程次序、可插前面或中间 + 重载相邻 + 转换跟随查询」是本集合的判据化取值**",
    "Google 只禁止「追加在末尾」这一形态",
]
# Java 落点侧同样**按条目自己的正文核**：「编码」一节里相邻条目含同样的字样
# （「成员与注解排序」含实体类字段与注解次序、「数据对象模板」含 builder 链），
# 整节核时把本条的载体名抽走也报不出来。锚点取**Java 侧的载体**——判据本体与
# 判定标准由通用层那条核，本侧只须证明「回指通用层 + 给到 Java 载体」还在。
[[guards.check_method_placement_guard]]
bullet = "成员与方法次序（L1，本文件是 Java 落点）"
file = "specs/stack/java.adoc"
kind = "bullet_tokens"
message = "成员与方法次序（Java 落点）防线被破坏：{file} 的「成员与方法次序（L1，本文件是 Java 落点）」条**本条自己的正文**缺失 {missing}——Java 执行者按栈文件学时仍会一律追加到类末尾、重载散落、转换方法跑到查询之前"
section = "编码"
tokens = [
    "按 `specs/general/coding.adoc`",
    "判据本体、判定标准、例外与存量口径见该处",
    "此处**不复述**",
    "**只给 Java 侧的载体**",
    "**成员**＝字段、方法、重载方法",
    "写在依赖它的 `RestTemplate` 之前",
    "**数据库实体与返回模型的互转方法**",
    "**静态初始化块与字段初始化顺序**",
]
[[guards.check_method_placement_guard]]
file = "AGENTS_COMMON.adoc"
kind = "line_tokens"
anchor = ["任何代码活动"]
tokens = ["要**新增字段/方法/函数**", "数据转换"]
message = "成员与方法次序防线被破坏：{file} 的「任何代码活动」条目缺失 {missing}——缺该识别特征时，新增方法不会触发加载该条"
[[guards.check_method_placement_guard]]
file = "AGENTS_COMMON.adoc"
kind = "line_tokens"
anchor = ["Java 项目"]
tokens = ["**成员与方法次序**"]
message = "成员与方法次序防线被破坏：{file} 的 Java 技术栈登记缺失 {missing}——缺该识别特征时 Java 项目按栈登记加载看不到这条"
[[guards.check_method_placement_guard]]
file = "README.adoc"
kind = "file_tokens"
tokens = ["**成员与方法次序**"]
message = "成员与方法次序防线被破坏：{file} 缺失要点 {missing}——README 目录说明不同步时，读者按 README 学习时无从知道有这条规则"
[[guards.check_method_placement_guard]]
file = "library/adoption.adoc"
kind = "line_tokens"
anchor = ["成员与方法次序", "本集合自己的判据化取舍"]
tokens = [
    "**没有任何材料规定**\"新增成员按流程次序落点",
    "**也**没有规定\"重载成员须相邻",
    "**只对新增成员生效**",
    "**review 时对既有成员的位置不提出问题**",
    "而\"只能往末尾加\"这条常见做法正是 Google 点名禁止的形态",
    "**本项目实证**：用户原文口径为",
]
message = "成员与方法次序防线被破坏：{file} 的该条登记行缺失 {missing}——本集合的取值须如实标注外部材料**未**规定该层（防读者把它读成标准规定），并须给出理由与实证"

# 『参数载体（不得以键值容器承载方法参数）』（用户从存量项目规范引入：严禁在任何方法形参中用
# `Map` 传递数据、必须使用具名对象；仅调用方主动声明时才允许）。
# 要治的失效：键值容器只有键名、没有成员声明——字段名/字段类型/必填性都没有落点，两端靠字面键
# 约定，**键名改一处即静默失效**，且键集合无法在编译期核对。
# 判据按**条目自己的正文**核（`bullet_tokens`，不是整节）：「数据契约的载体」一节里相邻条目同样
# 含「载体/契约/存量边界/依据」一类字样，按整节核时它们会把从本条抽走的要点**兜住**。
[[guards.check_param_carrier_guard]]
bullet = "不得以键值容器承载方法参数"
file = "specs/general/coding.adoc"
kind = "bullet_tokens"
message = "参数载体防线被破坏：{file} 的「不得以键值容器承载方法参数」条**本条自己的正文**缺失 {missing}——判据被抽走时，「这个参数会变」「字段太多」「只是个内部方法」会把键值容器放回来。每条锚点须在**本条 bullet 内唯一**"
section = "数据契约的载体（数据库实体类不进对外契约）"
tokens = [
    "不得以键值容器承载方法参数（L1）",
    "方法参数与返回值的载体是**具名类型**",
    "**字段名、字段类型与必填性都没有落点**",
    "**判定标准（任一命中即违规）**",
    "**已有具名类型或具名先例**",
    "**已有一个等价的具名类型**",
    "**例外与边界（L2）**",
    "**键值集合本身就是要表达的数据**",
    "**仅对该处、该次生效、不得泛化**",
    "本次未按需求变动的存量保持原样",
    "判据见 `specs/core/execution.adoc`",
]

# 同条的**定性**（该条是本集合自己的取舍、不是标准规定）与**依据名**在**节末的依据行**上——
# 那条是独立的 bullet，`bullet_tokens` 取不到它，故另起一步按整份文件核。
[[guards.check_param_carrier_guard]]
file = "specs/general/coding.adoc"
groups = [
    [
        "本集合取舍定性",
        [
            "「参数不得以键值容器承载」是本集合自己的判据化取舍",
        ],
        "定性：须如实标明这是本集合自己的判据化取舍（防读者把它读成某标准的规定，从而在'标准没这么要求'时自行放宽）",
    ],
    [
        "坏味道依据名",
        [
            "Martin Fowler《Refactoring》",
        ],
        "依据名：须保留可追溯的出处（该条的坏味道取向取自「数据泥团」；只写名称、不附链接）",
    ],
]
kind = "file_groups"
message = "参数载体防线被破坏：{file} 缺失要点 {missing}——{desc}"

# 加载门：调度器的「任何代码活动」条目须给出识别特征（缺则该条在"要写方法参数"这一场景
# 永远不会被加载——写规则与登记是同一个动作，与 `check_method_placement_guard` 同口径）。
[[guards.check_param_carrier_guard]]
anchor = ["任何代码活动"]
file = "AGENTS_COMMON.adoc"
kind = "line_tokens"
message = "参数载体防线被破坏：{file} 的「任何代码活动」条目缺失 {missing}——缺该识别特征时，写方法参数不会触发加载该条，键值容器仍会被随手用来传参"
tokens = ["要写**方法参数**（决定该参数用什么类型承载）"]
