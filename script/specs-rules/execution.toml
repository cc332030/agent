[[guards.check_dev_flow_guard]]
file = "specs/core/execution.adoc"
groups = [
    [
        "动手前先摸清现状与最佳方案（L1）",
        [
            "动手前先摸清现状与最佳方案（L1）",
            "先调研最佳实践",
            "先规划后执行",
            "调整内容一类需求",
            "先找现成可参照的既有标准与更优设计",
        ],
        "须有「动手前先摸清现状与最佳方案」L1 条且含**需求侧先找参照物**（用户提需求/新增与调整同属一类时，先查有无现成可参照的标准与更优设计；缺此半条则『调研最佳实践』只在执行者自愿时发生）",
    ],
    [
        "动手改文件前先读要改的那一处（L1）",
        [
            "动手改文件前先读要改的那一处（L1）",
            "**只到要改的那一处现状**",
            "**不得按记忆中的内容写入**",
            "按记忆写入会把本地内容覆盖掉",
        ],
        "须有「动手改文件前先读要改的那一处」L1 底线且含三要素：核对对象**只到要改的那一处现状**（与本次改动无关的部分**不必读**）、**不得按记忆中的内容写入**、后果句（按记忆写入会**把本地内容覆盖掉**）——缺则必加载层只剩『先查现状』这一句泛指，实测失效（按记忆覆盖本地内容）照样发生",
    ],
    [
        "不得绕开既有体系另写一套（L1）",
        [
            "不得绕开既有体系另写一套（L1）",
            "允许另写一套的条件只有三个",
            "不留两套并存",
        ],
        "须有「不得绕开既有体系另写一套」L1 条与三个允许条件（防『新写一套能满足需求』被当成完成）",
    ],
    [
        "大范围改动先确认（L1）",
        [
            "大范围改动先确认（L1）",
            "不得替用户判定某段既有流程",
            "已废弃",
        ],
        "须有「大范围改动先确认」L1 条与『是否废弃由用户认定』（旧流程的废弃不能被执行者自行认定）",
    ],
    [
        "改动前先定基线（L1）",
        [
            "改动前先定基线（L1）",
            "按改动性质判",
            "不做基线 ≠ 不做验证",
            "扫描项目声明的全部校验手段",
            "完整可用时必须先跑通",
            "落盘留证",
            "复跑同一套校验",
            "够不够用",
            "先按用例设计判据 review 既有用例",
            "本次改动的直接相关面",
            "不阻断动手",
        ],
        "须有「改动前先定基线」L1 条且含**适用边界回指**（按改动性质判、**不做基线 ≠ 不做验证**的降级防线，取值与例外只在通用层、此处不复述）+ 扫描/跑通/留证/复跑四要素 + **基线完整性（L2）**（清单够不够用、先 review 既有用例、只补本次直接相关面、无关缺口不阻断）",
    ],
    [
        "基线是否已定",
        [
            "基线是否已定",
            "是否与基线逐项比对",
        ],
        "任务生命周期表的「方案」「验证」两节点须带基线条（缺则该节点到点不会问基线）",
    ],
]
kind = "file_groups"
message = "开发流程防线被破坏：{file} 缺失要点 {missing}——{desc}；该要求不得删除、不得降级为建议"

[[guards.check_dev_flow_guard]]
file = "specs/general/planning.adoc"
groups = [
    [
        "先查现状（L1）",
        [
            "先查现状（L1）",
            "不得凭印象或标题推断",
            "核实方式",
        ],
        "须有「先查现状」L1 与『不得凭印象推断/须列核实方式』",
    ],
    [
        "先调研最佳方案（L1）",
        [
            "先调研最佳方案（L1）",
            "备选方案与取舍依据",
            "能交差",
        ],
        "须有「先调研最佳方案」L1（含备选方案与取舍依据、不得只给『能交差』的写法）",
    ],
    [
        "需求先找参照物（L1）",
        [
            "需求先找参照物（L1）",
            "新增与调整同属一类",
            "业界标准/权威约定",
            "既有通行设计范式",
            "本项目既有条目与先例",
            "既有实现与先例优先",
            "查不到",
            "未确证",
            "查到更优的设计就用更优的",
        ],
        "须有「需求先找参照物」L1 条（调整内容一类需求先检索①业界标准/权威约定②既有通行设计范式③本项目既有条目与先例三条路；查不到须能说出查证方式并标未确证；有更优设计用更优的；缺此条则『用户怎么说就怎么收』仍是合规路径）",
    ],
    [
        "说不出参照物与检索动作",
        [
            "说不出参照物与检索动作",
            "有标准可循却自造一套说法",
            "有更优设计却仍按原口述照收",
            "判定标准",
            "收的是哪一条",
            "不得只留结论、不留取舍",
            "依据",
        ],
        "「需求先找参照物」须带可核对判定标准（说不出参照物/自造一套说法/照收口述不说明理由三类命中即不合规），并写明**收的是哪一条须留痕**、**判定标准与依据各自成行**（判定标准句被抽走、或该条只剩一句口号时须报红）",
    ],
    [
        "默认改在既有实现上（L1）",
        [
            "默认改在既有实现上（L1）",
            "确实无法承载",
            "已被用户确认废弃",
            "已被证明优于",
            "不留两套并存",
        ],
        "须有「不得绕开既有体系另写一套」的三个允许条件与处置要求",
    ],
    [
        "大动之前先确认（L1）",
        [
            "大动之前先确认（L1）",
            "保持原状",
            "是否废弃只能由用户认定",
        ],
        "须有「大范围改动先确认」L1（未确认前保持原状、废弃由用户认定）",
    ],
    [
        "基线的适用边界（L1，只回指通用层真源）",
        [
            "基线的适用边界（L1）",
            "先判改动性质、再决定这条要不要做",
            "specs/general/verify.adoc",
            "唯一真源",
            "不重列第二套定义",
            "判不准时按更严的一侧",
        ],
        "「基线的适用边界」条须**只回指通用层真源**（性质判定与各档取值以 verify.adoc 为唯一真源、本条不重列第二套定义、判不准取严）——缺则要么基线分档失去通用层落点、要么本处重列一遍成为第二处会漂移的定义",
    ],
    [
        "动手前先定基线",
        [
            "动手前先定基线",
            "扫描既有验证手段（L1）",
            "能跑通即跑通、并落盘留证（L1）",
            "不完整或本来就红时怎么办（L1，防把基线变成硬性前置）",
            "没有任何校验手段",
            "留证不等于",
            "红的还是红的",
            "基线用于改完复跑（L1）",
            "既有用例不得为迁就改动而改判",
        ],
        "「动手前先定基线」须齐备扫描/跑通留证/**无手段与红测试不阻断**/复跑/与测试侧接口（缺降级分支会把基线变成引用方的硬性前置）",
    ],
    [
        "基线的完整性：手段与用例够不够用（L2，仅在该做基线时适用）",
        [
            "基线的完整性：手段与用例够不够用（L2，仅在该做基线时适用）",
            "先按这些判据 review 既有用例",
            "复核了哪些角度与样本",
            "正常路径",
            "异常路径",
            "正反例成对",
            "步骤与前置",
            "本次改动的直接相关面",
            "无关的存量缺口",
            "不阻断",
            "用例设计",
            "测试有效性",
        ],
        "「动手前先定基线」须含**基线完整性（L2）**条：手段与用例可能不完整（边界/正反例/步骤），先 review 既有用例并给出复核角度与未覆盖项，只补本次直接相关面、无关存量缺口不阻断（缺此条则『扫一遍声明的命令』仍会被当成完整基线）",
    ],
    [
        "ISO 10007",
        [
            "ISO 10007",
        ],
        "基线条须给依据（ISO 10007 配置管理与变更控制）",
    ],
]
kind = "file_groups"
message = "开发流程防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_dev_flow_guard]]
file = "specs/general/verify.adoc"
groups = [
    [
        "验证的适用边界（先判改动性质，再决定验证到什么程度）",
        [
            "验证的适用边界（先判改动性质，再决定验证到什么程度）",
            "这次改动会不会被未知项目加载、会不会改变别人的行为",
            "不做基线测试",
            "大规模重构",
            "会改动",
            "大部分内容",
            "移动/重命名不算",
        ],
        "「验证的适用边界」是改动性质 A/B/C 与「做不做基线」的**唯一真源**：须含判定问句与各档的基线取值（B 必做、A 默认不做但有例外、C 不做；移动重命名不算）——缺则基线又被当成所有改动的前置、代码类改动白付全量校验（本轮把散在 planning/execution/_common 的第二三处定义收敛至此）",
    ],
    [
        "验证须覆盖项目的全部既定校验手段（L1，仅限收尾那一次）",
        [
            "验证须覆盖项目的全部既定校验手段（L1，仅限收尾那一次）",
            "本身可能不完整",
            "先按 link:testing.adoc[]「用例设计」review",
            "哪些未覆盖",
            "本次改动的直接相关面",
        ],
        "「验证须覆盖项目的全部既定校验手段」须补『被跑的那一套够不够用』——手段与被跑用例本身可能不完整，须先按用例设计判据 review 并说明未覆盖/未确证项（只钉『跑全了』会漏掉『跑的那套本来就不全』）",
    ],
    [
        "按需验证、不滥验证（L1）",
        [
            "按需验证、不滥验证（L1）",
            "只在必须有验证的地方做",
            "尚未做完时不做阶段性验证",
            "合并为一次执行",
            "只覆盖本次改动的直接相关面",
            "先信已验过的、不重复验",
            "其后无改动",
            "不重复验",
            "自身之后又被改动",
            "不是这份内容",
            "不构成重验理由",
            "延后验证、一次到位",
        ],
        "「按需验证、不滥验证」五要素须齐：该不该验 / 没做完就不验（收尾须做、中间只在必要时加）/ 能合就合（合并为一次执行）/ 能简化就简化只覆盖改动范围（不得与其无关地全量验证）/ **先信已验过的、不重复验**（前面已验过且其后未改动的部分直接采信已有结果；重验只限『该部分自身之后又被改动 / 上次验的不是这份内容 / 上次结论本就不成立』三类，**单纯又提交了一个 commit 不构成重验理由**）——缺则验证重新被撒在每一步上、同一动作被来回重跑，交付被拖长而换不来新判据（用户口径：验证耗费太多时间）",
    ],
    [
        "汇报须与可核对的事实一致、不得狡辩（L1）",
        [
            "汇报须与可核对的事实一致，不得狡辩（L1）",
            "与**实际统计**不一致",
            "写进汇报前须重取一次",
            "先复取值再回答",
            "不得拿既有结论当辩词",
        ],
        "汇报一致性条：须含三条判定标准——①汇报中的数字与清单和实际统计不一致即不合规（数字须在写进汇报前重取一次）；②不得用『核对项全绿』顶替对用户所问的直接回答、用户给出相悖观察时先复取值再答；③不符成立时先更正汇报。缺则汇报与事实脱节无判据可拦（外部实证：两轮『压缩提交』后提交数与汇报对不上，执行者反复以核对项自证）",
    ],
]
kind = "file_groups"
message = "开发流程防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_dev_flow_guard]]
file = "specs/general/testing.adoc"
groups = [
    [
        "重构后须**同时满足既有用例与新用例**",
        [
            "重构后须**同时满足既有用例与新用例**",
            "既有用例不得为迁就重构而改判",
            "相关功能被显式移除",
            "前后完整兼容",
            "Semantic Versioning",
        ],
        "须有「重构后须同时满足既有用例与新用例」L1（含『既有用例不得改判、除功能被移除』与兼容性定义依据）——否则「让老用例变绿」会重成默认做法",
    ],
    [
        "兼容性无法满足时先确认（L1）",
        [
            "兼容性无法满足时先确认（L1）",
            "兼容不了",
            "老旧废弃流程",
            "不得自行取舍",
        ],
        "须有「兼容性无法满足时先确认」L1（含老旧废弃流程与『不得自行取舍』）",
    ],
]
kind = "file_groups"
message = "开发流程防线被破坏：{file} 缺失要点 {missing}——{desc}"

[[guards.check_dev_flow_guard]]
file = "prompts/_common.txt"
groups = [
    [
        "tag::baseline-and-compat[]",
        [
            "tag::baseline-and-compat[]",
            "先查现状、再谈方案",
            "先定基线",
            "按改动性质取值",
            "大动之前先确认",
            "不得绕开既有实现另写一套",
            "都不是不动手的理由",
            "基线清单还要核",
            "够不够用",
            "review 既有用例",
            "无关的存量缺口",
            "调整内容一类需求",
            "先找现成可参照的既有标准与更优设计",
        ],
        "须有 `baseline-and-compat` 片段（动手前：现状/最佳方案/基线（含清单够不够用）/大动先确认/不另写一套）",
    ],
    [
        "tag::compat[]",
        [
            "tag::compat[]",
            "既有用例不得为迁就改动而改判",
            "新老用例在同一套校验里同时全绿",
            "兼容不了就停下确认",
            "验证须与基线比对",
        ],
        "须有 `compat` 片段（改动后：老用例不得改判/新老同时成立/兼容不了先确认/与基线比对）",
    ],
    [
        "report-tail 的汇报一致性半句",
        [
            "汇报里的数字与结论须与实际取值一致（L1）",
            "先复取值再回答",
            "不得拿既有结论当辩词",
        ],
        "`report-tail` 片段须带同口径半句（数字与实际取值一致 + 被质疑先复取值再答、不得拿既有结论当辩词）——提示词会被未知项目复制执行，公共片段漏了这层则复制出去的那份没有这条（判据本体在 `specs/general/verify.adoc`，此处只须出现且回指）",
    ],
]
kind = "file_groups"
message = "开发流程防线被破坏：{file} 缺失要点 {missing}——{desc}；提示词会被未知项目复制执行，漏了这层则复制出去的那份没有这条"

[[guards.check_dev_flow_guard]]
file = "specs/general/planning.adoc"
subsection = "要改的那一处是否已实际读过"
groups = [
    [
        "改动面按处读（L1，防按记忆覆盖本地内容）",
        [
            "改动面按处读（L1）",
            "先读该文件里要改的那一处",
            "要改的那一处现状",
            "不必读",
            "记忆中的内容",
            "把本地内容覆盖掉",
            "判定标准（任一命中即不合规）",
            "跳过读取",
            "读成",
            "`specs/core/execution.adoc`",
            "`specs/general/self-check.adoc`",
        ],
        "须有「改动面按处读」L1 条且含三要素：核对对象是**要改的那一处现状**（与本次改动无关的部分不必读）、三条判定标准（未读即按记忆写入 → 覆盖本地内容 / 以『可能已改过』为由跳过读取 / 读取位置与要改处对不上）、**边界**（不得被读成『每个文件都要通读』）+ **两处落点的回指**（必加载层 `specs/core/execution.adoc`、自检清单 `specs/general/self-check.adoc`）——缺则本条退化成又一句『动手前先看代码』，实测失效（按记忆写入覆盖本地内容）照样发生；回指缺一处则必加载层不含该要求、自检关口也不会问到它，判据只在用到 `planning.adoc` 时才可见。**那一句里的两个路径是「本条有两处落点、分别在哪」这唯一一件事实，只核它在不在**；两处落点各自的内容由本防线的 `execution.adoc` 组与 `check_self_check_guard` 分别核，本条不设第二份判据",
    ],
    [
        "改动面按处读的对侧（必加载层与自检清单）",
        [
            "`specs/core/execution.adoc`",
            "`specs/general/self-check.adoc`",
        ],
        "本条须在**必加载层与自检清单**各留一个可加载落点（缺则必加载层不含该要求、自检清单也不会问到它，判据只在用到 `planning.adoc` 时才可见）",
    ],
]
kind = "subsection_groups"
message = "{file} 的对应小节缺失要点 {missing}——{desc}"
