[tokens]
ADOC_ROOTS = [
    "",
]
# 『引用坐标』防线的**取值锚点**（规则措辞外置在此，改措辞不动 .py）。
# 引用式坐标：判据本体条目的标题 + 三条判定标准 + 「位置式没有补救」那一句。
REFERENCE_COORD_TOKENS = [
    [
        "引用坐标不得靠位置（L1）",
        [
            "引用坐标不得靠位置（L1",
            "引用式坐标",
            "位置式坐标",
        ],
        "缺则「引用坐标」这一条没有本体：位置式坐标（条号/序号）无人拦、引用式坐标的目标不全时无落点",
    ],
    [
        "引用式坐标按目标全名写",
        [
            "内部引用（本仓库内）一律写",
            "不得只写文件名",
        ],
        "缺则引用式坐标可以只写文件名——同级/跨层解析落到哪一份文件不进判据，正是本轮实测的悬空形态",
    ],
    [
        "位置式坐标一律不得使用",
        [
            "位置式坐标（按行号、条号、序号",
            "一律不得",
        ],
        "缺则「按位置表述」仍是默许形态：增删一条即静默失效，而引用方找不到时无从判断是漏了还是漂了",
    ],
    [
        "位置式不得靠定期核对补救",
        [
            "定期核一遍",
            "必然漂移",
        ],
        "缺则把「按位置引用」当成可维护形态（用定期核对补救），执行者会继续写条号/序号",
    ],
]
# 位置式坐标的**例外范围**：该文件里的「第 N 条 / 序号」是**内容本身的载体**
# （清单表的行号、隐患清单的编号），不是"指向别处"的坐标——这些编号被增删时，
# 表里那一行/那一条跟着动，不构成"引用方找不到目标"（判据见 `source.adoc` 的边界句）。
# 另两处是**本仓库自己引用清单表行号**的地方（历史留痕：指 `guards.adoc` 的防线序号，
# 不构成引用方找不到目标）。**判据本体所在文件不在此列**——正文里写那句禁令本身时，
# "第 N 条"指的就是本节自己的编号，同样属"编号即内容本身的载体"。
POSITIONAL_SCOPE_OK = [
    "README.adoc",
    "library/adoption.adoc",
    "library/sources.adoc",
    "specs-project-maintainer/guards.adoc",
    "specs-project-maintainer/priority.adoc",
    "specs-project-maintainer/spec-lifecycle.adoc",
]
# skill 里指向**兄弟文件**的相对引用前缀（`references/...`、`scripts/...`）。
# 判据：skill 被安装到别的项目里时，实际落到落点的**只有 `SKILL.md`**（配套文件不进落点），
# 故指向兄弟文件的引用在那边必然落空——受支持的写法是**跟着 SKILL.md 就地落盘**。
SKILL_SIBLING_REF_PREFIXES = [
    "references/",
    "scripts/",
]
# 『引用坐标』防线的**判据图形**（正则）：位置式坐标的表述、它的反向读法、以及正当的
# "第 N 步 / 第 N 节 / 第 N 部分"一类（不报）——条文里的正当用法与引用相邻时不构成
# 引用坐标（实测：`prompts/refactor.adoc` 的"仅对第 2 步判定需性能测试的敏感点"）。写在规则数据里是刻意的——图形写在防线函数体里时，
# 把"条号"那一段正则整段删掉仍会全绿（本轮实测：形态判据被抽走而防线不报红）。
REF_COORD_POSITIONAL_RE = '''(?:第\s*\d+(?:\s*[~～-]\s*\d+)?\s*条(?![例款]|文)|第\s*\d+\s*(?:行|号线)(?!\S)|序号(?:为|是)?\s*\d+|(?:最后|末尾|倒数第\s*\d+)\s*(?:一)?\s*(?:条|行))'''
REF_COORD_POSITIONAL_OK_RE = '''(?:位置式(?:坐标)?[^。；\n]{0,20}?(?:可以|得用|才?可)(?:按|用)|按位置引用)'''
RELATIVE_POSITION_OK_RE = '''第\s*\d+\s*(?:步|部分|章|档|级|次)|第\s*\d+\s*(?:节|版)(?!\S)'''
AFTER_REVIEW_ANCHORS = [
    [
        "①机械手段必须先跑且跑全（L1）",
        [
            "机械手段必须先跑、且跑全（L1",
            "全量扫描",
            "报红就地修复",
        ],
        "缺「必须先跑、跑全、报红不留红」时，「改完跑一下」会被读成「跑一条像样的命令就行」",
    ],
    [
        "②干净子 agent 复核不可漏（L1，有了就忽略、没有就加）",
        [
            "干净子 agent 的语义复核不可漏（L1",
            "有了就忽略",
            "没有就加",
            "项目自身规范",
        ],
        "这条正是用户点名的要求：必须 review、干净子 agent 不能漏；「有了就忽略、没有就加」缺任一半，都会要么重复写第二份、要么永远没人补",
    ],
    [
        "③三视角一并回答、一次读取分栏",
        [
            "三视角一并回答、一次读取分栏列（L1",
            "完整性",
            "有效性与认知质量",
            "接纳面",
        ],
        "缺则复核退化成单一视角（只看完整性、漏掉有效性与接纳面）",
    ],
    [
        "④三态台账、不得写散文（L1）",
        [
            "三态台账",
            "未发现问题",
            "悬置",
            "不得合并",
        ],
        "缺「三态分列、不得合并」时，「查不出」与「没做」无法分辨",
    ],
    [
        "⑤子 agent 不可用时的降级留证（L1）",
        [
            "子 agent 不可用时的降级与留证（L1",
            "不得跳过复核",
            "外部来源",
            "标注独立性边界",
        ],
        "缺则环境不支持子 agent 时会把「没复核」写成「已复核」，或据此停任务",
    ],
    [
        "本节的边界（只对规范类改动）",
        [
            "只对规范类改动（B 类）",
            "代码类改动不做三视角",
        ],
        "缺边界则代码类改动被拖进概念性流程（把可控的验证做成不可控的多步流程）",
    ],
]
# 处理器探测次序（先 Ruby、后 Python）。**探测到 Python 版不算通过**：
# 它不支持 `--failure-level`，WARNING 级问题拦不住，属"降级处理器"——
# 本仓库口径：降级处理器不得记作通过，须装齐 `ASCIIDOC_REQUIRED_PROCESSOR`。
ASCIIDOC_PROCESSORS = [
    "asciidoctor",
    "asciidoc",
]
# 必须装齐的处理器（唯一支持 `--failure-level=WARN` 的实现）：缺它即语法段
# 无法拦住 WARNING 级问题——用户口径：**没有环境就要装环境，不得走降级通过**。
ASCIIDOC_REQUIRED_PROCESSOR = "asciidoctor"
CHAIN_CALL_ANCHORS = [
    [
        "链式调用一律换行（L1，长度不是判据）",
        [
            "链式调用一律换行（L1）",
            "每个环节各占一行",
            "长度不是判据",
            "不得**因为「这行放得下」而写成一行",
        ],
        "缺「长度不是判据」时本条会退回通行风格的『按行宽决定』——而用户点名要治的正是「很短就写一行」这个裁量点",
    ],
    [
        "判定标准（同行两处及以上环节即违规）",
        [
            "判定标准（任一命中即违规）",
            "两处及以上",
        ],
        "判定标准是唯一可核对的那句话；缺则「多短算短」回到执行者手里",
    ],
    [
        "例外（L2，单层调用与断言）",
        [
            "例外（L2）",
            "单个不可再分的原子表达式",
            "测试断言",
        ],
        "无例外口径时单层调用（`a.b()`）也会被拆行——那不是收益、只是刷行数",
    ],
    [
        "边界（只改形态、不改语义）",
        [
            "边界（防反用）",
            "只规定换行形态、不改变求值语义与调用顺序",
        ],
        "缺边界则本条会被反用成「把链改成多个中间变量 / 分步 return」，那是另一件事且会改变链式语义（与 P3 邻接的可读性口径冲突）",
    ],
    [
        "依据行与「本集合取值」定性",
        [
            "依据（标准名/编号）",
            "Google Java Style Guide",
            "阿里巴巴 Java 开发手册",
            "是本集合的判据化取值",
        ],
        "不写「严于通行风格（按行宽）」时，读者会以为风格指南就是这么规定的",
    ],
]
CHANGE_REVIEW_ANCHORS = [
    [
        "每次改动后的常规 review（L1）",
        [
            "每次改动后的常规 review（L1）",
            "本次改动的全部产物",
            "按改动性质取值",
        ],
        "缺「每次」与「按性质取值」时，review 只在专门做 review 的任务里发生",
    ],
    [
        "改完即审的固定动作（L1）",
        [
            "改完即审",
            "跑",
            "比",
            "核",
            "留",
        ],
        "缺固定动作（跑机械手段 / 比基线 / 核改动清单 / 留证）时，「审」没有可核对的落点",
    ],
    [
        "规范类改动不得只跑机械手段（L1）",
        [
            "规范类改动不得只跑机械手段（L1",
            "跑绿了",
            "不等于",
        ],
        "缺则「机械全绿」会被当成「复核已做」",
    ],
    [
        "复核者不可用时（L1）",
        [
            "复核者不可用时（L1",
            "不跳过复核",
            "如实标悬置",
        ],
        "缺则复核者不可用会被读成「这次免了」",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "IEEE 1028",
            "ISO 10007",
        ],
        "依据被删时，「每次改动都要评审」的来源无从核对（本集合严于 IEEE 1028 的「有评审发生」）",
    ],
]
DISPATCHER_DETAIL_MARKERS = [
    "判定标准",
    "任一命中",
    "判据见",
    "适用面（L1",
    "例外（L1",
    "边界（L1",
    "本文件不复述",
    "唯一落点",
    "不得降级",
]
FILLER_PLACEHOLDER_WORDS = [
    "此处",
    "本段",
    "待补",
    "待完善",
    "待补充",
    "后续补充",
    "内容同上",
    "详见上文",
    "TODO",
    "略",
]
GEN_EFF_ANCHORS = [
    [
        "先定完成判据（L1）",
        [
            "先定完成判据，再动手（L1）",
            "什么算做完",
            "返工来源",
        ],
        "没有「先定完成判据」这一条，生成效率会退化成「多跑几轮试试」",
    ],
    [
        "一次做对一次做完（L1）",
        [
            "一次做对一次做完（L1）",
            "一次改到位",
            "碎片推进",
            "本轮交付之后是否需要再改同一批文件",
        ],
        "这条给出的正是「剩下的重来从哪来」的根因；缺则「分步推进」重新成为默认",
    ],
    [
        "延后验证、一次到位（L1）",
        [
            "延后验证、一次到位（L1）",
            "攒到一处",
            "重启一次构建",
            "**存在真实依赖**",
        ],
        "缺「延后验证 + 例外口径」时，会把每次小改都重启构建（与执行吞吐的构建输出一次取到同向，但本条管「要不要现在跑」）",
    ],
    [
        "失败一次查根因（L1）",
        [
            "失败一次就查根因，不靠重试撞对（L1）",
            "同一问题上被执行第二遍",
            "新认识",
        ],
        "不写「不得靠重试撞对」，「改一处跑一次」就会被当成正常迭代（实证：轮次间隔占墙钟 65%）",
    ],
    [
        "按需读取、不全量预处理（L1）",
        [
            "按需读取、不全量预处理（L1）",
            "全量预读",
            "低信号",
        ],
        "缺则「先把可能有用的资料全读一遍」重新成为默认，输入膨胀且准确率下降",
    ],
    [
        "批量化同类操作（L2）",
        [
            "批量化同类操作（L2）",
            "批量一次做完",
            "脚本化",
        ],
        "同类操作串行的代价（每轮重付全上下文）不写时，没人会去合并",
    ],
    [
        "任务边界一次说清（L2）",
        [
            "任务边界一次说清（L2）",
            "一次把边界与产物形态说清",
            "先做一版看看",
        ],
        "缺则「先做一版看看」被当成探需求的手段，做完再问必然返工",
    ],
    [
        "依据名代替复述（L2）",
        [
            "依据名代替复述（L2）",
            "第二真源",
        ],
        "「依据名代替复述」与 token 纪律同源，缺则复述照写（既费篇幅又制造第二真源）",
    ],
    [
        "不重做已做完的事（L2）",
        [
            "不重做已做完的事（L2）",
            "再确认一次",
            "再跑一遍看看",
        ],
        "缺则已确认的结论会被重复确认、已跑的校验被重复跑",
    ],
    [
        "收尾一次收敛（L2）",
        [
            "收尾一次收敛（L2）",
            "一次写完",
            "零散补齐",
        ],
        "缺则汇报与留证被拖成多轮零散补齐",
    ],
    [
        "效率不得越过质量（L1，边界）",
        [
            "效率不得越过质量（L1，本节的边界）",
            "不得用于减少",
            "**更少的质量**",
        ],
        "边界条缺位时，本节可被读成「为省一轮可以跳过校验/复核/留证」——那正是用户要求不得出现的形态（不能影响效率和效果）",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO/IEC/IEEE 25010",
            "Anthropic",
            "Agent Skills",
        ],
        "依据被删时，「轮次当成本」的来源无从核对（外部材料讲注意力预算、不给执行侧判据）",
    ],
]
GUARD_CHECK_LIMITS = [
    "运行时事实（本仓库不可见：某次执行的实际行为、平台侧记录、产物正文）",
    "语义判断（判据是否仍成立、内容质量、机制在真实环境会不会发生）",
]
HISTORICAL_FILES = [
    "CHANGELOG.adoc",
]
INFO_DENSITY_ANCHORS = [
    [
        "与精炼性的分工（两把尺子）",
        [
            "与 `specs/general/review.adoc`「精炼性」是两件事、两把尺子",
            "同一描述有几处",
            "单处里有多少句是废话",
            "仍可能通篇是废话",
        ],
        "不分工时，本条会被读成精炼性的重复条目而合并删除；而两者判的是不同对象（跨处的重复面 / 单处的密度）——合并即少一把尺子",
    ],
    [
        "每一句都要有承载（含判定标准）",
        [
            "每一句都要有承载（L2）",
            "判定标准（任一命中即违规）",
            "复述",
            "空话",
            "同义反复",
            "可有可无的铺垫",
        ],
        "缺判定标准时本条自身不可判定（「有没有废话」交回执行者凭感觉），退化成又一句「要注意简洁」",
    ],
    [
        "限定语不得降格为表意不明",
        [
            "陈述句不得降格为表意不明（L2）",
            "不承载信息的限定语",
            "须写出边界",
        ],
        "缺则「为了看起来严谨而写的不承载信息的限定语」无从判定，而这正是学术文体与规范文体里最常见的冗余形态",
    ],
    [
        "与 P3 的边界（不得反用删内容）",
        [
            "边界（防反用，L1）",
            "内容不减少",
            "只有\"这一句没有承载\"才是",
            "违反 P3",
        ],
        "缺这条边界时，本条会被反用成「这一段很长就删短」——直接撞最高关注项 P3（内容不减少），也会把多条判据压成一句口号",
    ],
    [
        "适用面",
        [
            "适用面（L2）",
            "不适用",
        ],
        "缺适用面时会被套到代码与测试断言上（那是 coding.adoc「代码质量」的判据面），造成高频误伤",
    ],
    [
        "存量边界",
        [
            "发现即改、不单独发动全库清理（L2）",
            "`specs/core/execution.adoc`",
        ],
        "缺则会被读成「立刻发动全库瘦身」（一次性大改造）",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "**GB/T 7713.2-2022**",
            "**GB/T 7713.1-2025**",
            "ISO/IEC Directives Part 2",
            "ISO/IEC/IEEE 29148",
        ],
        "依据被删时「学术文体以信息密度衡量冗余」这一来源无从核对，而它正是本条与「精炼性」得以分家的理由。**标准名单独成对加粗**：本仓库实测的写坏形态是 `**GB/T 7713.2-2022（学术论文编写规则**`（标准名与编号错位），按『关键字在不』核会全绿而读者读到的依据是坏的——故核成对加粗的**标准名本身**",
    ],
]
JAVA_SERIAL_ANCHORS = [
    [
        "已实现者须显式声明（L1）",
        [
            "须显式声明 `serialVersionUID`（L1）",
            "**必须显式声明**",
            "父类已实现",
        ],
        "不写「已声明实现者必须显式声明、含父类已实现」时，本条会退化成「建议加上」——而它要治的正是「未显式声明 ⇒ 由类结构推导 ⇒ 结构一变即 InvalidClassException」",
    ],
    [
        "缺省取值 1L 与配置实时读取",
        [
            "取 `1L`",
            "`lombok.config`",
            "**实时取值**",
        ],
        "缺省值不写死时『默认值是多少』回到执行者手里（本条的动因就是用户被警告反复打扰）；不要求实时读取 `lombok.config` 则与既有配置冲突",
    ],
    [
        "`@Serial`（JDK 14+）",
        [
            "`@Serial`",
            "编译期报出",
            "JDK 14",
        ],
        "缺则显式声明照样可能拼错、且拼错要等运行期才发现",
    ],
    [
        "不为未实现者补（L1，防顺手扩大面）",
        [
            "不为未实现 `Serializable` 的类型补（L1）",
            "**不得**为「消警告」",
            "顺手给它加 `implements Serializable`",
        ],
        "这是用户原话里点名的边界（「仅限已经实现了 Serializable 的类型，没实现不自动加 Serializable 实现」）——缺则执行者会为『消警告』把类改成可序列化契约的一部分",
    ],
    [
        "显式声明优先于抑制（L1）",
        [
            "显式声明优先于抑制（L1）",
            "`@SuppressWarnings`",
            "警告与弃用",
        ],
        "缺则「抑制掉警告」会被当成完成——与 `specs/general/coding.adoc`「警告与弃用」的「新代码不得引入警告」相抵",
    ],
    [
        "判定标准与存量口径",
        [
            "判定标准（任一命中即违规）",
            "`specs/core/execution.adoc`",
        ],
        "缺判定标准时本条自身不可判定；缺存量口径则会变成一次性全库改造（与 P3 邻接口径冲突）",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "Java Object Serialization Specification",
            "Java 官方 API 文档",
            "**「默认取 `1L`」是本集合的取值**",
        ],
        "依据被删时「为什么必须显式声明、为什么默认 1L」的来源无从核对；不写「1L 属本集合取值」则会被读成标准的明文要求",
    ],
]
JAVA_TEST_SUFFIXES = [
    "Tests",
    "BootTests",
    "PerfTests",
    "IT",
]
JAVA_VAL_VAR_ANCHORS = [
    [
        "优先 val、可变才 var（L1）",
        [
            "局部变量强制使用 `val`/`var`，且优先 `val`、可变才用 `var`（L1）",
            "必须且一律**使用 `val`/`var`",
            "先取 `val`",
            "例外档、不是并列选项",
        ],
        "缺则「`val`/`var` 随意二选一」重新成立（用户要求的是『优先 val』——不可变优先于可变，不是两者等价）",
    ],
    [
        "判定标准（用 var 却没重新赋值即违规）",
        [
            "判定标准（任一命中即违规）",
            "用 `var` 声明的局部变量此后从未重新赋值",
        ],
        "这是本条唯一能机械核对的那句话——缺则「优先 val」只是口号",
    ],
    [
        "例外（类型推断不清）",
        [
            "例外（L2）",
            "类型推断结果不清晰",
            "以 `null` 字面量初始化",
        ],
        "缺例外口径时复杂泛型/匿名类处会被迫写出不可读的推断写法，反而降低可读性",
    ],
    [
        "未引入 lombok 时按 var 执行 + 字段不得用",
        [
            "未引入 lombok 时",
            "没有 `val`",
            "字段不得使用",
        ],
        "`val` 是 lombok 提供的能力——缺「未引入时按 `var` 兜底」会逼出为 `val` 另引依赖；缺「字段不得使用」则 `val`/`var` 会被误用到字段上",
    ],
    [
        "依据行与「本集合取值」定性",
        [
            "依据（标准名/编号）",
            "Project Lombok 官方文档",
            "Google Java Style Guide",
            "是本集合对 lombok 的判据化取值",
        ],
        "不写「更严、属本集合取值」时，读者会以为风格指南规定必须用 `val`",
    ],
]
LIBRARY_QUOTE_ANCHORS = [
    "Software Reviews and Audits",
    "IEEE Standard for Information Technology--Systems Design--Software Design Descriptions",
    "Software and systems engineering — Software testing — Part 4: Test techniques",
    "Systems and software Quality Requirements and Evaluation (SQuaRE) — Product quality model",
    "ISO/IEC Directives, Part 2 — Principles and rules for the structure and drafting",
    "Ergonomics of human-system interaction — Part 110: Interaction principles",
    "Key words for use in RFCs to Indicate Requirement Levels",
    "Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words",
    "UTF-8, a transformation format of ISO 10646",
    "Date and Time on the Internet: Timestamps",
    "The OAuth 2.0 Authorization Framework",
    "Best Current Practice for OAuth 2.0 Security",
    "The 'Basic' HTTP Authentication Scheme",
    "Digital Identity Guidelines: Authentication and Lifecycle Management",
    "Deserialization of Untrusted Data",
    "Observable Timing Discrepancy",
    "Generation of Error Message Containing Sensitive Information",
    "Active Debug Code",
    "Dependency specification for Python Software Packages",
    "Style Guide for Python Code",
    "Docstring Conventions",
    "**/IT*.java",
    "**/*ITCase.java",
    "**/*TestCase.java",
    "MAJOR version when you make incompatible API changes",
    "In many IETF documents, several words",
    "when they are in all capitals as shown below",
    "官方文本已取回",
    "官方网页已取回",
    "未逐字取回",
]
LIBRARY_SOURCE_MARKERS = [
    "官方文本已取回",
    "官方网页已取回",
    "未逐字取回",
]
LIBRARY_TOPICS = [
    "sources.adoc",
    "adoption.adoc",
    "usage.adoc",
    "throughput.adoc",
    "performance.adoc",
    "quality.adoc",
    "mirrors.adoc",
]
MAINTAINER_LAYER_FILES = [
    "specs-project-maintainer/priority.adoc",
    "specs-project-maintainer/spec-lifecycle.adoc",
    "specs-project-maintainer/verify.adoc",
    "specs-project-maintainer/context.adoc",
    "specs-project-maintainer/guards.adoc",
]
MAVEN_PARALLEL_ANCHORS = [
    [
        "默认启用多线程构建（L2）",
        [
            "默认启用多线程构建（L2）",
            "默认开 `-T`",
            "取值＝当前构建设备的核心数",
            "mvn -T <核心数>",
            "核心数怎么得到是执行动作，不写进规范",
            "不写 `-T` 即默认单线程",
            "取值以命令行参数形态显式传（L1）",
            "必须写在本次构建的命令行上",
            "不采用环境变量一类载体",
            "MAVEN_OPTS",
            "悄悄退回单线程",
        ],
        "缺则『不写 -T 用单线程跑』重新成为默认（多核机器上的构建时长白付）；缺『取值＝核心数、命令形态 `mvn -T <核心数>`』与『核心数怎么得到是执行动作、不写进规范』两句时，规则要么被读成『取一个固定值』、要么被写成一套随环境变的取数流程（判据本体见 specs-project-maintainer/spec-lifecycle.adoc「以对象定义规则、不给具体操作」）",
    ],
    [
        "配置过即以配置为准（L2，防覆盖引用方既有配置）",
        [
            "配置过即以配置为准（L2）",
            "`.mvn/maven.config`",
            "不得覆盖、不得重复追加",
            "项目级配置的落点",
            "命令行的覆盖口径是「同参数取最后出现者」（L1）",
            "命令行**总是在项目配置文件之后**生效",
            "即使命令行传的取值与项目配置的完全相同",
            "生效面（L1）",
            "只作用于本集合自己发起的构建命令",
        ],
        "缺则执行者会为了统一口径去改引用方的构建配置（用户点名要防的一面）。**取值写成『项目级配置的落点＝`.mvn/maven.config`』，不写『优先探测的落点』**——怎么知道配没配过属执行动作，口径见 `specs-project-maintainer/spec-lifecycle.adoc`「以对象定义规则、不给具体操作」",
    ],
    [
        "并行度只到模块粒度（L1）",
        [
            "并行度只到模块粒度（L1）",
            "模块间",
            "同一模块禁止并行构建",
        ],
        "缺则『并行只作用于模块之间』被读成『模块内也并发』，与同一模块禁止并行构建冲突",
    ],
    [
        "测试并行与并行构建默认为两件事（L2）",
        [
            "测试并行与并行构建默认为两件事（L2）",
            "forkCount",
            "reuseForks",
            "默认只启用构建并行，不因本条去开测试并行",
        ],
        "缺则要么把 -T 当成测试也并行了、要么顺手把测试并行一起开（改变既有行为、高频误伤）",
    ],
    [
        "构建产物不得因并行而退化（L2）",
        [
            "构建产物不得因并行而退化（L2）",
            "收窄并行度",
            "不得整体退回单线程",
        ],
        "缺则并行下的偶发失败会被用『整体退回单线程』或『重试撞过』处置（放弃收益且掩盖根因）",
    ],
]
MAVEN_PARALLEL_PROMPT_ANCHORS = [
    "配过就一律沿用、不覆盖、不重复追加",
    "**项目已配过并行度时以项目配置为准**",
    "没配过",
    "mvn -T <核心数>",
    "核心数怎么得到是执行动作",
    "不得写死 `-T 1C`",
    "取值必须以命令行参数形态显式传（L1）",
    "不得改用 `MAVEN_OPTS` 一类环境变量载体",
    "悄悄退回单线程且不报错",
    "并行到模块粒度为止",
    "只对 Maven 多模块构建生效",
]
MAVEN_PARALLEL_SOURCE_ANCHORS = [
    "<a name='T'>T</a>,--<a name='threads'>threads</a>",
    "<a name='T'>T</a>, --<a name='threads'>threads</a>",
    "Thread count, for instance 4 (int) or 2C/2.5C (int/float) where C is core multiplied",
    "defining `.mvn/maven.config` file",
    "Default : 1",
    "By default, Surefire does not execute tests in parallel",
]
PERF_LIBRARY_ANCHORS = [
    "== 当初要解决的失效（本项目实证）",
    "== 外部材料：JMH 官方文档（OpenJDK，微基准的行业事实标准）",
    "== 同义性差异与覆盖点（本集合自己承认的）",
    "https://github.com/openjdk/jmh",
    "ISO/IEC/IEEE 25010",
    "ISO/IEC/IEEE 29119-4",
    "未逐字取回",
    "非逐字摘录",
    "本集合自己的判据化取舍",
]
PERF_SECTION_ANCHORS = [
    [
        "承载与触发（何时需要性能测试）",
        [
            "按性能敏感度判定",
            "多方案选型需要数据佐证",
            "不得一律加、也不得漏掉真敏感点",
        ],
        "何时需要性能测试的判据被删后，执行者会一律加（噪声用例）或一律不加（漏掉真敏感点）",
    ],
    [
        "独立与对比测试",
        [
            "独立性（L1）",
            "类别至少 3 类",
            "含当前项目使用的那一类作基准",
        ],
        "对比测试缺了基准那一类，则\"要不要换\"无从判起（只剩候选方案之间的相对比较）",
    ],
    [
        "排除初始化干扰与测量口径",
        [
            "排除初始化干扰（L1）",
            "测量口径须可核对（L1）",
            "预热轮次、测量轮次",
            "运行环境",
        ],
        "测量口径不写明时，成绩不可核对、不可复现（\"只报一个孤立数字\"重新成为默许形态）",
    ],
    [
        "离散度与样本量",
        [
            "离散度与样本量（L1）",
            "至少 3 次有效采样",
            "无显著差异",
        ],
        "缺离散度/样本量时，单次采样会被当成结论、噪声会被当成胜出",
    ],
    [
        "公平比较与计时区间",
        [
            "公平比较（L1）",
            "同一进程",
            "计时区间界定（L1）",
            "消费结果",
        ],
        "公平比较与计时区间是微基准的两个经典失效（一组热一组冷、结果被 JIT 消除）",
    ],
    [
        "记录落点与必留证据",
        [
            "记录（成绩与依据落在哪、记到什么程度）",
            "落点（L1）",
            "每次运行的必留证据（L1",
            "未选方案为什么不选",
        ],
        "落点与必留证据被删后，\"记到什么程度\"回到靠发挥：要么只留一句结论、要么把原始日志灌进文档",
    ],
    [
        "成绩取数与方案组合",
        [
            "成绩怎么取数（L1",
            "终值（均值或中位数）+ 离散度",
            "方案组合与取舍须可核对",
            "与基线之比",
        ],
        "取数规则与方案组合是\"记录各方案成绩\"的可判定形态；缺则成绩表只剩孤立的均值。**只核取值形态**——记录载体与表格列（`方案编号 | 组合 | …`）属执行物，规则正文只到\"该给出哪些取值\"为止（口径见 specs-project-maintainer/spec-lifecycle.adoc「以对象定义规则、不给具体操作」）",
    ],
    [
        "优化过程与只记结果",
        [
            "优化过程须可回溯（L2",
            "只记结果不记过程（L1）",
            "不保留的改动同样须留痕",
        ],
        "优化过程（含失败尝试）与\"不记过程\"是一对：前者留住方向与教训、后者拦住把过程灌进文档。**只核取值形态**——日志列属执行物，不写进规则正文",
    ],
    [
        "迭代、归因与方向",
        [
            "闭环迭代（L1）",
            "收益判定用实测、不用估计",
            "瓶颈归因与方向（L1）",
            "优化方向与思路（L1）",
            "结果分析须给出原因与对策（L1）",
            "给出对应的解决方案",
        ],
        "迭代闭环与归因是用户要求的核心（\"不断反复进行瓶颈优化\"\"优化的方向和思路也要记录，避免踩坑\"）",
    ],
    [
        "结论落点、时效与不变行为",
        [
            "结论的落点（L1）",
            "结论的时效（L2）",
            "优化不得改变行为（L1）",
            "结果反哺基线（L2）",
        ],
        "结论只留在性能文档 = 落地后无人知道为何这么选；优化改行为 = 把重构做成了功能变更",
    ],
]
PROMPT_SURFACE_LIBRARY_FILES = [
    "library/README.adoc",
    "library/usage.adoc",
    "library/sources.adoc",
]
QUALITY_ANCHORS = [
    [
        "适用面与存量边界",
        [
            "**适用面（先读）**",
            "**写任何新代码、改任何既有代码**时适用",
            "**新增的那一部分永远按本节判**",
        ],
        "适用面不写明时，执行者会把本条当作「存量也要全库改造」或「只管新文件」两种极端",
    ],
    [
        "不得引入坏味道（L1）",
        [
            "新代码不得引入坏味道（L1）",
            "重复代码",
            "过长函数",
            "依恋情结",
            "注释代替澄清",
        ],
        "坏味道清单被压成「注意代码质量」后不可判定（判据退化成口号）",
    ],
    [
        "职责单一与嵌套上限（L1）",
        [
            "职责单一、结构清晰（L1）",
            "三层以内",
            "一句话说清",
        ],
        "没有嵌套上限与「一句话说清职责」这两个可核对判据时，「结构清晰」无从判定",
    ],
    [
        "命名表意（L1）",
        [
            "命名表意、不用缩写（L1）",
            "同一概念在项目中只有一个叫法",
            "拼音",
        ],
        "命名条缺「全文一致」与「禁缩写/拼音/数字后缀」时，只剩「命名要清晰」这类不可判定的话",
    ],
    [
        "可读性优先（L1）",
        [
            "可读性优先（L1）",
            "魔法值",
            "同一表达式不重复求值",
        ],
        "可读性条被删后，「短写法」重新压过可读性（与表达式与调用写法同源、此处只重申）",
    ],
    [
        "失败与边界显式处理（L1）",
        [
            "显式处理失败与边界（L1）",
            "不得吞异常",
            "边界条件必须显式处理",
            # 条件例外（用户口径）：纯取值转换的查找式 API 允许空 `catch`。
            # 锚点取**判据本体**（准入条件 + 判定标准），不是轴名——只核「有条件例外」四个字时，
            # 例外被写成一句"允许空 catch"（准入条件与反面清单全丢）照样全绿，
            # 而那正是本例外最危险的失效形态（例外一宽，L1 的其余部分随之失效）。
            "条件例外（L2",
            "异常语义单一",
            "三条同时成立才算命中",
            "可探测的非法输入",
            "仍是空 `catch`",
        ],
        "不写「不得吞异常 / 边界须显式处理」时，新代码会把失败与边界留给运行期；而条件例外缺了「异常语义单一 / 三条同时成立 / 不得绕过可探测的非法输入」这几条判据时，「允许空 catch」会被读成通用豁免（把真失败、可探测的非法输入一并吞掉）",
    ],
    [
        "无资源泄漏（L1）",
        [
            "无资源泄漏（L1）",
            "确定性释放",
            "成对释放",
        ],
        "资源释放条缺「确定性释放机制 / 成对释放」时会被读成「靠 GC 也行」",
    ],
    [
        "无并发隐患（L1）",
        [
            "无并发隐患（L1）",
            "共享可变状态",
            "锁范围与顺序",
        ],
        "并发条缺「共享可变状态须有明确同步策略」时，「没写同步」不会被判错",
    ],
    [
        "性能不写退化写法（L2）",
        [
            "性能不写退化写法（L2）",
            "循环内",
            "N+1",
        ],
        "可预见的性能退化（循环内 IO/查询、循环内拼串、N+1）不点名时，新代码照写",
    ],
    [
        "测试与文档跟得上（L1）",
        [
            "测试与文档跟得上（L1）",
            "新功能",
            "文档注释",
        ],
        "新功能必须配测试、行为改动须同步文档这两条不写，质量下限会漏掉「改动面」",
    ],
    [
        "交付前质量自检（L1）",
        [
            "交付前质量自检（L1）",
            "逐条自查",
            "能过机械判据是下限",
        ],
        "没有「交付前逐条自查、机械判据只是下限」这一条，质量要求会被当成「编译过了就合格」",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO/IEC 25010",
            "ISO/IEC/IEEE 12207",
            "Martin Fowler",
            "Clean Code",
            "SEI CERT",
        ],
        "依据被删到只剩名称或整段消失时，读者无法核对「质量下限」的依据是否仍成立",
    ],
]
# 『review 的默认检查面（只查问题，不动存量）』防线锚点（用户点名，Issue #203）：
# review 默认只查代码/文档/用例三类问题；取向性要求对存量默认不查不报、随动迁移；
# 唯一例外是用户主动声明（声明是例外而非默认、仅对当次生效）。
# 『review 时的干净子 agent 复核（强制）』防线锚点（用户点名，Issue #219）：
# review 须由与执行者相同的 Agent 在干净上下文里承担；通道须先实测、后派发；
# 派发优先一次性、不带工具、可超时；通道不可用时降级为本人串行并如实标独立性边界。
CLEAN_SUBAGENT_REVIEW_ANCHORS = [
    [
        "强制开干净子 agent（L1）",
        [
            "强制开干净子 agent（L1",
            "与执行者相同的 Agent",
            "上下文干净",
            "不得复用前序对话里的结论",
            "不得以自己那次执行的上下文充当复核的上下文",
        ],
        "缺「强制 + 上下文干净 + 改动方不得自任复核上下文」时，本条会被读成一句倡议，「沿用本次会话结论当作复核」照样发生（用户原话点名的那件事）",
    ],
    [
        "通道须先实测、后派发（L1）",
        [
            "通道须先实测、后派发（L1",
            "取不到按不成立处理",
            "不承载本次会话的上下文",
            "钉定",
        ],
        "缺实测判据时，「能不能拿到干净子 agent」会退化成自评（本项目已有实证：未实测却记成已派发）；缺对象钉定则该次调用核的不是本次改动",
    ],
    [
        "通道的可执行性与原子性判据",
        [
            "该入口的调用实际返回结果",
            "只带本次钉定的对象与其判据",
        ],
        "只写「须实测」仍不可判定：实测到什么算成立（调用真返回、不是知道它存在）与原子性判据都要写死，否则「实测过」无取值",
    ],
    [
        "派发形式与硬超时（L1）",
        [
            "一次性、边界明确、可超时",
            "不带工具",
            "换一次干净上下文",
            "硬超时（L1",
            "放弃 + 如实标悬置",
        ],
        "缺一次性/不带工具/换上下文时，复核者带上一次或前序上下文即失去独立性；缺硬超时则派发会无限挂起",
    ],
    [
        "不得换外部来源与降级路径（L1）",
        [
            "不得换外部来源",
            "降级路径（L1",
            "由执行者本人串行承担",
            "标注独立性边界",
            "如实标悬置",
            "不得混写成",
        ],
        "缺降级路径时，通道不可用会被读成「这次免了」；缺「不得换外部来源」时会把复核派给不可核对的跨来源执行者（本项目实证：派发即失败 / 1 h+ 未回传）",
    ],
    [
        "留证形态（三态台账、不重列取值）",
        [
            "三态各占一栏、不得合并",
            "实际读到的上下文范围",
            "specs/general/verify.adoc",
        ],
        "既钉「三态分列不得合并」，也钉「实际读到的上下文范围」这句取值（它由平台层要求），真源指向 verify.adoc、不在本处重抄逐态取值",
    ],
    [
        "对引用方的口径与生效面",
        [
            "对引用方的口径（L1）",
            "不指定实现",
            "以其自身规范为准",
            "本条生效之后",
        ],
        "缺接纳面口径时，一条公共要求会被读成「引用方必须用某产品」或「既有的做法一律判违规」（静默推翻引用方既有约定、且无迁移边界）",
    ],
    [
        "依据与落点",
        [
            "依据：",
            "IEEE 1028",
            "specs/general/collab.adoc",
            "specs/general/self-check.adoc",
        ],
        "依据被删时「为何必须独立复核」无从核对；缺落点则本条与协作/自检两处断链，执行者看不到能力自评与硬超时的归属",
    ],
]
DEFAULT_REVIEW_SCOPE_SECTION = "review 的默认检查面（只查问题，不动存量）"
DEFAULT_REVIEW_SCOPE_ANCHORS = [
    [
        "默认检查面：只查三类问题（L1）",
        [
            "默认检查面（L1）",
            "代码问题",
            "文档问题",
            "用例问题",
            "不查、不报、不要求整改",
        ],
        "缺「三类问题」清单或缺「存量默认不查不报」时，review 会退回全量符合性审计、把取向性要求的存量缺失报成问题（用户点名的口径被抽空）",
    ],
    [
        "存量随动迁移（L1）",
        [
            "存量不动、随动迁移（L1）",
            "`specs/core/execution.adoc`",
            "review 不得据此提出问题",
        ],
        "缺存量处置时，存量不合取向性规范会被当成违规、甚至发动全库改造（与「规范变更的存量处理」相抵）",
    ],
    [
        "唯一例外是主动声明（L1）",
        [
            "唯一例外是主动声明（L1）",
            "声明是例外而非默认",
            "仅对当次生效",
            "不得泛化",
        ],
        "缺例外条或缺「仅对当次生效、不得泛化」时，会出现要么永远不查（例外的口子被读死）、要么一次声明被泛化成默认检查面（反向失效）",
    ],
]
REFINEMENT_ANCHORS = [
    [
        "重复面是必查项（L1）",
        [
            "重复面是必查项（L1）",
            "没发现重复",
            "没查过重复",
        ],
        "缺「必查项 + 与『没查过』的分界」时，精炼性退回成一句口号：不查也算做到了",
    ],
    [
        "判定标准（逐条可核对）",
        [
            "判定标准（任一命中即为重复描述）",
            "以完整表述出现",
            "改一处要记得改 N 处",
        ],
        "缺判定标准则本条自身也不可判定（「什么算重复」交回执行者凭感觉）",
    ],
    [
        "收敛形态（L1）",
        [
            "收敛形态（L1）",
            "一处完整定义",
            "其余位置只留",
        ],
        "缺收敛形态时「发现重复」没有规定的处置方式，容易滑向「删掉了事」",
    ],
    [
        "与 P3 的边界（L1）",
        [
            "不得以去重换缺失（L1",
            "须留下可达的引用",
            "一律保留",
        ],
        "缺这条边界时，去重会以「删掉重复」的名义删掉唯一那份内容（与内容不减少的底线冲突）",
    ],
    [
        "两类形态不得被当成重复收敛",
        [
            "不得被当成重复收敛",
            "最高关注项与其引用",
            "各自的承接",
        ],
        "缺则会把「刻意强调的引用」与「分层承接」误当重复合并掉（本仓库已实证过的误删形态）",
    ],
    [
        "篇幅与重复是两件事",
        [
            "是两件事",
            "不是字多字少",
        ],
        "缺则精炼性会被读成「写短点」，长的条目被无理由砍内容",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO 10007",
            "ISO/IEC/IEEE 29148",
        ],
        "依据被删时，「同一要求只应有唯一表述落点」的来源无从核对",
    ],
]
REGISTRY_ANCHORS = [
    [
        "次序：就近/平台内 → 所在地区主流公共源 → 邻近境外源 → 更远境外源",
        [
            "就近/平台内已优化的源",
            "主流公共源",
            "地理上邻近的其他境外源",
            "更远的境外源站",
        ],
        "次序被删后，'换源'重新退化成凭感觉挑一个",
    ],
    [
        "逐级降级、不得跳级",
        [
            "只有当上一级",
            "未配置过任何源时",
        ],
        "缺这条会把'优先次序'读成'必须用某一家'，或反过来直接跳到最远一级",
    ],
    [
        "先实测可用再启用、一次配置到位",
        [
            "候选源须**已实测可用**",
            "一次配置到位",
            "不得**在每一轮里反复重试",
        ],
        "缺这条会'边构建边撞'或每轮重试同一个源（正是用户要求'先测试避免浪费时间'的落点）。**只核『实测可用才启用』这一取值**——怎么实测（真实请求/取某坐标）属执行动作，不写进正文（口径见 `specs-project-maintainer/spec-lifecycle.adoc`「以对象定义规则、不给具体操作」）",
    ],
    [
        "不覆盖引用方既有配置、已配置过即沿用",
        [
            "已配置过**包源时**一律沿用",
            "未配置过任何源\"时才动手配置",
            "**推荐源不是强制源**",
        ],
        "缺这条会把引用方既有私服/镜像配置重配一遍，或把推荐读成必须用某一家",
    ],
    [
        "推荐非强制、用户声明优先",
        [
            "**推荐源不是强制源**",
            "用户/项目声明的源永远优先",
        ],
        "缺这条会把推荐写成强制，与'用不了就换其他源'的初衷相反",
    ],
    [
        "实测取值与条件须可复核",
        [
            "结果须可复核",
            "取值与条件",
        ],
        "缺这条结论会只剩一句'已换国内源'，后人无法复核",
    ],
]
RENAME_SPLIT_ITEM_LABELS = [
    "**一句话规则**",
    "**适用对象与判定标准（L1）**",
    "**分量大小不是理由（L1）**",
    "**提交顺序（L1）**",
    "**无损拆分（L1，判定标准）**",
    "**两个提交都要进本次交付（L1）**",
    "**「压缩提交」的请求不覆盖本条（L1）**",
    "**例外（只有一条，L1）**",
    "**可执行抓手（引用方项目自检）**",
]
TOKEN_ANCHORS = [
    [
        "两个概念的区分（不是一回事）",
        [
            "**不是一回事**",
            "提高 token 利用率",
            "节省 token",
            "**手段大幅重叠**",
        ],
        "缺这条就直接回答不了用户的问题（「是一回事吗」），且会把「省 token」当唯一目标、顺手牺牲完整性",
    ],
    [
        "利用率判据：输入须被用到（L1）",
        [
            "利用率判据：输入须",
            "**用到了**",
            "答不出用途",
            "无效输入",
        ],
        "没有「每份输入都要说出用在哪里」这个可核对判据，「利用率」只剩口号",
    ],
    [
        "约束放外部、不进上下文（L1）",
        [
            "约束放在外部、不进上下文（L1）",
            "落成文件",
            "对话里每次请求都要重发",
        ],
        "缺则规则/清单/参数只留在对话里（每次请求重发），利用率永远上不去",
    ],
    [
        "少复述、多引用（L1）",
        [
            "少复述、多引用（L1）",
            "复述",
            "依据名",
        ],
        "缺则模型把已知内容抄一遍当产出（输入很大、有效产出很小）",
    ],
    [
        "不重复读、不重复贴（L1）",
        [
            "不重复读、不重复贴（L1）",
            "只读一次",
            "只贴一次",
        ],
        "缺则同一份文件被反复读入，纯浪费",
    ],
    [
        "只记结论与取值、不带原始日志（L1）",
        [
            "只记结论与取值、不带原始日志（L1）",
            "原始日志",
            "取值 + 来源",
        ],
        "缺则汇报里灌日志（既是 token 也是注意力浪费；与性能测试「只记结果不记过程」同向）",
    ],
    [
        "三件事不得让步（L1，边界）",
        [
            "三件事不得为省 token 让步（L1，本条的边界）",
            "**功能完整性**",
            "**代码质量**",
            "**验证完整**",
        ],
        "边界条是本条存在的前提（用户明确「会影响效率和效果就不加」）——缺位即等于允许以省 token 为名缩功能、降质量、漏校验",
    ],
    [
        "成本须可说明（L2）",
        [
            "成本须可说明、不得以",
            "说不出来即应砍掉",
        ],
        "缺则「不贵」这类含糊说法可以带过（与运行契约的成本维度同源）",
    ],
    [
        "不设必须量化 token 的要求",
        [
            "不设",
            "必须量化 token",
            "输入有没有被用上",
        ],
        "量化要求被塞进来时既增加开销、又不可核对（本集合的取舍须写明）",
    ],
    [
        "依据行（标准名/编号）",
        [
            "依据（标准名/编号）",
            "ISO/IEC/IEEE 25010",
            "ISO/IEC Directives Part 2",
        ],
        "依据被删时，「引用不复述」与「性能效率可度量」的来源无从核对",
    ],
]
_LIBRARY_ADOPTION_ANCHORS = [
    "== 同义性差异与覆盖点（本集合自己承认的）",
    "本文件是依据图书馆的主题之一",
    "**借用其原则**",
    "**未**规定\"配置类中任何逻辑一律禁止\"",
    "**未**规定此优先级顺序",
    "**均未**规定\"某一类执行者绝对不得合并\"",
    "是本项目自身的组织约定",
]
_LIBRARY_LOCATING_ANCHORS = [
    "== 为什么还要有\"定位协议\"（馆可以无限大）",
    "== 依据的定位协议（入口 + 索引 + 单点取值）",
    "**第一步：入口 = 常驻层里的固定地址（不要自己拼地址）**",
    "**版本固化（可选加固，不得写成前置）**",
    "**分段取值（馆特别大时怎么只取一段）**",
    "**形态约束（L1）**",
    "**文件名协议（短、无语义、不承担定位）**",
    "**作者侧**",
    "**取用侧**是**游客**",
    "**不解析页面结构**",
]
_LIBRARY_LOCATING_SOURCE_MARKERS = [
    "== 依据的定位与取值（git / RFC 9110）",
    "In its first form, the command provides the content or the type of an object in the repository.",
    "names the **blob or tree** at the given path",
    "The 206 (Partial Content) status code indicates that the server is",
    "**未实测**",
]
_LIBRARY_LOCATING_USAGE_ANCHORS = [
    "== 四、引用方怎么准确定位依据（不全量下载）",
    "**主键是内容、不是路径**",
    "**协议不承载的东西（形态约束，L1）**",
    "**终止条件（L1，同上）**",
    "**取用侧是游客**",
    "**只有 https、没有仓库、没有 git**",
    "**文件名不承担定位（短、无语义，L1）**",
]
_LIBRARY_USAGE_ANCHORS = [
    "== 一、什么时候该把依据写进图书馆（触发特征）",
    "== 二、依据与规则怎么关联（关联协议）",
    "=== 反查解析算法（只取一份，不遍历）",
    "== 三、默认引用面与非引用面（外部项目怎么处理）",
    "判据是问句，不是印象",
    "以下情形**不写**",
    "**入库必写项**",
    "**终止条件（L1）**",
    "**只取一段（馆特别大时）**",
]
_LIB_ROOT_FILES = [
    "AGENTS.adoc",
    "AGENTS_COMMON.adoc",
    "AGENTS.md",
    "PUBLIC.adoc",
    "README.adoc",
    "PROMPTS.adoc",
    "CHANGELOG.adoc",
    "check_specs.py",
    "check_effective.py",
    "clean_tmp.py",
    "_common.txt",
    "review.adoc",
    "refactor.adoc",
]
_LOCATING_COMMIT_PREREQ_EXEMPT = [
    "不得",
    "不要",
    "不能",
    "走不通",
    "取不到",
    "失效",
    "误区",
    "错误",
    "反例",
    "本站失效",
]
_REF_SCOPE_BAD_EXEMPT = [
    "不是",
    "并非",
    "≠",
    "错误",
    "不得写",
    "纠正",
    "区分",
]

# ---- 『NPC 禁合并·动作侧』（check_merge_state_guard）的措辞表 ----
# 与本文件其余段一样：**改措辞只改这里、不动 .py**。这道防线核的是"合并动作真没做"
# （提交说明 / 源分支 HEAD / 分支历史），与文本侧要点齐备无关。
[tokens.merge_state_guard]
# 提交说明里出现这些即视为"合并动作的痕迹"（命中即报红，除非同一行出现放行标记）
merge_action_markers = [
    "merge-pull",
    "merge --no-ff",
    "squash 合并",
    "已合并",
    "已合入",
]
# 放行标记：报错文案、实证记录、失效复述里都会出现上面那些词——"记录这件禁令的话"
# 不得被读成"执行了合并"。**注意这条边界本身也会被误伤**：只认否定词还不够，
# 故把"描述一件已发生的失效"的叙述标记也列进来（实测：本条自己的提交说明写过
# "把原话读成合并 PR、直接调平台合并并回已合并 ✅"，那是**记录**而不是**执行**）。
allow_markers = [
    "拒绝",
    "不得",
    "严禁",
    "禁止",
    "不予执行",
    "不做",
    "一律不",
    "实证",
    "失效",
    "曾",
    "误读",
    "读成",
    "记录",
    "反例",
    "复述",
    "说明",
]
# 『已被证否的旧口径不得写回』防线：把**实测证否**的口径的特征措辞钉住。
# 为什么需要它：口径被证否后改掉的是**方向**，而"旧的错法长什么样"只留在改动记录里，
# 下一个人很容易照旧句子复述回来（本仓库实证：`def test_` 的旧计数口径在改掉之后仍在
# 多轮沿革里成串出现，读者复述一遍就把错口径带回注释）。这里逐条核**措辞形态**；
# "这条口径今天还成不成立"属语义判断，交人/子 agent 复核。
[[guards.check_stale_wording_guard]]
kind = "file_forbidden"
file = "script/check_specs.py"
forbidden = [
    "里的 `def test_` 个数",
    "不是源码里 `def test_` 的个数",
    "写成\"只数 check_specs_test.py\"",
]
message = "script/check_specs.py 写回了**已被现场实测证否**的旧口径措辞（见 script/specs-rules/_tokens.toml 的 check_stale_wording_guard）：用例数的口径既不是「源码里写了几个 `def test_`」、也不是「只数某一个测试文件」，而是「`script/` 下全部 `*_test.py` 里真正会被 `unittest` 收集」的用例数——定义与唯一实现处见 `_test_cases`，本处只回指、不复述"
