[[guards.check_data_dictionary_guard]]
file = "specs/general/terminology.adoc"
groups = [
    [
        "只写名称",
        [
            "只写名称",
            "不得",
            "复述",
        ],
        "引用形态（L1）：引用处**只写名称**、必要时给一行指向定义的引用、**不得复述定义**——缺则同一名称出现 N 份会各自漂移的第二真源（名称是检索键、定义只有一处）",
    ],
    [
        "每档只一处",
        [
            "每档只一处",
            "唯一落点",
            "作用域决定落点",
        ],
        "归档口径（L1）：定义**按作用域**落进**唯一**落点、每档只一处——缺则同一档出现第二个落点（同一作用域被拆到多处，与防膨胀的初衷相反）",
    ],
    [
        "写在本文开头",
        [
            "写在本文开头",
        ],
        "**当前文档档的落点须写明是「该文档开头」**（用户点名要求）——缺则该档要么无处可归、要么被写成「文档末尾/另开一处」，读者进文件后仍拿不到解码表",
    ],
    [
        "抽取",
        [
            "抽取",
            "仍成立",
            "判据",
        ],
        "分档判据须**可核对**（拿它去核对：抽掉项目专有名词后该定义是否仍成立等）——缺则「算哪一档」留在执行者手里，任何名称都能被顺手放进上游落点",
    ],
    [
        "一行一个名称",
        [
            "一行一个名称",
            "独占一段",
        ],
        "**一行一个名称**（用户点名要求）：本文件的字典与任何字典条目须**每个名称独占一段**、不得多个名称挤在同一行/同一段——缺则增删与引用一个名称都要动整段、diff 一改一串，读者也无法按名称逐行核对（名称是检索键，键得能单独被看到）",
    ],
    [
        "ISO 1087",
        [
            "ISO 1087",
            "定义",
            "使用说明",
        ],
        "条目形态（L2）：照 ISO 1087 的元素次序写，且**定义与使用说明分列**（SKOS 的 `skos:definition` 与 `skos:scopeNote` 是两个属性）——缺则「什么时候用」混进「这是什么」，定义随用法漂移",
    ],
    [
        "反膨胀",
        [
            "反膨胀",
            "判定标准",
        ],
        "反膨胀须有 L1 与**逐条可核对的判定标准**（上游落点收录了下游名称 / 同一档出现第二个落点 / 上游落点因收录下游名称而单独显著变大）——只写「要防膨胀」即属口号，拦不住「反正顺手」",
    ],
    [
        "每次",
        [
            "每次",
            "新增",
            "调整内容时都须判定",
            "同步",
        ],
        "**生效时机（L1）：每次新增与调整内容时都须走一遍该判定**（用户原话「每次新增、调整内容时都应该生效」），且须给出三条判定标准（引入新名称却无定义 / 新名称未归档 / 调整后定义与引用不一致）——缺则本条只在「新建字典」那一次生效，之后照旧各写一遍",
    ],
    [
        "图书馆",
        [
            "图书馆",
            "不在默认引用面内",
        ],
        "图书馆等「不在默认引用面内」的落点须按其自身规则走（依据名 + 入口主题登记表、短名文件不承担定位、不复制规则本体、公共内容不给可点开的路径）——缺则依据被当名称收进规则正文、或规则被复制进图书馆（第二真源）",
    ],
]
kind = "file_groups"
message = "数据字典防线被破坏：{file} 缺失要点 {missing}——{desc}"
