# 编辑器约定（取值与判据见 specs/general/encoding.adoc「跨平台检出」）：
# 仓库内容以 UTF-8 + LF 为基准；Windows 批处理与 PowerShell 用 CRLF。
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true

[*.{bat,cmd,ps1}]
end_of_line = crlf
