= 图书馆（本仓库私有的依据库）

本目录是本项目的**图书馆**：集中存放本项目里**需要找依据**的东西——规范条目与项目决策背后的**依据**：**为什么这样要求、来源是哪一条、逐字原文是什么、当初要解决的失效长什么样**。

== 为什么要有它

规范正文是"怎么走"的形态（规则 + 判定标准 + 依据名）——这是**刻意**的（见 `specs/core/execution.adoc`「分级与最高关注项」、link:../specs/general/verify.adoc[]「验证总纲」）：执行时多看"为什么"只会占注意力。

但**依据不能只存在名称**：一条规则被执行久了，就会退化成"只记得是这么做的"——提出时的原因不再有人知道（可能原文里写过，也可能只是散在讨论里），于是无法判断它**还成不成立**、无法在争议中据以取舍、无法据以**举一反三**（link:../specs/general/verify.adoc[]「验证总纲」之「防"慢慢脱离初衷"」要求的正是"当初要解决的失效现在还在不在"）。它和 link:../specs/general/source.adoc[] 是同一件事的两端：那份管**引用不许编**，本目录给**依据一个能被检索、被逐字核对的落点**。

== 放什么、不放什么

* **放**：本项目里**任何需要依据的东西**——能**逐字对照**的外部标准原文（引文）、业界权威实践与真实事故的公开陈述、当初的**决策记载**（为什么这样要求、为什么不做别的方案）、**反例**（失效长什么样、实证是什么）、以及技术选型与组织约定的依据。**不限于规范依据**：规范、提示词、脚本、CI 乃至日常决策的依据都可入库。
* **不放**：规则本体（规则在 `specs/` 各文件，图书馆只补依据、不复制规则）、没有出处的观点与推测、**只把讨论过程落盘的会议记录**（link:../specs/general/doc.adoc[]「注释与文档」：空话套话、复述既有结论、为对称凑数者一律不收）。
* **依据找不全时**：按 link:../specs/general/source.adoc[]「无法确证时的正确做法」**如实标"未确证"并写明已查证方式**，不得用虚构内容填补。

== 放哪、怎么用、何时加载

* **位置**：本目录在**仓库根**、与规范入口 `AGENTS_COMMON.adoc` 平级，**属本仓库私有内容、不随规范分发给引用方**（登记与维护说明见项目规范入口 link:../AGENTS.adoc[]）。**为什么不放 `specs/` 下**：`specs/**` 是随规范分发给任意项目的**公共内容**，其中一个字节都会被引用方取到；图书馆天然包含**只对本项目成立**的内容（当初的失效、取舍、实证、选型理由），放进公共内容要么逼着全部改写成"对谁都不犯错"的空泛表述，要么把本站的处置口径（可关闭、不新增要求、冲突次序）反复声明一遍——**项目自己的依据库就是项目自己的内容**，故落在根目录、按需加载。
* **怎么用**：规范正文里的"依据（标准名/编号）"是入口——按其标准名/编号检索本目录；找不到对应主题时**按需补一篇**（判据见 link:../specs/general/source.adoc[]「内部引用」「外部引用」）。也可以**先读本文件**（主题登记表）再取对应主题文件。
* **何时加载（按需懒加载，非必加载）**：要**思考或决策**、出现**争议或取舍**、要**复盘**、要**核对某条依据今天是否仍成立**时才加载——具体触发特征登记在 link:../AGENTS.adoc[]。日常执行按规范正文走即可，**不加载本目录不会有任何功能缺失**。
* **每次取一份即可**：每个主题文件自足（只引用本项目内可加载到的落点），入口登记全部主题，不存在"必须整目录读"的情形。

== 主题登记

[cols="1,3", options="header"]
|===
| 主题 | 覆盖的依据

| link:sources.adoc[] | **外部标准与权威材料的原文摘录**：IEEE 1028、IEEE 1016、ISO/IEC/IEEE 29119-4、ISO/IEC/IEEE 25010、ISO/IEC/IEEE 29148、ISO/IEC Directives Part 2、ISO 10007、ISO 9001、ISO 9241-110、RFC 2119、RFC 8174、RFC 3629、RFC 3339、RFC 6749、RFC 7617、RFC 9700、RFC 8018、SemVer、PEP 8、PEP 20、PEP 257、PEP 508、POSIX、GNU Make、Maven Surefire/Failsafe、Microsoft Learn、git、The Twelve-Factor App、OWASP、CWE、NIST SP 800-63B、JaCoCo、PIT、Google Java Style、Google Shell Style、ShellCheck、DORA（Accelerate 报告系列）、GitHub Docs（受保护分支与 required reviews）、阿里巴巴 Java 开发手册——**覆盖公共内容各"依据"行**，并逐条写明它支撑哪些条目
|===

> 说明：**"内在依据"（为什么这样要求、当初要解决的失效、真实失效实证）目前不另建主题文件**——它散落在**各规范文件自身的说明性段落与依据行、各提示词正文与公共片段、`PROMPTS.adoc` 的维护约定、以及本仓库工具的注释**里（与规则同处一处、是最贴近的落点，也最不容易漂移），例如 link:../specs/core/execution.adoc[]「为什么需要分级（业界依据）」、link:../specs/general/testing.adoc[]「验证总纲」的"防慢慢脱离初衷"、link:../specs/general/collab.adoc[]「资源限制与感知」的实证记录、link:../specs/general/encoding.adoc[]「换行符（行尾）」的执行失败说明、`prompts/_common.txt` 中 `primary`/`priority-rules` 的方向取舍。按 link:../specs/general/doc.adoc[]「注释与文档」"多余文档直接去除"，**不预建空壳主题**——某类内在依据确实需要独立成篇时（例如跨多处、需要长期追溯的实证与反例），再按 link:../specs/general/source.adoc[] 的判据新增主题文件并**同步登记到上表**（写文件与登记是同一个动作）。
