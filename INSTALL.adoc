= Agent 规范安装

在目标项目根目录创建入口文档，文件名取值：默认 `AGENTS.adoc`；根目录**已存在** `AGENTS.md` 时紧随其后用 `AGENTS.md`（先查 `AGENTS.adoc`，再查 `AGENTS.md`）。

**入口文档是本流程唯一持久化到目标项目的产物**：一次会话结束后，下一个会话（或另开的新实例）只能看到**落到项目里的文件**——故凡"要跨会话、跨实例继续成立"的信息（规范副本的落点、本机与项目共用的那些约定）**一律写进入口文档内容**，不得只留在安装过程或会话里。**该写什么、不写什么**的判据（最小集、与临时产物的分界）见 `specs/general/entry-doc.adoc`——那是判据的唯一真源，本文件不重复其条文。

**本流程可重复执行、且以远程为准**：安装脚本（本文件与随规范分发的取文件抓手）会持续更新，任何时候再执行一次都应拿到**当前最新**的一份——入口文档按下面的形态幂等核对后**就地更新为最新入口占位**（保留项目自身内容），本地规范副本按**远程内容**核对、远程改过即以远程为准（详见「重复执行（更新）时的行为」）。

下面代码块内的内容必须**逐字原样**复制到新建的入口文档中：保留全部换行与空行，不得合并、折叠、删除或重排任何一行，不得改写任何文字——逐字符与代码块内容完全一致（仅去除代码块定界符 `----` 与语言标识 `[source,asciidoc]`）。代码块末尾几节（**规范与安装文档的位置**、规范副本位置、本项目持久化信息）**同属入口文档内容**：新开实例读入口文档即从这里知道**去哪儿重装/更新、怎么再取一次规范**、副本落在哪、哪些约定须跨会话保留；**安装须知本身不抄进来**——只给路径，详述以安装文档为准。

创建入口文档，内容为：

[source,asciidoc]
----
= Agent 规范入口

本项目的 agent 执行规范入口为：

https://agent.c332030.com/AGENTS_COMMON.adoc

安装与更新的文档入口为（要重装、要更新、要看完整的取法与退路时，**直接读它**，不必在本文件重复摘抄）：

https://agent.c332030.com/INSTALL.adoc

取规范脚本（要再取一次规范、或本机没有副本时用它）：

https://agent.c332030.com/script/fetch-specs.py

优先取到本地副本（避免网络原因无法访问）；取不到时就直接读上面的远程入口；需要最新规范时再运行一次取规范脚本即是更新。

规范属强制约束：**开工前必须先读取规范再执行**，不得因未读取/记不全而跳过或放宽任何条款。

读取该入口及其引用的 specs/ 规范，并持续遵守其全部要求。

== 规范与安装文档的位置

* 规范入口：https://agent.c332030.com/AGENTS_COMMON.adoc
* 安装文档（要重装、要更新、要看完整的取法与退路时读它）：https://agent.c332030.com/INSTALL.adoc
* 取规范脚本（下一次怎么下载规范、怎么再取一次）：https://agent.c332030.com/script/fetch-specs.py

== 规范副本的位置

* 默认落点：本机用户级缓存（所有项目共用一份），位置由平台/系统的缓存目录决定（Linux/macOS 取 `XDG_CACHE_HOME`、未设时用 `~/.cache`；Windows 取 `%LOCALAPPDATA%\.cache`），不钉具体内容。
* 项目内落点：`tmp/agent-specs/`（缓存不可用的退路）；要项目内副本时给 `fetch-specs` 加 `--local`。
* 取回方式：再运行一次取规范脚本即是更新。

== 本项目持久化到入口文档的信息

* 尚无（本节只写本项目自己的约定，不由模板预填；照着装出来的项目不应带着一份与它无关的清单）
----

同时将 tmp* 加入到 .gitignore

== 取规范到本地副本

入口的"取到本地副本"不要手工逐条下载：规范集合由 `script/fetch-specs.py`（跨环境脚本）承载**一次取全**，各平台入口已备好、调用方只给脚本名即可：

* 脚本地址：https://agent.c332030.com/script/fetch-specs.py（Linux/macOS 另有 `fetch-specs.sh`、Windows 另有 `fetch-specs.bat`，与脚本同目录、主名相同）。
* 用法：把脚本（与其平台入口）取到任意位置，在**项目根目录**执行一次即可；来源可用 `--base` 改；**默认以远程为准**——每份文件取回的字节与本地不同才落盘，远端改过就刷新、一致则不动，重复执行**即是更新**（要"不动本地那份"加 `--keep`）。**取不到时按退出码判**（0 成功、1 有文件没取到、2 参数或前置条件错误），非 0 就别把副本当已就绪。
* **落点默认是本机用户级缓存**：默取平台用户级缓存目录（Linux/macOS 取 `XDG_CACHE_HOME`、未设时用 `~/.cache`；Windows 取 `%LOCALAPPDATA%\.cache`）下按来源分槽的目录，**同一台机器上的所有项目共用这一份**；缓存内容按来源分槽，换过来源的副本互不覆盖；脚本**不删除缓存里的任何文件**。加 `--local` 改取**项目内临时目录 `tmp/agent-specs/`**（入口模板里的退路落点，按临时产物随项目清理，故 `tmp*` 须加进 `.gitignore`）；缓存不可得或来源是本机地址时**自动退回项目内落点**；可用 `--cache-dir` 或环境变量 `AGENT_SPECS_CACHE` 改缓存落点。
* **清单从入口自身解析**：脚本按 `AGENTS_COMMON.adoc` 的加载调度器登记自动解析要取哪些 `specs/...`，故新增一份规范**不需要改脚本、也不会漏取**——不要自己另外拼一份文件清单。
* 这份本地副本只用于读取（相对解析基准不变），**不替代入口地址**；网络正常时也可直接读远程入口。

* **解释器兜底（不必先自己装 python）**：两个入口都按次序探测解释器、用探测到的那个运行逻辑代码：
** Linux/macOS：先取 `python3`、没有再取 `python`——覆盖**只装了 python2** 的发行版（CentOS/RHEL 8 及更早、老 macOS）。
** Windows：先取 `py -3` 启动器、再取 `python3`、最后 `python`——覆盖只装了官方 py 启动器、或只有 Store 别名的机器。
** **连一个都没有**时脚本报一句错并退出非 0，**不静默继续**。此时按你的环境装一个 python 3 再重跑，或直接读远程入口地址。

== 取规范时连 python 都没有

上面那一步需要**一个 python 解释器**（逻辑层是 Python 脚本；两个入口已按次序探测）。**机器上一个 python 都没有**时，取规范这一步会**报一句错并退出非 0**（错误信息与**退出码**：脚本报错、**非 0** 退出，**不得静默继续**）——**此时规范仍然要取到**，按下面的次序走：

注意这里说的是**一个都没有**：只装了 python2 的发行版不算，上一条的次序会取到 `python`，脚本照样能跑。

* **先按本平台既有的软件分发方式装一个 python 3**（Linux 用发行版包管理器、macOS 用 Homebrew/官方安装器、Windows 用 `py` 启动器的官方安装器）。这是**人**的一步，**不是脚本的一步**——入口**不得**代为安装/下载运行时：它会改变系统状态、要管理员权限、失败方式因环境而异，且"装什么、从哪装、改了什么"对调用方不可预期。
* **本脚本报错退出，不等于"这一步可以跳过"**：退出码 1/2 是给调用方判定的信号，**不是**"继续往下走也差不多"。
* **让 AI 在能看见命令与报错的会话里去做**：这类"环境缺件"的处置交给 agent 在本机正常执行即可，但**不得**在入口或非交互执行里静默进行。
* **不装解释器的替代路径**：如果连装一个运行时都不允许（离线、无权限、精简镜像），**直接读远程入口地址**即可——本规范在网络上就是这套文件，不取副本也能读。**不建议**改用 `curl … | python3 -` 那条远程执行形态绕开：**它同样要 python**。

**取不到不该是终点，但取到的方式不该由入口决定**：把"怎么让这个运行时可用"留给**人**（或能看见报错的会话里的 agent），入口只负责"没有就报清楚、退出非 0"。这样"取规范"这一步失败时，你能一眼看出卡在**环境缺件**、而不是卡在规范本身。

== 重复执行（更新）时的行为

安装不是一次性的：本文件与随规范分发的抓手都在更新，故**任何时候都可以再执行一次**，按"**以远程为准**"把事情做对——判据是**内容**、不是时间：

* **入口文档（幂等）**：先按上面的规则判命中哪个文件名（`AGENTS.adoc` → `AGENTS.md`，取**第一个命中者**），再核对其内容：
** 已含上段代码块的入口占位、且**与最新模板逐字一致**（含换行与空行）：不动。
** 已含入口占位但**与最新模板不一致**：**就地更新为最新模板的那几行**，保留其后的项目自身内容；**不得**因此改名、迁移或另建第二个入口文档。
** **没有**入口占位：**在开头融合**（它可能与项目自身规范合写在同一文件里，此时不新增文件、只在开头补上入口占位）；若该文件已指向本入口，不重复追加。
** 模板末尾的**「本项目持久化到入口文档的信息」这一节**：**只补缺失的那一节、不覆盖已被填入的项目内容**。
** 该文件的其余内容**（项目自身规范、约定）：**原样保留**。
* **规范副本（以远程为准）**：在项目根目录再跑一次取文件抓手即可。它把清单内每份文件**取回的字节与本地比较**，**不同才落盘**；取不回来时**保留本地已有的那一份**并如实报失败。结果分三态打印（新取 / 刷新 / 内容一致），按**退出码**判成败。
* **要"不动本地那份"时**加 `--keep`：它换来的是"本地副本可能落后于远端"，故**安装流程按默认的"以远程为准"走、不加它**。
* **报红就地修复**：任何一步非 0 或与预期不符，先如实说明卡在哪一步（网络、解释器缺失、参数），**不要**把"以前取到过一份"当成"这次装好了"。
* **与项目自身规范的关系（重复执行同样成立）**：项目自身规范优先于通用规范。本项目自己写的规则**不因重新执行安装而被改写或删除**；本流程只动入口占位与规范副本这两处。

== 已存在 AGENTS 文档

先检查目标项目根目录是否已存在 agent 规范文档，按 `AGENTS.adoc` → `AGENTS.md` 顺序取**第一个命中者**：

* 都不存在：新建 `AGENTS.adoc` 并写入上面的入口占位。
* `AGENTS.adoc` 存在：就在它上面融合，**不覆盖、不删除**、不另建 `AGENTS.md`。
* 仅 `AGENTS.md` 存在：在它上面融合（文件名保持 `AGENTS.md`，**不重命名、不迁移**成 `AGENTS.adoc`）。
* 融合做法：保留原文档全部内容，在开头添加上面的入口占位；若原文档已指向本入口，则不重复追加。项目自身规范优先于通用规范。

== 约束

* 只创建入口占位，不复制规范本体。
* 入口文档一律按**固定格式**（`= Agent 规范入口` 标题 + 入口地址 + **安装文档路径与取规范脚本路径** + 遵守要求 + 规范副本位置 + 本项目持久化信息）书写：`AGENTS.md` 同样承载该入口占位，不因扩展名不是 `.adoc` 就降级成一句"读某地址"或改写成别的格式。
* **持久化信息一律落入口文档**：凡"要跨会话/跨实例继续成立、且不属于项目自身规范"的内容写进上段代码块的「本项目持久化到入口文档的信息」一节；进度、日志一类临时产物仍按通用规范的 `tmp/` 约定处理——两者判据见 `specs/general/entry-doc.adoc`「最小集与边界」。
* 代码块内容须逐字原样保留（换行与空行不得丢失），禁止改写格式或压缩排版。
* 安装过程只新增入口占位与临时副本，不改动目标项目的其他文件；副本按临时产物处理（默认落在用户级缓存，项目内 `tmp/` 那份是缓存不可用时的退路，故 `tmp*` 已加进 `.gitignore`）。

完成后告知用户已创建或已融合（并说明落在 `AGENTS.adoc` 还是 `AGENTS.md`）。
