= Bash/Shell 脚本规范（技术栈层）

以下规范适用于 bash/sh 等 POSIX shell 脚本。跨所有脚本语言的通用格式规则见 link:../general/script.adoc[]，shell 脚本中的编码规则遵守 link:../general/coding.adoc[]。本文件只收录 shell 脚本专属规则。

== 文件头与解释器

* shebang 用 `#!/usr/bin/env bash`（优先用 env 定位解释器，便于不同环境）；必要时 `bash -eu` 也可在 shebang 中体现严格模式
* 脚本须可执行（`chmod +x`），以 `./脚本` 直接运行

== 严格模式

* 脚本开头须启用 shell 严格模式，常用组合：
  `set -euo pipefail`
  ** `-e` 遇到错误即退出；`-u` 使用未定义变量即报错；`-o pipefail` 管道中任一命令失败即整体失败
* 对预期会失败的单个命令（如用返回码判断的命令），显式捕获其退出码或在命令后追加 `|| true` 并按需判断，避免 `set -e` 误杀；不得靠关闭 `set -e` 来容忍错误

== 变量与引用

* 使用变量一律加引号（`"$var"`），防止空格/通配符被展开；数组元素、路径等尤其要加引号
* 使用未定义变量是错误：先给默认值再使用（如 `"${VAR:-default}"`），或确保已赋值；避免裸 `$var` 直接进入命令
* 命令替换用 `$(...)` 而非反引号，便于嵌套与阅读

== 参数处理

* 用 `getopts` 或位置参数 + 前置校验处理命令行参数；必填参数缺失时输出 `usage:` 提示并退出非 0
* 对每个位置参数做存在性/合法性校验，解析后再使用；引用参数变量时统一加引号

== 退出码

* `exit 0` 成功、非 0 失败；参数错误、依赖失败等不同失败可用不同退出码，并在头部注释说明
* 出错信息输出到 stderr（`echo "..." >&2` 或 `>&2`），成功结果输出到 stdout

== 错误处理

* 依赖外部命令时检查其返回码（结合 `set -e`，或在需要时 `if ! cmd; then ...; fi` 显式处理），失败时输出错误并退出
* 处理路径：涉及删除/覆盖/写外部资源时，先校验目标在预期范围内、必要时显式确认（与 link:../general/script.adoc[]「安全」一致）
* 临时文件：优先写入目标项目 `tmp/`（见执行原则），并用 trap 在退出时清理（`trap 'rm -f "$tmp"' EXIT`）

== 其他

* 代码结构与命名遵守 link:../general/coding.adoc[]；shell 脚本中避免不必要的子进程与管道滥用，保持可读性
